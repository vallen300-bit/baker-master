# Secrets
config/.env

# Python
*.pyc
__pycache__/

# regen_hot_md.py side-effect logs (regen_failed.log + regen_diff.log)
scripts/regen_failed.log
scripts/regen_diff.log

# Outputs
analysis_outputs/

# Gmail OAuth credentials (stored in Render Secret Files)
config/gmail_credentials.json
config/gmail_token.json
config/gmail_poll_state.json

# PL working files
EMAIL-FIX-1_items1-2.patch
PL-2_HANDOVER.md

# Session outputs (local artifacts, not deployed)
# NOTE: outputs/ is production code (dashboard.py, static/) — do NOT ignore the folder itself
strategy/
!docs-site/strategy/
pm/scheduled-tasks/

# Generated deliverables in outputs/ — text artifacts archived, binaries ignored
outputs/*.png
outputs/*.pdf
outputs/build_*.py
outputs/convert_*.py
outputs/generate_*.py
outputs/share-site/

# Claude Code worktrees (parallel sessions)
.claude/worktrees/

# Subagent definitions live at user-global ~/.claude/agents/ post-HARNESS_SUBAGENT_MIGRATION_1
.claude/agents/

# Python venv (local dev only — CI and Render create their own)
.venv*/
venv/

CLAUDE.local.md
.r2_creds.tmp.json

# C3 gate-runner evidence logs (generated artifacts)
scripts/c3_gate/_runs/
