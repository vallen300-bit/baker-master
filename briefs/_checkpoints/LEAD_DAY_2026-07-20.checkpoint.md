---
brief_id: LEAD_DAY_2026-07-20
attempt: 1
owner: lead (AH1)
status: IN PROGRESS — day session after night watch; cockpit revamp 6/9 live
---

# Lead day checkpoint — 2026-07-20 (~11:20Z)

## Closed today (all merged + live + verified)

1. WAKE_DISPOSITION_REWAKE_1 phase-2: lab main @0124451, listener+plist
   deployed (WAKE_RECEIPT_URL), b1 live AC PASS #13923. Arc CLOSED.
2. Sweep guard: baker @061c884e (codex PASS #13901); forge scripts converged,
   drift alarm root-fixed (deployed hooks WERE the unmerged branch — never
   blind-run installer on drift; diff vs unmerged branches first).
3. Split view + sidebar (revamp items 4+5): PR #612 @484da27b (codex PASS
   #13934, 218 tests), static synced, live. No-blur = hard AC, test-locked
   (test_grid_never_blurs_behind_an_open_card), incl. #msgveil scrim.
4. BUS_DRAIN_PER_TURN_1: merged @7040ca6b (4 codex rounds → PASS #13973),
   installer converged, live AC PASS both directions (probe #13977 auto-
   rendered; deputy-codex verdict #13983). Mid-session hand-paste era closed.
5. deep55: removed from cockpit (layout commit + static sync + ttyd bootout)
   and wake fleet (BRISEN_LAB_AUTOWAKE_DISABLED_SLUGS env, already held);
   tmux seat + shell picker KEPT per Director. Registry dormant-note pending
   vault commit.

## Director ratifications today (recorded in project memory)

- Wake design COMPLETE: wait-for-idle + deferred re-wake + per-turn drain;
  no barge-in tier; Director direct-types for urgency.
- Lab unification starts ONLY after revamp items 1/8/9 ship.
- Baker dashboard embeds into unified Lab (bridge pattern, no code merge).
- COCKPIT_REVAMP_STATUS_20260719.html = living registry; update at every
  status change (standing obligation).
- No escalation-siren brief; observe operations for one week (from 07-20);
  stale-inbox alarm = red card + re-wake + daily smoke.

## In flight (check bus first)

- ctx-bars item 1: deputy-codex fixing pane-hash regression (self-audit
  #13921); then codex gate → merge → resync static → registry HTML update.
- LAB_GLANCE_ZERO_COUNT_FIVE_SEATS_1 @22481753: b1 diagnose-first (#13938).
  5 seats: movie/origination/publisher/researcher/russo-ai. Diagnose gate
  hard: failing test before fix.
- Split-view items 8+9 (history tab + verdict cards): brief AFTER ctx-bars
  lands — write FORMAL briefs (AH2 audit #13980: mailbox dispatches bypassed
  brief SOP on 7/8 PRs this week; tighten).
- ARM housekeeping G0: cowork-ah1 lane, round-3 REQUEST CHANGES (F9 TOCTOU).
- Vault reconcile owed (isolated worktree): cowork-ah1 deltas #13974
  (PINNED §A, activity-log, AO CONTEXT.md, meeting-prep REV12) + deputy's
  ah2/v2-audit-marker-20260720 ff-merge + deep55 registry dormant-note.

## Watch list (week of 07-20)

- Bus congestion: repeated bus_busy_retry/503 windows today; if persistent →
  brief candidate (Lesson #123 capacity cliffs).
- Stale-inbox week watch: red cards lingering >10 min = alarm; revisit
  escalation tier only if observed.
- Per-turn drain note (non-blocking, codex #13973): top-level JSON array
  responses don't advance cooldown/state — fold into next drain touch.

## Method notes

- Lab tests: TEST_DATABASE_URL_BRISEN_LAB via op item l52lf6yww3p4zkjbyfjnbax4jq
  (vault "Baker API Keys", field credential). 48 listener tests, 0 skipped.
- Controller wake: POST /api/sessions/{slug}/wake (basic auth, port 7800).
  "no wake obligation" refusal is CORRECT when glance shows 0.
- Director cockpit tabs: ghost tab on dead port 7809 caused false "no
  messages" Copy — check tab URL before diagnosing cockpit bugs.
- Daemon lifecycle msgs to lead (13915-13917, 13946-13947) — ack 403
  not_party class, leave.
