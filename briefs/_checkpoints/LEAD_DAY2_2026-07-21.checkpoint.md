---
brief_id: LEAD_DAY2_2026-07-21
attempt: 3
owner: lead (AH1)
status: session 4 afternoon — bus outage root-caused + fixed @5cce9ae (minconn raise supersedes #14473 scope), b3 cookie fix MERGED @cd681ecf, ctx-band gate FAIL awaiting deputy-codex re-ship
---

# Session-4 delta (~14:40Z) — READ FIRST, supersedes session-3 state below

## Bus outage arc (the day's spine) — RESOLVED, soak pending
- ~13:00-14:25Z full bus outage: /healthz 200 but ALL /msg GET/POST hung (0 bytes, 30s+). Two restarts (lead via Render API srv-d7q7kvlckfvc739l2e8g, cowork-ah1 independently) did NOT clear — restart-proof.
- Diagnosis (lead, evidence-run): direct psql via app DSN instant; pg_stat_activity clean (901 max_conn, ~43 used) → Neon healthy, hang app-side. Render logs: 503 budget-exhaust walls STILL firing post-d5a19e2 (b1 Option A merged by an intermediate session — see local brisen-lab log; also f8162c3 healthz commit-marker, lead recovery op). Root: psycopg2 free list caps at minconn; minconn=1 hardcoded = ONE warm conn; fleet load ⇒ fresh Neon TLS handshake per request ⇒ probe-budget exhaustion + threadpool starvation. Capacity, not poisoning.
- Fix: BUS_CONGESTION_MINCONN_RAISE_1 @5cce9ae (brisen-lab main, lead recovery op, POST-HOC CODEX GATE OWED like f8162c3): db.py env knob BRISEN_LAB_POOL_MINCONN (default 4, clamp [1,maxconn]); Render env set =8 BEFORE push; b1's minconn=1 guard test rewritten to new invariant (test_pool_created_with_configured_minconn). 373 passed; 5 pre-existing failures (registry drift) verified on clean main via stash.
- Supersedes #14473 Option-B rejection; b1 told (thread #14598) + asked to run 30-min soak + POST_DEPLOY_AC_VERDICT vs 5,311/hr baseline against tip 5cce9ae. One post-fix probe dropped (HTTP:000) — soak is the verdict, not my 3 probes.
- Render API access confirmed via `op read 'op://Baker API Keys/API Render/credential'`; brisen-lab=srv-d7q7kvlckfvc739l2e8g. Env-var PUT + /restart + /v1/logs all work.

## Merges + gates this session
- b3 ARRIVALS_COOKIE_REFRESH_ON_VISIT_1 PR #617 @1cc34057: codex PASS no-findings #14587(2) → MERGED @cd681ecf baker-master main via bm-b4 (branch restored). Render deploying. NEXT: Lab arrivals-embed live AC in Director's Chrome (one top-level /arrivals visit → iframe reload) → registry item 11 LIVE → Phase A joint AC CLOSED.
- deputy-codex COCKPIT_CTX_BAND_LIVE_REFRESH_1 @09ed2a3: codex FAIL #14587(1) — (a) cockpit.js/css stale-render changes UNCOMMITTED in worktree; (b) statusline.sh:92 mv kills <alias>.current symlink; (c) P2 stale label contrast on light theme. Re-ship nudge posted (parent #14587). On re-gate PASS: merge → cockpit static resync + kickstart → live lag probe → tell Director bars trustworthy.
- b1 BUS_CONGESTION_DIAG_2 PR #170 @449b161 (observability-only): codex gate requested (idempotency lead-gate-diag2-449b161).
- Earlier gate request for b3 landed as #14599 deduped — codex had already gated unprompted.

## Bus admin closed
- 11-message backlog ACKed (14504-14598 list in bus-drain snapshot). Airport tickets #14559/#14570 checked in VALID (ARM RED + RECOVERY = same congestion incident, no new work). Researcher #14523 answered (wake-loop cause = outage; flag if recurs post-15:00Z). cowork-ah1 paste-block reply sent via Director (Neon healthy — do NOT touch compute; owes me SPEC_build_loop_orchestrator_v1 reply exchange after bus restore).

## Successor order (rewrites session-3 list)
1. b1 soak verdict → congestion arc CLOSE (+ formal day-2 capacity note discharge) + post-hoc codex note on 5cce9ae.
2. baker-master deploy → arrivals embed AC → registry item 11 LIVE → Phase A CLOSED → tell Director.
3. deputy-codex re-ship → re-gate → merge → cockpit resync/kickstart → ctx bars trustworthy notice.
4. codex verdict on PR #170 → merge diag.
5. Then Phase B (SKILLS+LOOPS) brief for b2 after Director click-through.

# ORIGINAL session-3 header (superseded)

# Lead day-2 checkpoint — 2026-07-21 (~11:35Z, session-3 refresh)

## Shipped this session (session 3)
- **Congestion arc:** codex re-gate r2 FAIL #14466 (TOCTOU) → b1 scope blocker #14470: warmer = structural prod NO-OP (pool minconn=1 → psycopg2 free list hard-capped at 1; free_start>=2 never true; instrumented free:0 all incident) → **lead ruling #14473: OPTION A** — get_conn budget-exhaust discards poisoned conn (fence lifted for that branch ONLY) + warmer STRIPPED from PR #169; B (minconn raise) rejected (stale surface), C rejected (new design). Codex scope notice #14474 + supersede of dup FAILs #14476/#14477 → #14497. b1 building round 3.
- **Redesign Phase A MERGED @5f5bfb3** (brisen-lab main; b2 @965be99, codex PASS-w-notes 3 rounds). Deployed + live AC run in Director's Chrome: shell + 6-entry sidebar + hash routing PASS; **Baker Dashboard embed PASS** (screenshot-verified, full cockpit in-Lab); **Arrivals embed FAIL** — legacy SameSite=Strict cookie never re-issues on plain visit (server only sets cookie on ?pin= path; @c6a99048's samesite=none IS live at tip, dashboard.py ~L2110/2127). Top-level /arrivals visit did NOT cure (probe-proven).
- **ARRIVALS_COOKIE_REFRESH_ON_VISIT_1 dispatched b3 #14494:** re-issue cookie on successful cookie-auth visits so legacy cookies upgrade transparently; CHIPS/Partitioned as fallback check only. On ship: codex gate → lead merge → Render → I re-run embed AC in Director's Chrome (reload arrivals iframe after ONE top-level visit).
- **COCKPIT_CTX_BAND_LIVE_REFRESH_1 dispatched deputy-codex #14475** (brief briefs/_tasks/COCKPIT_CTX_BAND_LIVE_REFRESH_1.md; ACK #14486, executing in .codex-worktrees/cockpit-ctx-band-live-refresh-1/). Anchor: Director caught b2 card 10% vs real 32% — writer fires only on prompt-submit, b2.current froze 2h16m mid-turn; render stale-dim too subtle. Fix = statusline-tee live writer (≤60s lag) + unmistakable stale render. At merge: lead runs cockpit static resync + kickstart.
- **Airport ticket #14490** (ARM out-of-band RED db_unreachable(cadence) email): checked in VALID #14498 — same congestion incident, no new work.
- Registry LAB_UNIFICATION_STATUS_20260720.html updated: items 10 (Phase A MERGED) + 11 (arrivals cookie BUILD) added.
- Director told: cockpit ctx bars UNTRUSTED until fix confirmed live; Lab click-through invited (steers Phase B).

## Open (successor order)
1. **b1 round-3 ship** → codex gate → merge PR #169 (brisen-lab) → 30min soak → POST_DEPLOY_AC_VERDICT vs 5,311/hr baseline (#14402/#14407).
2. **b3 cookie-refresh ship** → gate → merge (bm-b4 for baker-master) → Render → arrivals embed AC → registry item 11 LIVE → Phase A joint AC CLOSED.
3. **deputy-codex ctx-band ship** → gate → merge → cockpit resync + kickstart → live lag probe → tell Director bars trustworthy.
4. Then: **Phase B (SKILLS+LOOPS rebuild) brief** for b2 — after Director click-through feedback.
5. Congestion day-2 capacity brief OWED (from overnight memory) — partially discharged by the fix arc; close formally after soak verdict.
6. Dup-post plague: codex nudged (#14497) to use idempotency keys on gate verdicts.

## Mechanics (unchanged from attempt 1 + additions)
- Lead merges brisen-lab via ~/bm-lead-brisen-lab (main); baker-master via bm-b4 main (Lesson #131, restore b4 branch after).
- Bus congested ALL session: every post/ack needs warm-conn trick (health-GET `--next` real request) + retry loop; run in background, idempotency-key EVERYTHING (deduped:true = earlier "failed" attempt landed — proven again today #14475).
- Bus key: `. scripts/brisen_lab_terminal_key.sh; KEY=$(brisen_lab_read_terminal_key lead "")` — bare scripts/brisen_lab_terminal_key.sh call returns empty (source-only helper).
- List endpoint /msg/lead returns EMPTY bodies; per-message /msg/lead/{id} for content. JSON needs strict=False (control chars).
- Cockpit resync: `rsync -a --delete scripts/cockpit_static/ "$HOME/Library/Application Support/baker/cockpit/static/"` + `launchctl kickstart -k gui/$(id -u)/com.baker.cockpit-controller`.
- Director browser AC via claude-in-chrome skill → ToolSearch select tools → tabs_context_mcp first; lab tab had Baker+Arrivals iframes; arrivals top-level tab open at BRISEN AIR board.
