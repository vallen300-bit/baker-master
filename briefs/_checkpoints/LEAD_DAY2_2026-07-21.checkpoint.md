---
brief_id: LEAD_DAY2_2026-07-21
attempt: 1
owner: lead (AH1)
status: midday session 2 — redesign build running, congestion root-cause arc at re-gate, 2 merges live
---

# Lead day-2 checkpoint — 2026-07-21 (~10:40Z)

## Shipped this session (all codex-gated, receipts on bus)
- **Congestion arc:** b1 diagnosis ACCEPTED (#14402: stale_probe_budget 100% exclusive, pool near-idle; NOT saturation) → AC verdict PASS #14407 (deputy cc'd) → fix brief BUS_CONGESTION_KEEPALIVE_FIX_1 @461b4df5 dispatched #14408 → b1 ship r1 → codex FAIL #14429 (warmer drained free list; getconn CREATES below maxconn, never raises) → relay #14433 → b1 round-2 @aa7f2e4 (max-1 checkout + free-floor brake) → **re-gate #14459 PENDING**. On PASS: merge PR #169 (brisen-lab) → 30min soak → AC verdict vs #14402 baseline (5.3k/hr).
- **Redesign Phase A:** proposal amended w/ Triaga rulings block + committed @99a98ab5 (canonical ratified record; 4.5 plain-language strip NOT recorded — re-ask at Phase D). Brief LAB_REDESIGN_PHASE_A_NAV_SKELETON_1 dispatched b2 #14409. b2 prototype gate found split (#14414): Baker embeds OK, Arrivals blocked → lead ruling: honest fallback + server fix.
- **ARRIVALS_EMBED_COOKIE_FIX_1** (b3, @17327f7b): SameSite=None + frame-ancestors (arrivals + bus-console per lead ruling #14431) → codex PASS no-findings #14450 → **MERGED @c6a99048** (baker-master, Render deploying). b2 wires embed after deploy (unblock sent); Director may need one top-level /arrivals pin visit.
- **Cockpit boot-retry round 2** (deputy-codex @4afb8348, 3 gate rounds, PASS #14444) → **MERGED @7385c86d**; cockpit static rsynced + controller kickstarted (verified 401-gate answering). Arc CLOSED.

## Open (successor order)
1. **Re-gate #14459 verdict** → merge b1 PR #169 → soak ≥30min → POST_DEPLOY_AC_VERDICT vs 5,311/hr baseline (>95% target). Warm-connection curl trick for bus walls: health-GET `--next` POST reuses conn, sails through.
2. **b2 Phase A ship** → codex gate → merge → live AC → Director click-through. Then Phase B (SKILLS+LOOPS rebuild) brief.
3. **Live embed AC** for arrivals (with b2, after Render deploy of @c6a99048).
4. Registry LAB_UNIFICATION_STATUS HTML update owed after Phase A merges.
5. Duplicate-post plague: 7+ dupes today (b1×2, b2×1, deputy×4) — deputy hygiene-nudged #14457 (use idempotency_key); warmer fix is the real cure.

## Mechanics (unchanged + today's additions)
- Lead commits via bm-b4 main (Lesson #131); b4 branch b4/docs-cleanup-orphaned-reports restored after each use.
- Direct bus post shape: POST /msg/{to} JSON {kind:dispatch, body, to:[..], tier_required:B, topic, parent_id, idempotency_key} — ALWAYS key it.
- Ack: POST /msg/{id}/ack (no terminal segment). 503-swallowed responses usually LANDED — verify by idempotent re-post (deduped:true = landed).
- Cockpit resync: `rsync -a --delete scripts/cockpit_static/ "$HOME/Library/Application Support/baker/cockpit/static/"` + `launchctl kickstart -k gui/$(id -u)/com.baker.cockpit-controller`.
