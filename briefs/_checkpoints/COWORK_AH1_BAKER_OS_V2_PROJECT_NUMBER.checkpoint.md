# CHECKPOINT — cowork-ah1 — Baker OS V2 project-number + 1Password fix

**Type:** Director-facing computer-use + Baker OS V2 build coordination. Rollover at ~103% context, 2026-06-30. **Refreshed 2026-07-03 at ~72% (fable session) — see 0703 block below; all 0630/0701 state below it is CLOSED history.**

## REFRESH-READY STATE (2026-07-03 — top priority on respawn)
- **Canonical resume doc:** local memory `session_handover_2026-07-03_baker_os_v2_review_day.md` (auto-loads via MEMORY.md top entry) + shared `~/baker-vault/_ops/agents/aihead1/PINNED.md` §A-COWORK-0703. Read those two; this checkpoint is only the pointer.
- **TONIGHT (Director returns):** ratify desk-process review pack `_ops/build/baker-os-v2/05_outputs/desk-process-review-pack-cowork-ah1-20260703.md` (17 items) + five-workstream desk menu (OPEN cue) + URGENT BB Aukera uncommitted records (sign ~10 Jul). After ratification → lead restores 3 desk-wake env flags (PINNED §A-LEAD-0703b).
- **Desk wakes PAUSED (live, reversible)** per Director via #5149→#5150; queues intact.
- **Ratified today:** runbook main objective; D-29 two-surface (ClickUp + Control Tower; BB-AUK-001 content redesign deferred); Fable map (Fable 5 = AH1s only — NOT yet bussed to lead); two-tier memory split; V6 live map (implicit).
- **Awaiting lead:** #5093 room commit · #5109 step-2 BB pilot spec (queued fable session; 16 tickets at gate, outbound_events=0) · #5140 Mini install (ready; Tailscale 100.112.139.2).
- **Known breakage:** cowork-ah1 `/msg/inbox` 403 `reader_slug_mismatch` (own key) — same family as AO/hag ack-403; read lead replies via shared PINNED until fixed.
**Bus key:** cached at `scratchpad/.bus_key` (600). `export BRISEN_LAB_TERMINAL_KEY=$(cat <that>)` before any bus call — do NOT call `op` (see #2). Daemon `https://brisen-lab.onrender.com`; `GET/POST /msg/cowork-ah1`, ack `POST /msg/<id>/ack`.
**Repo state:** clone on stale branch `cowork-ah1/rollover-checkpoint-20260607`; live code = `origin/main` (d88abd8). Read live code via `git show origin/main:<path>` / `git grep <pat> origin/main`.

## REFRESH-READY STATE (2026-07-01 — top priority on respawn)
- **Box 5 briefs A–E ALL authored + handed + committed.** Merge state: registry #439 + A #440 + B #441 + C #442 all MERGED (dark). D (PR #443, SHA 39671d6) AWAITING codex G3 verdict #4773. E (BOX5_SOFT_FAST_LANE_1) staged in vault, queued behind D. All 5 briefs also in clone `briefs/BRIEF_BOX5_*.md` + shared `~/baker-vault/_ops/briefs/`.
- **LEAD (Terminal AH1) ROLLED OVER** (#4774/#4775, ~89% ctx; checkpoint main 3d78ff3 = `briefs/_checkpoints/BOX5_HARD_FAST_LANE_1.checkpoint.md`). Lead DELEGATED D's gate+merge to cowork-ah1 (or a fresh lead). **OPEN DIRECTOR DECISION:** does cowork-ah1 take acting-lead for D's gate+merge, or does Director spawn a fresh lead? I recommended cowork-ah1 takes it (🟢 GO pending). If taking it: on codex G3 #4773 PASS → G4 /security-review → squash-merge #443 → dispatch E (add pre-merge-refs adaptation note #4772); ask codex to route the D G3 verdict to cowork-ah1 (currently addressed to lead). On FAIL → route to b4.
- **Director-gated, NOT done:** A activation (flag `AIRPORT_CHECKIN_SWEEP_ENABLED`), BB seed run (matter_slug=`aukera`), the 4 deferred follow-ups (feed-widening, thread-continuity, VISIBLE_HOLD, soft-escalation — see list below).
- Bus: 0 addressed-to-me unacked (through #4775). Sweep method: scan `to_terminals contains cowork-ah1 AND acknowledged_at IS NULL` across the full 200-window (do NOT filter by id>last — that missed a gap, Director caught it).

## TWO OPEN THREADS (superseded — see REFRESH-READY STATE above; kept for history)

### 1. 1Password prompt — MID-ACTION, blocked on Director
- **Root cause (confirmed via Director screenshot):** the popup is the 1Password DESKTOP app authorizing the **"Claude" (Cowork) app** for CLI access ("Allow Claude to get CLI access", Brisen Capital SA). NOT a service-token call. It fires when `op` runs from the Claude app without the service token in that process.
- **Fix:** disable **1Password → Settings → Developer → "Integrate with 1Password CLI"**. Then `op` uses the service-account token (`OP_SERVICE_ACCOUNT_TOKEN`, set, reaches "Baker API Keys" + "Passwords") non-interactively → never prompts. Safe; re-enable if any secret fails to read.
- **DONE (2026-06-30):** Director unlocked; I unchecked **1Password → Settings → Developer → "Integrate with 1Password CLI"** via computer-use (confirmed unchecked via zoom). `op` now uses the service token non-interactively → no more "Allow Claude CLI access" prompts. If any agent's `op` later fails to read a secret, re-enable that checkbox. (Note: "Integrate with other apps", "MCP clients" left ON; only the CLI toggle changed.)
- Memory: `project_1password_prompt_open_diagnostic.md`. Lesson logged: verify `op` auth mode before claiming op behavior (I mis-diagnosed twice).

### 2. BRIEF_PROJECT_NUMBER_REGISTRY_1 — written, staged, dispatch requested
- **Brief:** `briefs/BRIEF_PROJECT_NUMBER_REGISTRY_1.md` (this clone) + staged shared at `~/baker-vault/_ops/briefs/BRIEF_PROJECT_NUMBER_REGISTRY_1.md` (322 lines).
- **What:** NEW `kbl/project_registry_store.py` — project-number registry. Format **`DESK-MATTER-###`** (e.g. `BB-AUK-001`), Director-ratified (codex #4679). One table `project_registry` (idempotent `CREATE TABLE IF NOT EXISTS`, mirrors `ensure_airport_ticket_table`) + 3 resolvers: `resolve_project_number` (hard lane) + `resolve_by_participant` + `resolve_by_alias` (soft-lane primitives, codex #4680). Validates matter_slug via `slug_registry.is_canonical`. + `tests/test_project_registry.py` (7 vertical live-PG). Additive, touches NO live code. Low / ~1.5h.
- **Guardrails (codex #4680, in brief):** number-alone never clears; fast-lane needs number AND (participant in manifest OR thread continuity); sender-only forbidden; soft-lane multi-signal logic lives in Box 5 (downstream, NOT this brief). Builder = a B-code (NOT Deputy/Codex).
- **State:** dispatch requested from `lead` = **bus #4681** (`dispatched_by: cowork-ah1`). **Awaiting lead to name the free B-code** (his dispatch lane; mailbox single-threaded). On respawn: poll bus for lead's reply; if he named a B-code, write `briefs/_tasks/CODE_<N>_PENDING.md` after clearing with lead, else let lead dispatch.
- **Confirm before non-pilot seed:** canonical Aukera/Annaberg slug (`slug_registry.canonical_slugs()`) + `DESK_CODES` values.

## BOX 5 BUILD — IN FLIGHT (2026-06-30 pm)
- **PR #439 MERGED** to baker-master main (squash 8284537; codex G3 PASS #4708 after 3 rework rounds + b4 self-audit + lead G4). `kbl/project_registry_store.py`, table `project_registry` (cols: project_number/match_key/desk_code/desk_owner/matter_slug/clickup_list_id/participants/aliases/status). This is the Box 5 Block-1 foundation. `extract_project_codes()` stays mine to build in Box 5.
- **Division of labor LOCKED (lead #4713):** I author BRIEF-A..E (own Box 5 design + did the second-pair). Lead dispatches + gates (codex bus) + merges (git single-thread). B-codes build, codex gates. No double-authoring.
- **Second-pair on codex-arch's Box 5 build brief DELIVERED** (bus #4711 to codex-arch, #4712 sequencing to lead). 9 blockers; both blind-spot fixes adopted by lead (#4713): reuse+extend `project_registry` (no fork of `project_number_registry`); runner EXTENDS existing `airport_ticketing_tick`/`run_tick` + reuses `scheduler_lease` + FOR UPDATE SKIP LOCKED (no new lease cols). Full review saved: `scratchpad/box5_review_synthesis.md`.
- **BRIEF-A AUTHORED + HANDED to lead (#4716):** `BRIEF_BOX5_RECEIPT_TTL_1` (Box 5 Build Order 1-2) — check-in reply-reader + stale-ticket TTL nudge against frozen `airport_tickets`. #439-independent. Staged `~/baker-vault/_ops/briefs/BRIEF_BOX5_RECEIPT_TTL_1.md` + clone `briefs/`. New `orchestrator/airport_checkin_reader.py` + `triggers/airport_checkin_tick.py` + ~6-line scheduler block + ONE additive idempotent ALTER (`last_nudged_at`,`nudge_count`) mirrored in `ensure_airport_ticket_table`. Ships DARK behind `AIRPORT_CHECKIN_SWEEP_ENABLED` (default false). Authored via /write-brief + explore→draft→verify workflow; verifier caught+fixed a hallucinated `bus_body` JSONB key. Gate: G1→codex G3→lead G4→lead merge. dispatched_by: cowork-ah1.
- **BRIEF-B AUTHORED + HANDED to lead (#4720):** `BRIEF_BOX5_SCHEMA_FOUNDATION_1` (Build Order 3-4) — additive `airport_tickets` terminal columns (`terminal_status` own CHECK, 6 states, NO VISIBLE_HOLD + 15 result fields) via `ensure_airport_ticket_terminal_columns` mirrored inline + `migrations/20260630_airport_tickets_terminal_columns.sql`; + idempotent gated BB pilot seed via new `scripts/seed_bb_pilot_registry.py` → #439 `register_project`. Verified PASS (origin/main f7b3250). Staged shared + clone. **SLUG — DIRECTOR RATIFIED `aukera` 2026-06-30** (#4737): pilot = "the Aukera financing in BB Desk." matter_slug=`aukera` (canonical, slugs.yml v23 L65), NOT annaberg. Makes BB-AUK-001 self-consistent (BB / Aukera / 001). **Merged seed scripts still hardcode annaberg** (`seed_bb_pilot` #439 + `scripts/seed_bb_pilot_registry.py` #441) — held un-run; I fold the corrected aukera seed into BRIEF-D (no throwaway micro-PR). Lead notified. **A/B COLLISION FLAGGED to lead:** BRIEF-A + B both extend `ensure_airport_ticket_table` + both add a migration → must NOT dispatch in parallel; serialize (lead's call on order).
- **MERGE STATE (as of 2026-06-30 pm):** BRIEF-A `BOX5_RECEIPT_TTL_1` MERGED PR #440 (63a2dd8, DARK, codex caught 2 P1 reliability bugs b4 fixed — replay-no-ACK + lost-escalation+escalated_at col). BRIEF-B `BOX5_SCHEMA_FOUNDATION_1` MERGED PR #441 (ddb2ea4, 16 terminal cols + 6-state CHECK live). Box 5 now has: registry #439 + receipt/TTL #440 (dark) + terminal schema #441.
- **BRIEF-C AUTHORED + HANDED to lead (#4739):** `BOX5_TICKETING_RUNNER_1` (Build Order 5) — extend `run_tick`: per-source cursor (`trigger_watermarks` key `airport_ticketing:email`) + FOR UPDATE SKIP LOCKED claim + status-guarded idempotent terminal write + kill-switch (`BOX5_FAST_LANE_ENABLED` default false → safe-default TICKET). Deterministic clears only (DUPLICATE/REJECT_NOISE); no D/E logic, no VISIBLE_HOLD. Verified; fixed 2 substantive ref errors (baker_actions column set, env var name) + 3 minor. Ships dark behind existing `AIRPORT_TICKETING_BRIDGE_ENABLED`. **C must MERGE before D/E dispatch** (D/E plug into C's classify hook, same file).
- **BRIEF-D AUTHORED + HANDED to lead (#4742):** `BOX5_HARD_FAST_LANE_1` (Build Order 6) — (1) NET-NEW `extract_project_codes` pure-regex F4 conflict pre-check in `kbl/project_registry_store.py` (reuses `_NUMBER_RE`); (2) hard fast lane in C's classify path: 1 code + active registry row + participant-binding → FAST_TICKET; conflict/no-row/no-binding/error → safe-default TICKET (never VISIBLE_HOLD); gated `BOX5_FAST_LANE_ENABLED`; (3) **aukera seed correction** folded (`seed_bb_pilot` kbl:304 + `scripts/seed_bb_pilot_registry.py:21` + test:318), seed still gated/un-run. Verdict PASS. **KNOWN PILOT LIMITATION:** thread-continuity has no clean signal on main → pilot v1 = participant-binding only; TODO = add `email_thread_id` col to `airport_tickets` (future micro-brief). Depends on BRIEF-C MERGED. Ships dark.
- **BRIEF-E AUTHORED + HANDED to lead (#4752) — FULL BOX 5 SET A–E COMPLETE:** `BOX5_SOFT_FAST_LANE_1` (Build Order 7) — manifest soft fast lane. Inserts after D's hard lane, before C's safe-default. ≥2 independent signals (`resolve_by_participant`+`resolve_by_alias` agree on same project_number) → ROUTED TICKET (matter_slug/desk_owner/manifest_match_signals/confidence set), NOT FAST_TICKET. Sender-only/single/conflict/error → safe TICKET, never VISIBLE_HOLD. Pilot v1 = participant+alias only. Extends C's `write_terminal_status` with 4 optional routing kwargs (backward-compat). Verdict NEEDS-FIX→fixed 3 (blocker: tests use fixture `CANONICAL_SLUG` 'alpha' not 'aukera' which is prod-only). Depends on C+D merged.
- **FLAG CONTRACT RATIFIED (#4749, my design call):** `BOX5_FAST_LANE_ENABLED` gates ONLY the D/E fast/soft lanes. Deterministic clears (DUPLICATE/REJECT_NOISE) + safe-default TICKET run ALWAYS under master `AIRPORT_TICKETING_BRIDGE_ENABLED`. My BRIEF-C AC4 self-contradicted the body (my defect; b4/codex caught it); body authoritative, AC4 superseded, no code change. Lesson: verify pass checks signatures not AC-vs-body consistency — applied to E's verify.
- **BOX 5 PIPELINE STATUS (2026-07-01):** A merged #440 (dark) · B merged #441 · **C MERGED #442 (squash 86ae607, dark; codex G3 PASS after 2 re-gate rounds — 4 P1s caught+fixed: cursor-strand, bus-fail-strand, dead no-keyword branch, blank-cursor 24h-fallback strand; lead G4 clean)** · D DISPATCHED to b4 (branch box5-hard-fast-lane-1, aukera seed folded) · E queued behind D. Registry #439 merged. Lead: dispatch order C→D→E (shared classify hook in airport_ticketing_bridge.py; can't parallel). Everything ships dark; activation = Director GO (flag flip) after the chain merges. BB-AUK-001 seed = matter_slug `aukera`, un-run until GO.
  - BRIEF-C: extend `run_tick` — per-source cursor via existing `trigger_watermarks`, SKIP LOCKED claim, status-guarded single terminal write, kill-switch flags.
  - BRIEF-D: hard fast lane — `extract_project_codes` conflict pre-check (F4 step 0) + `resolve_project_number` + auth binding + dup/noise + FAST_TICKET.
  - BRIEF-E: soft fast lane — `resolve_by_participant`/`resolve_by_alias`, ≥2-signal manifest.
  - VISIBLE_HOLD = its OWN brief (owner+TTL+escalation+sweep), NOT a casual enum add (#4677.7). Until it ships, route conflict/weak → FILE_UNSORTED/desk-review.

## BOX 5 DEFERRED FOLLOW-UP BRIEFS (tracked, author after A–E land)
1. **FEED-WIDENING / no-keyword coverage** (#4755→#4756, my ratify (a)): C's REJECT_NOISE = automated-sender ONLY; no-keyword arrivals stay prefiltered at `fetch_email_arrivals` (pre-existing). To fully honor #4677.2 liveness ("every raw arrival ends in a visible terminal outcome"), a dedicated brief broadens the fetch scan + REJECT_NOISE/FILE_UNSORTED's no-keyword arrivals with MEASURED precision. Separate from the dark runner (avoids false-rejecting legit no-keyword arrivals).
2. **thread-continuity `email_thread_id`** (from BRIEF-D): add a queryable `email_thread_id` col to `airport_tickets` (populated from `EmailArrival.thread_id` at reserve) so the hard-lane binding can use thread-continuity (the OR branch of #4679.2), not participant-only. Micro-brief.
3. **VISIBLE_HOLD** (#4677.7): its own owner+TTL+escalation+sweep brief. Until it ships, conflict/weak → TICKET/FILE_UNSORTED (never VISIBLE_HOLD — not in the 6-state CHECK).
4. **FAST_TICKET-on-soft escalation** (from BRIEF-E): once soft-lane precision is measured, consider letting a high-confidence soft match FAST_TICKET instead of routed-TICKET. Data-gated.

## BOX 5 DESIGN FOLLOW-UP (captured 2026-06-30, do not lose)
- **F4 ruling (Director-ratified, bus #4700→#4701→#4702):** `resolve_project_number` ships deterministic-first — single-return `Optional[dict]`, FIRST-IN-TEXT-ORDER. NO candidate-list return (no caller, YAGNI).
- **Box 5 owns multi-number conflict detection** per guardrail #4680 (weak/conflicting → VISIBLE_HOLD). When Box 5 is built, add a thin pure-regex `extract_project_codes(text)->list` (no registry hit); Box 5 calls it first: >1 distinct valid code = conflict → VISIBLE_HOLD, never fast-board; single code → `resolve_project_number`. Fold into the Box 5 design doc (`_ops/build/baker-os-v2/05_outputs/`).

## DONE THIS SESSION (no action needed)
- PR #438 (ClickUpClient singleton, 3 sites + CI guard + regression test) — MERGED by lead, squash `8759e49` on main. Closed.
- Baker OS V2 live audit filed: `_ops/build/baker-os-v2/05_outputs/baker-os-v2-signal-journey-audit-cowork-ah1-20260630.md` (bus #4670).
- Box 5 second-pair review filed: `_ops/build/baker-os-v2/05_outputs/baker-os-v2-box5-second-pair-review-cowork-ah1-20260630.md` (bus #4676). codex locked 7 decisions (#4677). Fast-lane second-pair (#4678).
- Bus acked through #4680. My posts: #4670 #4676 #4678 #4681.

## REGISTER STYLE
Director-facing = laconic V2 (Bottom line / CAPS headers / numbered ≤12-word items / math / Recommendation if options / 👉 YOU or 🟢 GO? end-cue). Hooks enforce Recommendation + end-cue.
