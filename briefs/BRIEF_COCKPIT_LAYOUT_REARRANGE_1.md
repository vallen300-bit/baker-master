# BRIEF: COCKPIT_LAYOUT_REARRANGE_1 — Finish the cockpit layout rearrange to mock-V3 fidelity

**Executor:** b2 · **Author:** deputy (AH2) · **Dispatch:** lead #12946 (Director wants cockpit finish parallelized)
**Base branch:** `lead/cockpit-layout-rearrange-wip` @ `019bb602` (baker-master) — take it over, do NOT branch from `main`.
**Design source of truth:** `COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html` (repo root, present on the branch).

## Estimated time: ~3–4h
## Complexity: Medium (mostly finish + fidelity + tests; no new backend)
## Prerequisites: local cockpit controller running at http://127.0.0.1:7800/ (see `.claude/how-to/lab-cockpit.md`); the WIP is already live as Director's preview on the local static dir (working tree == deployed at time of branch).

## Baker Agent Vault Rails
Relevant rails: `verification-surfaces` (Director-clickable cockpit page), `build-command-center` (fleet cockpit), `skills-and-playbooks` (lab-cockpit how-to).
Ignore unrelated rails: `bus-and-lanes`, `memory-and-lessons`, `loop-runner`, `standing-contract` — no bus/memory/loop change in scope.

## Context
Director hand-built a free-drag layout mock (v2 → v3) for the fleet cockpit and wants it finished. lead started the work on `lead/cockpit-layout-rearrange-wip` (5-file WIP: summary bar, sync/roster notes, CSS regroup ~570-line delta, launch-manifest tweak) and it is already showing as a local preview. This brief hands b2 the finish-to-fidelity + test + ship job so lead can parallelize. **The cockpit is a pure local client** — it builds NO backend; it consumes the controller's `GET /api/agents` + `cockpit_layout.json` and POSTs to `/api/sessions/<slug>/{start,go,wake}`. Every field and endpoint the mock needs **already exists** (verified below) — this is frontend fidelity + tests, not backend work.

### Scope — exactly 6 files (lead ruling, #12951)
The WIP branch `@019bb602` diff vs **current** `origin/main` is exactly these 6 files — this is the whole PR:
1–3. `scripts/cockpit_static/{index.html, cockpit.css, cockpit.js}` — static fidelity to the mock.
4. `scripts/cockpit_launch_manifest.json` — a **legitimate regenerate** (librarian removed, 29→28 seats); regenerate via `python scripts/generate_cockpit_manifest.py --write` (generated file — **never hand-edit**).
5. `scripts/cockpit_manifest_reconciliation.md` — reconciliation note; **rides along** (already on branch).
6. `COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html` — the mock; **relocate to `briefs/_plans/` before merge** (do not leave it in repo root).
Note: `scripts/cockpit_static/cockpit_layout.json` (plate/roster metadata) is **NOT** in this PR — the roster/plate composition already lives on `main`. If a roster item looks wrong (see fidelity checklist), flag it to lead as a separate concern; do **not** add `cockpit_layout.json` to this PR.

### Surface contract (ui-surface-prebrief skill, V1)

1. **User action:** Director scans every fleet seat and (a) opens a seat's live terminal, (b) sends GO/Enter, (c) clicks an attention seat to fire a `check bus #<id>` wake, (d) reads a seat's unacked bus messages in a panel.
2. **Backend route:** all pre-existing on the local B-1 controller `scripts/cockpit_controller.py`:
   - `GET /api/agents` — L801 `async def get_agents(request)` → `{"agents":[{slug, alias, port, session_up, ttyd_up, **GLANCE_FIELDS}], "lab_glance_ok": bool}`.
   - `POST /api/sessions/{slug}/start` — L825 · `POST /api/sessions/{slug}/go` — L833 · `POST /api/sessions/{slug}/wake` — L841 (D6: `send_wake` sends `check bus #<oldest-id> <topic>` + Enter into the tmux session, L547–557; 600s re-nudge dedupe).
3. **Endpoint contract:** `/api/agents` takes no params, returns per-seat glance fields `unacked_count`, `unacked_messages` (list of `{id, topic, created_at}`), `oldest_unacked_age_sec`, `unacked_topics`, `context_pct` (0–100, null-safe when the Lab band is absent), `is_working`, `needs_go`, `has_telemetry` (`GLANCE_FIELDS`, L47–66). The `/start|go|wake` POSTs take `{slug}` in the path, empty body, and are behind the same same-origin + Basic-auth middleware; `/wake` no-ops with `{"ok":true,"sent":false}` when the session is down or nothing is unacked.
4. **State location:** live fleet state = Lab `GET https://brisen-lab.onrender.com/api/v2/terminals` (proxied/derived by the controller, `context_used_percent` → `context_pct` per LAB_CONTEXT_BAND_EXPOSURE_1 #12055); plate/card metadata = `scripts/cockpit_static/cockpit_layout.json` (generated by `generate_cockpit_layout.py`, mirrors Lab `CONTROL_GROUPS` + registry + manifest).
5. **UI repo (= state repo):** `baker-master` — surface: the **local cockpit page** (`scripts/cockpit_static/`, served loopback-only at `127.0.0.1:7800/`). Not Slack, not the Render dashboard.
6. **Director surface preference:** already ratified — Director **authored the mock himself** (free-drag v2 → v3); the surface is his own local cockpit page. No alternative surface is in play.
7. **Gate-1+2 reviewer instruction:** Reviewers MUST run the static re-sync, load `http://127.0.0.1:7800/` (hard-refresh), and confirm the live page matches the mock + the endpoints return non-error. Code-shape review (XSS-safe vanilla JS, valid HTML/CSS) is necessary but NOT sufficient.

---

## Fix/Feature 1: Finish cockpit to mock-V3 fidelity

### Problem
The WIP branch renders the new scan-surface layout but is not yet a faithful, tested build of `COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html`. It must reach mock fidelity, pass its test suite, and re-sync cleanly to the served static dir so Director's live page updates.

### Current State (already wired on the branch — do NOT rebuild)
`scripts/cockpit_static/` on `@019bb602` vs **current** `origin/main`: `index.html` +27 (summary section `#stat-total/#stat-attention/#stat-terminals`, `#sync-note`, `#roster-note`, state legend, `fleet-columns` header, D9 `#msgpanel` bus-message dialog with Copy), `cockpit.css` ~+570 (regroup), `cockpit.js` +32 (most of the earlier JS delta already merged to main via the cockpit-bridge/UI-polish PRs — the remaining delta is small). The WIP `cockpit.js` already references `unacked_count`, `unacked_messages`, `context_pct`, the `/wake` POST, `msgpanel`, `amber`, `stat-total`/`sync-note`/`roster` (much of it now on main). So the summary bar, msgpanel, amber-on-unacked, wake POST, and context bar are **already scaffolded** — b2 completes and verifies them against the mock, not from scratch.

### Fidelity checklist (the mock's own legend is the acceptance spec)
Visual (should already be present — verify each against the mock):
- No AG numbers on cards.
- Narrower cards so the full fleet fits one screen.
- Cowork/APP seats visually recessed with inner shadow (`.card.app`); services marked `SERVICE`.
- Context line on driveable (terminal) cards, filling green→amber→red by `context_pct` (hidden/null-safe when absent).

Behaviors to wire/verify (endpoints all exist — see Surface contract):
- Amber card face when `unacked_count > 0`, with the count + the message IDs listed in the `#msgpanel` (Lab parity; data from `unacked_messages`).
- Clicking an attention seat fires the `check bus #<id>` wake via `POST /api/sessions/<slug>/wake` (the controller composes the text; the frontend only POSTs).
- Roster composition (**verify only — driven by `cockpit_layout.json`, which is on `main` and NOT in this PR's file scope**): confirm **Codex Arch** shows in the Control Tower plate as a status-only APP seat; **Interns** plate has **Deep55, Clerk Qwen, Clerk Haiku**; **Cortex** sits in the Engineering plate. If any is missing/wrong, flag lead — do not fix it by adding `cockpit_layout.json` to this PR.
- `#sync-note` reflects telemetry freshness; `#stat-total/#stat-attention/#stat-terminals` compute from `/api/agents`; `#roster-note` seat count matches the regenerated `cockpit_launch_manifest.json` (28 seats).

### Engineering Craft Gates
- **Diagnose:** N/A — not a bug. Feedback loop is still explicit: `pytest tests/test_cockpit_*.py` + a manual browser smoke at `127.0.0.1:7800/` after re-sync.
- **Prototype:** N/A — the mock IS the ratified prototype (Director's own free-drag v2→v3). Do NOT re-open design questions or invent new layout; match the mock. If the mock is ambiguous on a detail, ask lead — do not improvise a redesign.
- **TDD/verification:** applies. Public seams = the cockpit render + the `/api/agents`→card-face mapping + the `/wake` click path. The branch already carries `tests/test_cockpit_msgpanel.py`, `test_cockpit_mux.py`, `test_cockpit_wake.py`, `test_cockpit_card_geometry.py`, `test_cockpit_contrast.py`, `test_cockpit_controller.py` deltas — get them green and add one vertical test for any behavior the mock requires that isn't yet covered (e.g. amber-face-on-unacked, message-ID list in panel). Don't bulk-write mocks that couple to implementation.

### Implementation
1. Check out and continue the branch: `git fetch origin && git switch lead/cockpit-layout-rearrange-wip` (tip `019bb602`). Do NOT rebase onto unrelated in-flight work.
2. Walk the mock (`COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html`) top to bottom; for each legend item, confirm the live page matches after re-sync. Close any gap in `index.html` / `cockpit.css` / `cockpit.js` only.
3. `scripts/cockpit_launch_manifest.json` is a generated file — if it needs a refresh, run `python scripts/generate_cockpit_manifest.py --write` (it reflects the librarian removal, 29→28). **Never hand-edit it.** The `cockpit_manifest_reconciliation.md` note rides along as-is.
4. Bump the static cache reference if `index.html` pins `?v=N` on `cockpit.css`/`cockpit.js` (grep for `?v=`); the served page is StaticFiles-from-disk + hard-refresh, but keep any existing cache-bust convention consistent.
5. Before opening the PR, **relocate `COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html` from repo root to `briefs/_plans/`** (`git mv`).
6. Get the cockpit test suite green.

### Key Constraints
- **No backend changes.** `scripts/cockpit_controller.py` and its endpoints are out of scope — the contract already provides every field. If you believe a field is missing, STOP and raise it to lead; do not add a route.
- Vanilla JS only, XSS-safe (use `textContent` / `createTextNode`, never `innerHTML` with seat-derived strings) — matches the existing page.
- `context_pct` is null-safe: when absent, the context bar stays hidden — do not render a 0% bar as if it were real telemetry.
- Loopback-only, Basic-auth: do not weaken the same-origin/auth middleware or expose the page beyond `127.0.0.1`.
- Match the mock; do not redesign. Scope is exactly the 6 files in the Scope section — nothing else.

### Verification
1. `pytest tests/test_cockpit_msgpanel.py tests/test_cockpit_mux.py tests/test_cockpit_wake.py tests/test_cockpit_card_geometry.py tests/test_cockpit_contrast.py tests/test_cockpit_controller.py -v` — all green.
2. **Static re-sync (MANDATORY AC step):**
   `rsync -a --delete scripts/cockpit_static/ "$HOME/Library/Application Support/baker/cockpit/static/"`
3. **Hard-refresh verify:** open `http://127.0.0.1:7800/` (Basic-auth prompt), hard-refresh (Cmd+Shift+R), and confirm against the mock:
   - narrower cards, no AG numbers, APP seats recessed, context bars on terminal cards;
   - a seat with an unacked message shows amber + the message ID(s) in the `#msgpanel`;
   - clicking an attention seat fires the wake (session receives `check bus #<id> <topic>` + Enter);
   - Codex Arch (APP), Deep55 / Clerk Qwen / Clerk Haiku, and Cortex appear in the right plates;
   - summary stats + roster count are correct.
4. Ship via PR to `main`; request codex gate; hand the PR to lead to merge (b2 does not self-merge).

## Harness V2

- **Context Contract:** everything the worker needs is in this brief + the WIP branch (`lead/cockpit-layout-rearrange-wip@019bb602`) + the mock; no vault reads required. Endpoints/fields are verified against `scripts/cockpit_controller.py` (see Surface contract). Live probe surface: `http://127.0.0.1:7800/` (local cockpit, Basic-auth). No secrets in the brief.
- **Task class:** production implementation, Director-facing UI (local cockpit page); Tier-A merge path, codex-gated. No backend/auth change.
- **Done rubric / done-state class:** post-deploy AC verdict on the bus (`post-deploy-ac-bus-gate` convention) on topic `gates/cockpit-layout-rearrange-1` — each fidelity + behavior AC (narrower cards / no AG numbers / APP recessed / context bars / amber+message-IDs / wake click / summary + roster count) PASS/FAIL, with a screenshot of the live re-synced page attached.
- **Gate plan:** codex bus-seat gate on the branch tip (pre-merge) → lead line-read + merge → static re-sync (`rsync … cockpit_static/ → …/cockpit/static/`) + hard-refresh smoke → post-deploy AC verdict on `gates/cockpit-layout-rearrange-1`.
- **Report topic:** `gates/cockpit-layout-rearrange-1`.

## Files Modified (exactly these — the 6-file scope)
- `scripts/cockpit_static/index.html` — summary bar, roster/legend, msgpanel, columns header (finish to mock).
- `scripts/cockpit_static/cockpit.css` — regrouped layout to mock fidelity.
- `scripts/cockpit_static/cockpit.js` — summary stats, amber-on-unacked, msgpanel population, wake click, context bar (small remaining delta — most already on main).
- `scripts/cockpit_launch_manifest.json` — generated refresh via `generate_cockpit_manifest.py --write` (29→28); never hand-edit.
- `scripts/cockpit_manifest_reconciliation.md` — rides along (already on branch).
- `COCKPIT_LAYOUT_REARRANGE_MOCK_V3.html` → `git mv` to `briefs/_plans/` before merge.
- (Existing `tests/test_cockpit_*.py` already merged to main — run them green; add a vertical test only if a required mock behavior is uncovered.)

## Do NOT Touch
- `scripts/cockpit_controller.py` and any `/api/*` route — backend is complete and out of scope.
- The same-origin / Basic-auth / loopback middleware — security invariant.
- Anything outside the 6-file scope. In particular do NOT add `scripts/cockpit_static/cockpit_layout.json` (roster/plate metadata already on main) or any test/backend file to this PR.
- `~/Library/Application Support/baker/cockpit/credentials` — read-only, never hardcode.

## Quality Checkpoints
1. Live page (after re-sync + hard-refresh) matches the mock legend item-for-item.
2. Amber + message-ID panel proven with a real unacked message (Lab parity).
3. Wake click delivers `check bus #<id>` into the target session (watch the native Terminal mirror).
4. Roster: Codex Arch (APP), Deep55, Clerk Qwen, Clerk Haiku, Cortex all in the correct plates.
5. `context_pct` null case renders no bar (no phantom 0%).
6. Full `tests/test_cockpit_*.py` suite green; `bash -n`/`python -m py_compile` clean on touched files.
7. PR scoped to `scripts/cockpit_static/` + its tests only — no unrelated branch files.

## Gate-1 + Gate-2 reviewer instructions
Reviewers MUST run `rsync -a --delete scripts/cockpit_static/ "$HOME/Library/Application Support/baker/cockpit/static/"`, load `http://127.0.0.1:7800/` and hard-refresh, and confirm the live page matches the mock AND the referenced endpoints return non-error responses. Code-shape review (XSS-safe vanilla JS, valid HTML/CSS) is necessary but NOT sufficient (ui-surface-prebrief anchor 2026-05-19).
