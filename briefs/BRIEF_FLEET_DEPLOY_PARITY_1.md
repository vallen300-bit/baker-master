# BRIEF: FLEET_DEPLOY_PARITY_1 — installer `--check` hash-parity + fleet deploy manifest + two-sided drill set (written GO-gate per arming-ladder step)

> Case One reliability-hardening. Authored by deputy (AH2, standing bus-health owner) from the
> authoring queue lead greenlit at **#11158**, in order (this is item 1 of 2; RECEIPT_WRITE_DURABILITY_1
> is item 2). Folds **codex-arch #11125 findings 1 / 2 / 8 + finding-4** and the item-A root cause:
> **a "deploy" that lands in the repo/env but never reaches the live host is undetectable today** —
> the epoch-flip POST_DEPLOY_AC stalled ~25 min because a Render env PUT does not auto-deploy, and
> nothing surfaced the divergence until the Director noticed (verdict #11157). This brief makes
> "is what's running the same thing we shipped?" a machine question for **both** the host-side launchd
> fleet **and the per-seat client scripts** (`bus_post.sh` & siblings).
> **AMENDED 2026-07-14 (lead #11248, ratified):** folded in the **fleet client-script sync sweep** (F5)
> — the live CLIENT_STARTED_EMISSION_1 drill (verdict #11247) proved my own seat's `bus_post.sh` was
> stale and silently failed to emit `started` until pulled, so other seats are presumed stale too. Lead
> ratified this as the exact class this brief exists for: the client-code leg alongside the launchd leg.
> **Sequencing rider (binding, #11248): the client-script parity sweep runs BEFORE the arming-ladder
> step-4 drain measurement (acked-not-started ~0) — else the drain metric lies on stale seats.**
> **TO LEAD FOR REVIEW BEFORE WORKER DISPATCH.** Codex seats lifted (#9711) → cross-vendor codex
> correctness gate available; #9255 independent-verdict-before-merge holds.

dispatched_by: lead (pending review)
assigned_to: <builder — lead assigns after review>
task_class: fleet deploy-parity reliability tooling (baker-master `scripts/` — Leg A: the ARM launchd installer/`--check` family + fleet deploy-manifest + host parity checker; Leg B (#11248): a per-seat client-script parity sweep for `bus_post.sh` & siblings; shell + launchd + a small Python verdict helper; NO dashboard.py, NO bus daemon, NO DB migration)
Harness-V2: Context Contract + done rubric + gate plan inline.
effort: medium

## Context

**Context Contract.** Repo: **baker-master** only (`scripts/`). Two legs of the same "shipped == running" question:
- **Leg A — host-side launchd (F1–F4):** the ARM launchd family, confirmed: `scripts/install_arm_alarm_job.sh`, `install_arm_cadence_job.sh`, `install_arm_semantic_job.sh`, `arm_alarm_check.sh`, `arm_cadence_poll.sh`, `arm_cadence_drift_check.sh`, `arm_alarm_drift_check.sh`, plus `scripts/tests/test_arm_alarm.sh` + `test_arm_cadence.sh`. Deployed to a TCC-safe dir on the always-on ARM host.
- **Leg B — per-seat client scripts (F5, added #11248):** `scripts/bus_post.sh` (+ any sibling fleet-distributed client script the sweep enumerates) is checked out in EVERY seat's git clone (b1-b4, desks, cowork, aihead1/2, codex seats). "Deployed" here = the seat's working-tree copy matches the repo canonical (origin/main). The CLIENT_STARTED_EMISSION_1 leg lives here — a stale `bus_post.sh` silently skips the `started` emit (verdict #11247).

**NO brisen-lab, NO baker-master dashboard.py, NO Postgres migration, NO bus schema.** All shell + launchd + a small Python verdict helper. **No new service** — a manifest file + parity logic inside the existing `--check` subcommands + one fleet-sweep wrapper covering both legs.

**Relevant vault rails:** `verification-surfaces` (the `--check`/drift-sentinel family is the verification surface for host-side jobs), `memory-and-lessons` (Lesson: "push before declaring done" / "env var set but missing on deploy" — this brief is the generalization of both to the launchd fleet). **Ignore rails:** bus-and-lanes (no bus call — the alarm path is deliberately off-bus), standing-contract, skills-and-playbooks, loop-runner.

**The one-line problem:** three ARM launchd jobs (`arm-alarm`, `arm-cadence`, `arm-semantic`) each self-install to a deploy dir and each expose a `--check` drift subcommand — but **`--check` proves the deployed worker *parses*, never that it *matches the repo source it was cut from*** (`install_arm_alarm_job.sh:55-58` literally documents this: *"Health = installed + loaded + worker parses"*). So a worker merged in the repo but never re-installed on the host — the exact "shipped but not deployed" gap that stalled item-A — reads **CLEAN**. And when a job *does* fail at runtime (DB unreachable, a source going non-200), the tolerant exit-0 design degrades it to a **silent AMBER** instead of a loud RED, and the alarm that finally fires **drops the specific failure names**. This brief closes all four.

### SCOPE DEDUPE (MANDATORY — lead #9563 discipline). Already shipped / owned elsewhere; do NOT re-cover:
- **`--check` subcommand skeleton (presence + loaded + syntax + interval-clamp) — SHIPPED** in all three installers (`install_arm_alarm_job.sh:60-88` and siblings). This brief **EXTENDS** that block with a parity assertion; it does NOT rewrite the presence/interval checks. Diff against the live `--check` before editing.
- **Drift sentinels (`arm_*_drift_check.sh`) — SHIPPED.** They already bus-post/log when the job is not installed/healthy. This brief adds **parity** as a new drift signal they surface; it does NOT re-invent the sentinel loop or its off-band send.
- **Cadence snapshot freshness (`arm_cadence_poll.sh` DEGRADED snapshot + `arm_cadence_drift_check.sh`) — SHIPPED.** finding-2 is about **propagating** the existing DEGRADED state into a RED verdict, not adding a second freshness mechanism.
- **MISSING-marker policy (`arm_alarm_check.sh:39-44,83-84,165-168`) — SHIPPED** as AMBER-by-default with a `ARM_ALARM_MISSING_IS_RED=1` flip already wired. finding-4 does NOT add the flip — it makes the flip **safe to arm** by giving us a parity proof that "the producer is deployed and live," and folds the flip into the arming ladder.

## Problem (four findings, one theme: a host-side job can diverge from what we shipped, and fail silently when it does)

1. **`--check` has no repo↔deployed parity assertion (finding-1 root; item-A generalization).** `WORKER_SRC="${SCRIPT_DIR}/arm_alarm_check.sh"` (`install_arm_alarm_job.sh:33`) is the repo source; `WORKER_DEPLOY="${DEPLOY_DIR}/arm_alarm_check.sh"` (`:37`) is what launchd actually runs. `--check` (`:60-88`) verifies `WORKER_DEPLOY` exists, is executable, is loaded, and *parses* — but never compares it to `WORKER_SRC`. A merged-but-not-reinstalled worker (repo ahead of host) is invisible. This is the launchd-fleet form of "env PUT didn't deploy" — the item-A stall class.
2. **No fleet-level view of deploy state.** Each installer answers "am *I* drifted?" in isolation; there is no single artifact that says "these N host-side jobs should be at these N repo shas on this host," so no one can run a **one-shot fleet parity sweep** or diff the deployed fleet against a known-good manifest. "Is the whole host-side fleet current?" is un-answerable today.
3. **A runtime failure degrades to a silent AMBER instead of a loud RED (finding-1 db_unreachable + finding-2 cadence DEGRADED).** `arm_cadence_poll.sh` tolerates any transient failure with exit-0 (`:29,33`) and only *logs* `"DEGRADED snapshot … (a source returned non-200/unparseable)"` (`:146`); when the underlying DB is unreachable the poller writes no fresh snapshot, the marker goes stale, and the missing-marker policy reads it **AMBER** (`arm_alarm_check.sh:83-84`) — a DB outage is indistinguishable from install-day quiet. A cadence that is *actively degraded* must produce a RED verdict, not a log line.
4. **The alarm drops the specific failure names (finding-8), and MISSING_IS_RED can't be safely armed (finding-4).** When the alarm does fire, the escalation should name *which* source(s) failed (`report:stale`, `canary:ok=false`, `db_unreachable`) — the verdict tuples already carry the key (`arm_alarm_check.sh:151` "`<key>\t<incident-type|detail>`") but the human-facing alarm must surface them, not a generic "a source failed." And `ARM_ALARM_MISSING_IS_RED=1` — the fail-loud posture where a never-seen marker is RED — can't be turned on today without false-paging, because we have no proof a producer is actually deployed. Parity (findings 1+2) is the missing proof that unblocks the flip.

## Fix (four pieces; each EXTENDS a shipped surface — no forks)

### F1 — Repo↔deployed hash-parity in every installer `--check` (findings 1 + 2)
In each of the three installers' `--check` block, add a parity assertion: `sha256` of `WORKER_DEPLOY` must equal `sha256` of `WORKER_SRC`; on mismatch emit `[FAIL] deployed worker drifted from repo source (deployed <sha8> != repo <sha8> — re-run install)` and set `rc=1` → `RESULT: DRIFT`. Use the same portable hash the repo already uses elsewhere (prefer `shasum -a 256`, fall back to `sha256sum`; a helper `_sha256()` in a sourced `scripts/lib/parity.sh` keeps the three installers identical — DRY, single definition). **The plist is parity-checked too:** the *rendered* installed plist encodes `StartInterval` + the worker path; assert the installed `StartInterval` still equals the value the current installer would render (the interval-clamp check at `:74-85` already reads it — extend to compare, don't add a second reader). Parity is **additive to** the existing presence/loaded/syntax/interval checks; a CLEAN result now means *deployed == repo*, not just *deployed parses*.

### F2 — Fleet deploy manifest + one-shot parity sweep (finding-2, fleet view)
Add `scripts/arm_fleet_manifest.json` (checked into the repo) enumerating each host-side job: `{ job_label, installer, worker_src (repo-relative path), deploy_dir_env, expected_interval_s, host_role }`. Add `scripts/arm_fleet_parity.sh` — a **read-only, zero-side-effect** wrapper that, for every manifest entry, invokes that installer's `--check` and aggregates: prints one line per job (`CLEAN` / `DRIFT <reason>` / `NOT-INSTALLED`), exits non-zero if **any** job is drifted or missing, and — critically — a job listed in the manifest whose installer/worker is **absent on the host is RED, never skipped** (`MISSING_IS_RED` applied at the fleet layer: a job we expect and don't find is a failure, not silence). The manifest is the single source of truth for "what the host-side fleet should be"; the sweep is the machine answer to "is it?". The sibling `arm_*_drift_check.sh` sentinels gain a call to `arm_fleet_parity.sh` (or reuse its `_sha256`) so parity drift surfaces off-band on the same cadence as install-health drift — do NOT add a new launchd job for the sweep; it rides the existing sentinels + is runnable by hand and in the drill.

### F3 — DB-unreachable / degraded → RED verdict carrying failure names (findings 1 + 2 + 8)
Make an *active* degradation loud, distinct from install-day quiet:
- **`arm_cadence_poll.sh`:** when a source is unreachable specifically because the **DB/endpoint connection failed** (vs a parseable non-200), tag the snapshot with a machine field `"health": "db_unreachable"` (not just the generic DEGRADED log). Keep exit-0 (launchd crash-only tolerance is deliberate — `:29`), but the *snapshot content* now carries the RED-worthy reason.
- **`arm_alarm_check.sh`:** a source whose latest snapshot reads `health=db_unreachable`, or a cadence marker that is stale **after having once been fresh** (already RED per `:42-43`), yields a RED verdict — and the emitted alarm body/subject **lists the failing source keys** from the verdict tuples (`:151`), e.g. `RED: db_unreachable(cadence), report:stale` — never a nameless "a source failed." This is finding-8: the alarm carries *what* broke.
- Preserve the E15 alarm-fatigue guard: one incident per `source-key + incident-type` (`:47`), recovery + re-arm unchanged. Adding names to an existing incident's body is not a new incident.

### F4 — MISSING_IS_RED flip folded into the arming ladder, gated on a passing two-sided drill (finding-4)
`ARM_ALARM_MISSING_IS_RED=1` is the fail-loud end state (a never-seen marker = RED). It ships **OFF** and is armed only at the end of a stepped ladder, each step an explicit lead GO, exactly mirroring the BTUNE arming-ladder pattern (`BRIEF_CASE_ONE_BTUNE_STARTED_SLO_TERMINAL_1.md` §Rollout). The **precondition for the flip is a passing two-sided drill** (below): parity must prove every producer marker's job is deployed + current on the host, so "missing = RED" cannot false-page a producer that was simply never installed.

### F5 — Fleet client-script parity sweep (Leg B; added #11248, ratified) — the client-code leg of "shipped == running"
Leg A proves the launchd *host-side* code is current; F5 proves the *per-seat client* code is current. `scripts/bus_post.sh` (and any sibling the sweep enumerates) is checked out in every seat's git clone, and the CLIENT_STARTED_EMISSION_1 `started` emit lives in it — a seat on a pre-#561 `bus_post.sh` silently skips the emit (verdict #11247, my own seat did exactly this mid-drill). Add `scripts/fleet_client_parity.sh` — a read-only checker that, for each enumerated client script, compares the **local working-tree copy's sha256 against the repo canonical (`git show origin/main:<path>` after a `git fetch`)** and reports `CLEAN` / `STALE <local-sha8> != origin <sha8>` / `UNTRACKED-MODIFIED`; non-zero exit on any stale/modified. It carries a `--capability-probe` mode that greps the deployed `bus_post.sh` for the load-bearing marker (`CLIENT_STARTED_EMISSION` / the `/started` emit block) so "stale" is reported as **"missing the started-emit capability"** in human terms, not just a sha delta. Because a seat's clone is only current after *that seat* pulls, F5 has two halves: (i) the **per-seat check** (each seat can run `fleet_client_parity.sh` locally, and the sweep is added to the seat's session-start/health path so a stale seat self-reports), and (ii) a **fleet roll-up** the dispatcher (deputy) runs to list which seats are stale — sourced from each seat's reported clone HEAD (reuse the existing forge-session/heartbeat telemetry if it already carries a commit, else a lightweight per-seat `git rev-parse HEAD` self-report; builder picks the cheapest honest signal and says which). Add `scripts/bus_post.sh` (+ siblings) to `arm_fleet_manifest.json` under a `client_scripts` section with `{path, capability_marker, distributed_to (seat class)}`. **Missing/unreachable seat telemetry = RED at the fleet layer** (a seat we can't confirm is stale-until-proven-current — same MISSING_IS_RED posture as Leg A). **Sequencing rider (binding, #11248): the F5 client-script sweep must run — and every seat report CLEAN — BEFORE the arming-ladder step-4 drain measurement (acked-not-started ~0), because the drain metric reads false-high on any seat that isn't emitting `started`.** The `arm_semantic`/dispatcher sweep surfaces the roll-up; do NOT add a new launchd job — F5 rides the seat session-start path + the dispatcher's existing sweep.

### The two-sided drill set = the written GO-gate per ladder step (the deliverable that makes each arming step provable)
"Two-sided" = every drill exercises **both** the repo/installer side **and** the live host side, so a green drill means the code is right *and* it's actually running:
- **Repo side (hermetic, in `scripts/tests/`):** extend `test_arm_alarm.sh` / `test_arm_cadence.sh` (senders/launchctl injected as recorders, per the existing suites) — (a) parity PASS: deploy the current worker, `--check` → CLEAN; (b) parity FAIL: mutate the deployed worker one byte, `--check` → DRIFT with the sha-mismatch `[FAIL]`; (c) fleet sweep: a manifest with one entry not-installed → `arm_fleet_parity.sh` exits non-zero + prints RED for that job; (d) db_unreachable snapshot → cadence marks `health=db_unreachable` → alarm verdict RED naming `db_unreachable`; (e) MISSING_IS_RED=1 with a never-seen marker → RED (proving the flip does what the ladder will arm).
- **Host side (live, in the drill runbook, run by deputy as bus-health owner):** on the actual ARM host — install → `arm_fleet_parity.sh` CLEAN; then deliberately stale one deployed worker (edit the deployed copy) → sweep detects DRIFT within one sentinel cycle; re-install → back to CLEAN. This is the **written GO-gate**: no arming-ladder step advances until its two-sided drill (repo test green **and** the live half demonstrated) is recorded.
- **F5 client-script drill (Leg B):** (repo/hermetic) a fixture clone whose `bus_post.sh` lacks the `/started` capability marker → `fleet_client_parity.sh` reports `STALE` + "missing started-emit capability", exit≠0; a current copy → CLEAN. (live) the exact verdict-#11247 repro — a seat on pre-#561 `bus_post.sh` replies with `--parent` → no `started` emitted (drain reads acked-not-started); pull → the same reply emits `started` within seconds → the seat's parity check flips to CLEAN. **The GO-gate for arming-ladder step 4: EVERY seat in the manifest's `distributed_to` classes reports F5 CLEAN before the drain measurement is trusted (#11248 sequencing rider).**

## Files Modified
- `scripts/lib/parity.sh` — **new.** Sourced `_sha256()` helper (portable `shasum`/`sha256sum`) so the three installers share one hash definition.
- `scripts/install_arm_alarm_job.sh`, `scripts/install_arm_cadence_job.sh`, `scripts/install_arm_semantic_job.sh` — extend each `--check` block with the F1 repo↔deployed sha-parity assertion + plist-interval parity (extend the existing `:74-85` reader; do not fork). Source `parity.sh`.
- `scripts/arm_fleet_manifest.json` — **new.** Leg A: host-side jobs (label, installer, worker_src, deploy_dir_env, expected_interval_s, host_role). Leg B (F5): a `client_scripts` section — `{path, capability_marker, distributed_to (seat class)}` — enumerating `scripts/bus_post.sh` (+ siblings).
- `scripts/arm_fleet_parity.sh` — **new.** Read-only host-side fleet sweep over the manifest's launchd jobs; missing job = RED; non-zero exit on any drift/missing.
- `scripts/fleet_client_parity.sh` — **new (F5).** Read-only per-seat client-script parity: local working-tree sha vs `git show origin/main:<path>` (after fetch), `--capability-probe` for the started-emit marker; STALE/UNTRACKED-MODIFIED = non-zero. Wired into the seat session-start/health path (so a stale seat self-reports) + a dispatcher roll-up listing stale seats (reuse forge-session/heartbeat commit telemetry if present, else per-seat `git rev-parse HEAD` self-report — builder states which). Unconfirmable seat = RED.
- `scripts/arm_cadence_poll.sh` — F3: tag snapshot `health=db_unreachable` on a connection-class failure (keep exit-0).
- `scripts/arm_alarm_check.sh` — F3: `db_unreachable`/degraded → RED verdict; alarm body/subject lists the failing source keys. F4: no new logic — `MISSING_IS_RED` flip is a ladder/config action, not a code change.
- `scripts/arm_alarm_drift_check.sh`, `scripts/arm_cadence_drift_check.sh` — surface parity drift (call `arm_fleet_parity.sh` or reuse `_sha256`); no new launchd job.
- `scripts/tests/test_arm_alarm.sh`, `scripts/tests/test_arm_cadence.sh` — add the F1–F4 hermetic cases above (senders/launchctl injected as recorders, matching the existing 52 + 20 checks).
- `scripts/tests/test_fleet_client_parity.sh` — **new (F5).** Hermetic: a fixture clone with a current vs a capability-missing `bus_post.sh` → CLEAN vs STALE("missing started-emit"); an unconfirmable seat → RED.
- Drill runbook (append to the ship report / a `scripts/tests/DRILL_arm_fleet_parity.md`): the host-side half + the F5 client-script half + the per-ladder-step GO-gate record (incl. the #11248 step-4 sequencing gate).

## Do NOT Touch
- The off-bus property of the alarm path — the alarm worker makes zero bus calls (test 1 asserts no `X-Terminal-Key`/`LAB_URL`/`/msg`); parity + naming stay off-bus. Do NOT add a bus post to the alarm/poll workers.
- launchd crash-only KeepAlive semantics + exit-0 tolerance in the pollers (`:29,33`) — a poller that starts exit-non-zero would make launchd back off and defeat the SLO. Degradation rides the *snapshot content*, not the exit code.
- The interval clamp `[60,300]` and the ≤300s SLO ceiling — extend the reader to compare, never loosen the clamp.
- baker-master `dashboard.py`, brisen-lab, any Postgres migration, the bus schema — all out of scope (Context Contract).

## Engineering Craft Gates
- **Diagnose:** applies. Feedback loop = the hermetic suites (`bash scripts/tests/test_arm_alarm.sh` / `test_arm_cadence.sh`, senders/launchctl as recorders — no real email/launchctl/DB). Reproduction of the item-A class: deploy a worker, bump the repo source one byte, run `--check` → today CLEAN (the bug), after F1 DRIFT (the fix). Hypotheses, ranked: (1) `--check` never hashes deployed-vs-repo [confirmed by read: `install_arm_alarm_job.sh:55-58,60-88`]; (2) a DB-unreachable failure degrades to AMBER not RED [confirmed: poller exit-0 `:29` + missing-marker AMBER `arm_alarm_check.sh:83-84`]; (3) the alarm body omits source names [verdict tuples carry the key `:151` but the send generalizes]. Probe/regression: the (b) parity-FAIL and (d) db_unreachable-RED tests.
- **Prototype:** N/A — no UI/state-model uncertainty; the mechanism (sha compare + manifest sweep + verdict tag) is deterministic and the surfaces already exist.
- **TDD/verification:** applies. Public seams = each installer's `--check` exit code + printed RESULT line, `arm_fleet_parity.sh` exit code + per-job lines, the cadence snapshot `health` field, the alarm verdict/emitted-name. Write the parity-FAIL test first (it fails today, passes after F1) — that is the honest vertical proof. Do NOT bulk-write all cases before the parity assertion exists.

## Verification
1. **Hash-parity (finding-1 / item-A class):** deploy the current worker → `install_arm_alarm_job.sh --check` prints `RESULT: CLEAN`. Mutate the deployed copy one byte → `--check` prints the sha-mismatch `[FAIL]` + `RESULT: DRIFT`, exit≠0. Re-run install → CLEAN. Same for cadence + semantic installers.
2. **Fleet sweep (finding-2):** `arm_fleet_parity.sh` over the full manifest prints one line per job; with every job current → all CLEAN, exit 0. Remove/rename one deployed worker → that job prints RED `NOT-INSTALLED`, sweep exits ≠0 (missing = RED, never skipped).
3. **DB-unreachable → RED with names (findings 1/2/8):** feed the cadence poller a source whose connection fails → snapshot carries `health=db_unreachable`, exit 0 (launchd tolerance intact). Run the alarm check against it → RED verdict, and the emitted alarm body/subject **names** `db_unreachable(cadence)` (+ any other failing keys). A parseable non-200 still reads DEGRADED, not a false RED. E15 guard intact: same incident re-runs suppress within cooldown.
4. **MISSING_IS_RED flip (finding-4):** with `ARM_ALARM_MISSING_IS_RED=1`, a never-seen marker reads RED (test (e)); with it OFF (ship default) the same marker reads AMBER (no regression to install-day quiet). The flip is armed live ONLY after the two-sided drill records parity-proof for every producer.
5. **F5 client-script parity (Leg B; #11248):** `fleet_client_parity.sh` on a current clone → CLEAN; on a pre-#561 `bus_post.sh` → STALE("missing started-emit capability"), exit≠0. Live repro of verdict #11247: a stale seat's `--parent` reply emits no `started` (drain reads acked-not-started) → pull → same reply emits `started` within seconds → seat flips CLEAN. Dispatcher roll-up lists every stale seat; an unconfirmable seat = RED. **GO-gate: all `distributed_to` seats CLEAN BEFORE the step-4 drain measurement is trusted.**
6. **Two-sided drill / GO-gate:** repo suites green (`test_arm_alarm.sh` + `test_arm_cadence.sh` + `test_fleet_client_parity.sh`, ~52+20 existing checks + the new F1–F5 cases, zero regressions) **and** the live halves demonstrated (host: install → sweep CLEAN → stale one worker → DRIFT within one sentinel cycle → re-install → CLEAN; client: the #11247 stale-seat repro above), recorded in the drill runbook. deputy runs the live halves as bus-health owner and posts `POST_DEPLOY_AC_VERDICT v1` after the live drill.

## Quality Checkpoints / Acceptance criteria
- **done rubric:** (1) all three installer `--check` blocks assert repo↔deployed sha-parity + plist-interval parity via a single shared `_sha256`, additive to the existing presence/loaded/syntax/interval checks; (2) `arm_fleet_manifest.json` (host jobs + a `client_scripts` section) + read-only `arm_fleet_parity.sh` give a one-shot host sweep where a manifest job absent on the host is RED (never skipped); (3) a DB-unreachable/degraded source yields a RED verdict whose alarm names the failing source keys, with the poller exit-0 + E15 guard intact; (4) `MISSING_IS_RED=1` proven to make a never-seen marker RED, shipped OFF, flip folded into a stepped arming ladder each step lead-GO'd; (5) **F5 (Leg B, #11248): `fleet_client_parity.sh` detects a stale/capability-missing `bus_post.sh` per seat (started-emit marker probe) + a dispatcher roll-up of stale seats, unconfirmable seat = RED, wired into the seat session-start path — and the step-4 drain measurement is GO-gated on every `distributed_to` seat reporting CLEAN;** (6) two-sided drill set (hermetic repo cases + live host + live client-#11247 runbook) is the written GO-gate per ladder step, with `POST_DEPLOY_AC_VERDICT v1` on the live drill.
- **done-state class:** host-side reliability tooling → live host drill AC required (a parity checker that mis-reports is worse than none — a false CLEAN re-buries the item-A class; the parity-FAIL case must be proven, not assumed).
- **gate plan:** deputy authors → **lead reviews BEFORE worker dispatch** → builder implements → **independent verdict BEFORE merge** (codex correctness, cross-vendor; codex seats lifted #9711; or lead picks a Claude-side B-code line-review; #9255 holds) → lead merges → host-side install/deploy → **deputy verifies live as bus-health owner** + posts `POST_DEPLOY_AC_VERDICT v1`. The `MISSING_IS_RED=1` flip is deferred to the arming ladder, each step lead-GO'd on a recorded two-sided drill.
- **Harness-V2:** Context Contract + done rubric + gate plan covered inline.

## Dedupe / cross-links
- **Root incident:** item-A epoch-flip POST_DEPLOY_AC stall (~25 min; Render env PUT does not auto-deploy; lead manual-deployed) — verdict #11157/#11158, logged in `briefs/_reports/A_CLEAR_AUDIT_2026-07-14.md`. FLEET_DEPLOY_PARITY_1 is the generalization: make "shipped ≠ running" a machine question for the host-side launchd fleet.
- **codex-arch #11125 findings 1/2/8 + finding-4** — folded per the F1–F4 map above (parity; fleet manifest/missing-is-red-at-fleet-layer; db_unreachable/DEGRADED → RED-with-names; the MISSING_IS_RED ladder flip).
- **F5 (Leg B) provenance:** CLIENT_STARTED_EMISSION_1 POST_DEPLOY_AC drill, verdict #11247 (my seat's `bus_post.sh` stale → silent no-emit), lead ratified the fold #11248 ("the client-code leg alongside the launchd leg; parity sweep BEFORE step-4 drain"). This is the same "shipped ≠ running" invariant applied to per-seat git clones instead of the launchd deploy dir.
- **Arming-ladder pattern** mirrors `BRIEF_CASE_ONE_BTUNE_STARTED_SLO_TERMINAL_1.md` §Rollout (kill-switch OFF at merge, each ladder step an explicit lead GO). Same discipline, different switch.
- **Companion (item 2 of the queue):** `RECEIPT_WRITE_DURABILITY_1` — receipt write-path repair + created_at-anchored epoch/obligation filter (fixes the finding-3 leak). Distinct object (bus-daemon receipt write path in brisen-lab); do NOT conflate with this host-side launchd parity work. Sequenced after this one.
- **Lessons applied:** "push before declaring done" + "env var set but missing on deploy" (`tasks/lessons.md`) — this brief is the fleet-wide enforcement of both: a deploy that doesn't reach the host is now RED, not silent.
