# Handover — AI Head (Claude Opus, Mac App) — 2026-04-19

**Date:** 2026-04-19 (afternoon, late)
**From:** AI Head (outgoing — Director-requested refresh)
**To:** Fresh AI Head instance
**Director:** Dimitry Vallen
**Supersedes:** `briefs/_handovers/AI_HEAD_20260418_v2.md`

---

## 🚨 READ BEFORE ANYTHING ELSE

**Director's durable rules (cumulative across sessions; all in memory at `/Users/dimitry/.claude/projects/-Users-dimitry-Vallen-Dropbox-Dimitry-vallen-Baker-Project/memory/`):**

1. **Fenced-block tasks only.** Every dispatch to Director for B1/B2/B3 goes in a triple-backtick code block so he can one-click copy. One block per agent per dispatch.
2. **Narrative only for Director-intervention.** Merges, rebases, process incidents, agent coordination = handle silently. Only flag when Director needs to act.
3. **Auto-merge authority.** B2 APPROVE + green CI + no blockers = I merge with `gh pr merge <N> --squash`. Architectural commits (CHANDA amendments, KBL-B/C ratification) still route to Director.
4. **Always include explicit recommendation** (`feedback_always_recommend.md`). Every A/B/C or forced-choice ask gets a dedicated `**Recommendation: <option>** — <one-line rationale>` line at the end. Director is speed-optimized; scan → accept → move on.
5. **Code agents in `~/bm-b{N}`, not `/tmp/`** (`feedback_code_working_dirs.md`). `/tmp/` gets wiped on macOS reboot — wiped B1 mid-flight once this session. AI Head can still use `/tmp/bm-draft` (stateless; re-clone on reboot).
6. **After each PR cycle: agents quit Terminal tab + start fresh.** Director's Mac hit 95 GB Terminal RAM this session from long-running Claude Code CLI agents. Already briefed all B codes to do this.
7. **Never touch Dropbox paths from agent shells.** Director confirmed Dropbox prompt seen for CHANDA.md earlier; cancelled. Work only in `~/bm-b{N}` or similar `~/` paths.
8. **CHANDA.md is Gold — Director only.** Bootstrap-creation exception applied at commit `915f8ad` in a prior session. No agent touches it.

**Trap to avoid:** don't say "I'm starting X" then spend 10 turns orchestrating instead of doing X. If you say you're writing, open the file and write.

---

## Who you are

You are **AI Head** — orchestration + brief-authoring + architecture-decision agent for Baker / KBL / Cortex 3T development. You coordinate three Code Brisen agents (B1, B2, B3), write implementation briefs, review architecture decisions, relay between Director and Code via mailbox pattern at `briefs/_tasks/CODE_{1,2,3}_PENDING.md`. You also orchestrate **AI Dennis** (IT shadow agent — deployed as Cowork skill this session) for IT/infra tasks.

You do NOT write production code. Code Brisens do.

You run in Claude App. B1/B2/B3 run in Claude Code CLI (Terminal). AI Dennis runs in Claude App Cowork (just deployed).

---

## 🎉 Major milestone this session — Cortex T3 Phase 1 SHIPPED

**All 7 pipeline steps on main:**

| # | Step | PR | Merge SHA |
|---|---|---|---|
| 0 | Layer 0 (intake filter) | #7 | `be21dc70` |
| 0.5 | CHECK constraint migration | #12 | `68db3568` |
| 1 | Triage (Gemma) | #8 | `6382ee50` |
| 2 | Resolve (Voyage + metadata) | #10 | `6a607a3a` |
| 3 | Extract (Gemma) | #11 | `312279a8` |
| 4 | Classify (deterministic) | #13 | `bc2efa9f` |
| 5 | Opus synthesis | #14 | `58ed935e` |
| 6 | Finalize (Pydantic + cross-links) | #15 | `4cb47e1a` |
| 7 | Commit (flock + git push) | #16 | `20370e7e` |

Code complete. **Pipeline NOT YET LIVE** — shadow mode not flipped; no real signals flowing through KBL-B yet. See "what's next" section.

---

## Agent roster + current workload (state at handover)

| Agent | Platform | Role | State |
|---|---|---|---|
| **B1** | Terminal CLI | Pipeline implementer | **IDLE**. Last ship: PR #16 S1 delta at `2e845d5` (merged). Terminal closed per memory-hygiene rule. Next dispatch draft READY at `briefs/_tasks/CODE_1_PENDING.md` but **NOT FIRED TO DIRECTOR YET** — pending Director's refresh of AI Head. |
| **B2** | Terminal CLI | Independent reviewer | **IDLE**. Last task: PR #16 S1 delta APPROVE at `afaa0d8` (merged). Terminal closed per memory-hygiene rule. |
| **B3** | Terminal CLI | Design + empirical + infra | **IDLE**. Last task: `MAC_MINI_LAUNCHD_PROVISION` complete — poller + heartbeat plists loaded on Mac Mini, 60s tick verified via 3 rows in `mac_mini_heartbeat`. Standing down. |
| **AI Dennis** | Cowork (Claude App) | IT Shadow Agent | **DEPLOYED, NOT YET INVOKED**. Skill lives at `~/.claude/skills/it-manager/SKILL.md` + plugin-dir equivalent. Triggers: "AI Dennis", "IT manager", "M365", etc. Director hasn't opened a Cowork window for him yet. Reserved for FUTURE IT work (M365, BCOMM, vendor mgmt). Not for Mac Mini prep — that was handled by B3 this session. |

### Reviewer-separation matrix (inviolable)

| Author | Reviewer |
|---|---|
| AI Head brief | B2 |
| B1 code | B2 |
| B3 prompt/rule/spec draft | B2 |
| B2 schema draft | B1 (reverse) |

---

## What shipped this session (2026-04-19, morning + afternoon)

### Merges (auto-merged on B2 APPROVE per durable rule)

- `bc2efa9f` **PR #13 STEP4-CLASSIFY-IMPL** (deterministic policy classifier, hot.md-driven allowed-scope, first-match-wins decision table)
- `58ed935e` **PR #14 STEP5-OPUS-IMPL** (Anthropic SDK wrapper with prompt caching, cost gate at €50/day, R3 retry ladder, transaction-boundary contract — YELLOW audit remediation folded in)
- `4cb47e1a` **PR #15 STEP6-FINALIZE-IMPL** (5 Pydantic models, 21 validation rules, Option C cross-link flow via dedicated `kbl_cross_link_queue` table with UPSERT idempotency, Inv 9 zero-FS-write test)
- `20370e7e` **PR #16 STEP7-COMMIT-IMPL** (flock mutex + atomic writes + rebase retry + TOAST cleanup + Inv 4 guard inside lock+rebase window per B2's race catch)

### Dispatches that did NOT become PRs

- **Fanout brief parked** to new `briefs/_future_optimization/` folder with README convention (Director ratification: "we need to get into production no matter what")
- **Mac Mini Step 7 prep** (B3 task `AI_DENNIS_MAC_MINI_STEP7_PREP.md`) — items 1-6 ALL COMPLETE: baker-vault clone healthy, pipeline git identity set, dedicated deploy key `149032903` active with write access, flock tested, `com.brisen.baker.poller.plist` + `com.brisen.baker.heartbeat.plist` loaded on Mac Mini via launchd, KBL-A legacy agents surgically retired to `.retired-2026-04-19`.
- **`mac_mini_heartbeat` migration** direct-to-main by AI Head at `5d0a366` (idempotent CREATE TABLE IF NOT EXISTS).
- **Fireflies index** committed by Director at `c201794` in parallel session (107 transcripts indexed, 8 active-slug coverage gaps flagged).
- **B3 SLUGS-V9-FOLD** landed at `50167a1` (worked examples `mo-vie → mo-vie-am`, `theailogy → m365`, hot.md blocks synced to v9).
- **B3 STEP5-WORKED-EXAMPLES-EXPAND** landed at `fceb22f` (§3 of Step 5 prompt grew from 3 → 7 examples: threat-vedana, opportunity-vedana, contradiction, stub-only boundary).
- **B3 STEP6-FINALIZE-SCHEMA-SPEC** landed at `ffa2a26` (5 Pydantic models, 21 validation rules, 12-class error matrix, 8 open questions).
- **AI Head OQ resolutions** at `bf1ae53` (all 8 Step 6 OQs ratified per B3's own recommendations).
- **AI Dennis skill deployment** to `~/.claude/skills/it-manager/SKILL.md` + Claude App plugin dir. Now available via "AI Dennis" / "IT manager" / "M365" triggers.
- **Ollama cleanup** — deleted `mistral-small3.1`, `qwen2.5:14b`, `all-minilm:latest` (~24 GB disk reclaimed). Kept `gemma4:26b` + `gemma4:latest` (pipeline use).
- **Tailscale activated** on dev Mac (was stopped; Mac Mini already on tailnet). `ssh macmini` now works via MagicDNS.

### New ratified decisions (fold into thinking; do NOT re-open)

- **Cross-link flow = Option C** (dedicated `kbl_cross_link_queue` table with UPSERT idempotency). NOT JSONB on signal_queue. Reason: clean Step 7 consumption, idempotency via composite PK, future dashboard queries trivial.
- **Status CHECK constraint expanded to 34 values** via PR #12. Naming reconciled to `<step>_running` pattern (brief §3.2 was inconsistent; PR #12 standardized).
- **Mac Mini = single AGENT writer.** Director writes welcome from any machine. (`project_mac_mini_role.md` in memory.)
- **Daily cost cap = €50** (not $15 per brief §9.2's stale naming). Folded into PR #14.
- **`KBL_MATTER_SCOPE_ALLOWED` derives from hot.md ACTIVE set** (not hardcoded env list). Applied in Step 4.
- **Fanout brief parked** — `briefs/_future_optimization/` folder established as the collection for post-Cortex-3T optimization ideas.
- **Step 5 stub paths: 3 distinct status values** (`stub_auto`, `stub_cross_link`, `stub_inbox`) for Director distinguishability. Per OQ5.
- **Currency enum: `{EUR, USD, CHF, GBP, RUB}`** (ship-with-5; extend when concrete signal surfaces need). Per OQ6.
- **Step 5 Opus prompt worked examples: 7 total** (was 3). Per B3 expansion + Director's vedana-schema discipline.

---

## Workflow patterns (inviolable)

### Mailbox

- Tasks: `briefs/_tasks/CODE_{1,2,3}_PENDING.md` + `briefs/_tasks/AI_DENNIS_*.md` — overwrite, commit, push.
- Reports: `briefs/_reports/B{N}_<topic>_<YYYYMMDD>.md`.
- Chat dispatch to Director = THIN POINTER in fenced code block: `cd ~/bm-b{N} && git pull, read briefs/_tasks/CODE_{N}_PENDING.md (commit <SHA>), execute <TASK>. ~X min.`
- NO task content in chat. File is sole source.

### Git flow

- Fresh `/tmp/bm-draft` clone for AI Head (stateless, re-clone on reboot).
- Pull → work → commit (`GIT_AUTHOR_NAME="AI Head" GIT_AUTHOR_EMAIL="ai-head@brisengroup.com"`) → push.
- On push conflict: `git pull --rebase origin main` + retry.
- Two repos: `github.com/vallen300-bit/baker-master` (code) + `github.com/vallen300-bit/baker-vault` (KBL Silver/Gold). Only Mac Mini writes to baker-vault (agent-writer); Director writes from any machine.

### Auto-merge protocol

```bash
gh pr merge <N> --repo vallen300-bit/baker-master --squash --subject "<brief-title> (#<N>)"
```

Gate: B2 APPROVE + CLEAN mergeable + no Director blockers. Ping Director with merge SHA post-merge in one line.

### Review cycle

- Author brief/PR → B2 reviews → REDIRECT with inline-appliable fixes OR APPROVE.
- Small-surface fixes: B1 applies as amend commit on same branch. No second PR.
- Structural fixes: full review cycle.

### Verification discipline

- Before merge: `gh pr view <N> --json state,mergeable` — if `UNKNOWN`, wait 10s + recheck (use `until` loops, NOT `sleep` chains).
- Before claiming "done": read actual commits + actual files, don't trust summary messages.
- Duplicate-relay handling: Director sometimes pastes the same B1/B2/B3 message twice. Acknowledge, verify git state, don't re-dispatch.

---

## Key files

| File | What | Editable? |
|---|---|---|
| `CHANDA.md` | Gold architectural intent — Director authored | **NO** (Inv 4) |
| `briefs/_drafts/KBL_B_PIPELINE_CODE_BRIEF.md` | KBL-B spec (§1-13 complete) | Small amends only |
| `briefs/_drafts/KBL_C_INTERFACE_CODE_BRIEF.md` | KBL-C skeleton, §1-3 ratified, **§4-10 authoring-pending** | **ACTIVE AUTHORING — YOUR NEXT BRIEF WORK** |
| `briefs/_drafts/KBL_B_STEP5_OPUS_PROMPT.md` | 7 worked examples post-B3 expansion (was 3) | B3 owns |
| `briefs/_drafts/KBL_B_STEP6_FINALIZE_SPEC.md` | B3-authored, all OQs resolved | ratified |
| `briefs/_drafts/KBL_B_STEP6_OQ_RESOLUTIONS_20260419.md` | AI Head OQ1-OQ8 ratifications | ratified |
| `briefs/_drafts/FIREFLIES_MATTER_INDEX_20260418.md` | 107-transcript index | frozen |
| `briefs/_tasks/CODE_{1,2,3}_PENDING.md` | Mailboxes; overwrite per dispatch | yes |
| `briefs/_tasks/AI_DENNIS_MAC_MINI_STEP7_PREP.md` | B3 completed 1-6; archived | read-only |
| `briefs/_reports/` | Inbox from agents | read-only |
| `briefs/_future_optimization/README.md` | Parked-ideas convention (NEW this session) | yes |
| `briefs/_future_optimization/BRIEF_MORNING_DIGEST_FANOUT_1.md` | Parked; unpark post-T3 | yes |
| `migrations/` | All KBL-A + KBL-B migrations landed | add-only |

### Memory files (`/Users/dimitry/.claude/projects/-Users-dimitry-Vallen-Dropbox-Dimitry-vallen-Baker-Project/memory/`)

Cumulative across sessions. MEMORY.md is the index.

---

## Open follow-ups

### Immediate (blocking shadow-mode go-live)

1. **KBL_PIPELINE_DASHBOARD_MVP dispatch** — **DRAFTED at `briefs/_tasks/CODE_1_PENDING.md` but NOT FIRED TO DIRECTOR YET.** Pending Director's refresh. Brief content: 4 widgets (signal state / cost rollup / Silver landed / Mac Mini heartbeat), 4 read-only API endpoints, new "KBL Pipeline" dashboard tab, ~60-90 min B1 work. Target PR #17. Director ratified option (iii) parallel: dashboard builds while AI Head verifies shadow-mode activation flags. **Fire this immediately after refresh** by writing the paste block for B1.

2. **Shadow-mode activation verification** — I was about to check these when Director requested handover:
   - Is KBL `pipeline_tick` cron-registered on Render? (Older kill-switched agent `com.brisen.kbl.pipeline` was on Mac Mini; new Sentinel pipeline on Render may or may not have Steps 1-6 wired to a scheduler.)
   - Where does `KBL_FLAGS_PIPELINE_ENABLED` live on Render? (B3 saw it on Mac Mini's `~/.kbl.env`. Render equivalent unclear.)
   - Does Step 7 (`kbl/steps/step7_commit.py`) respect `BAKER_VAULT_DISABLE_PUSH=true` per my original brief? B1 should have implemented it but verify via `grep BAKER_VAULT_DISABLE_PUSH kbl/steps/step7_commit.py` in the merged code.
   - Output: a concrete "paste these env-var flips, on Render and in `~/.kbl.env`" instruction block for Director.

3. **Director's question** pre-handover was about shadow-mode observability: "how will I know what's going on? Is it going to be visible in the Baker dashboard?" He ratified **option (iii)** — parallel dashboard build + shadow flip. Resume from there.

### Near-term (post-go-live)

4. **Polish PR** — consolidate:
   - PR #15 S2 (DDL `ALTER TABLE ADD COLUMN IF NOT EXISTS` in `_increment_retry_count` hot path; belongs in migration)
   - PR #16 2×S2 (rebase-abort before hard-reset; idempotent no-op commit edge case)
   - PR #14 5 nice-to-haves (UTC-symmetric SQL, dead CB probe, Inv 3 docstring overclaim, commit-before-raise silent-swallow, 3 ledger rows on R3 exhaust deliberate)
   - PR #15 4 nice-to-haves (pipeline_tick docstring stale, `_links.md` .gitignore hygiene, regex robustness, `git clean -fd` scope)
   - PR #16 6 nice-to-haves (B2 flagged inline)
   Total: ~60-90 min B1. Post-shadow-mode stable.

5. **KBL-C §4-10 authoring** — this is YOUR ongoing work. §1-3 ratified by Director in prior session. §4-10 covers: per-handler I/O contracts, WhatsApp command grammar spec, ayoniso threshold design, error matrix, testing plan, rollout, acceptance criteria. ~60-90 min substantive authoring. Route to B2 for review when done.

6. **AI Dennis first invocation** — Director hasn't opened a Cowork window yet. When he does, AI Dennis reads his 3-file memory at `/Users/dimitry/Vallen Dropbox/Dimitry vallen/AI project managers/AI_DENNIS_{OPERATING,LONGTERM,ARCHIVE}.md` and delivers a status briefing. Future IT tasks (M365, BCOMM, vendor mgmt) route to AI Dennis, not B3.

7. **Fireflies 15 pending transcripts** — Director paused labeling at T93; Jan 19 → Feb 3 window remains. Not urgent; resume when Director has bandwidth.

---

## Director communication style + traps

- Bottom-line first, supporting detail second.
- Speed-optimized, parallel-first.
- Terse. "next?", "tasks?", "ok", one-letter ratifications common.
- Challenges your recommendations and expects you to update. **Don't anchor on your initial framing when pushed** — in this session Director pushed back on Option B cross-link flow; I steel-manned against myself and flipped to C. That was the right move.
- Reads recommendations carefully; wants them as dedicated callout lines, not buried in prose. If you miss one, he'll remind you with "pls always add your recommendations" — treat as durable rule, don't repeat the miss.
- Will paste duplicate agent messages sometimes (relay confusion). Acknowledge, verify git, don't re-dispatch.
- Will paste fenced blocks into the wrong shell (literal shell expansion errors). Patient re-explanation on where blocks go (Claude CLI vs App vs fresh Terminal tab) is part of the job.

---

## If Director asks "what's next?"

Based on state at handover:

1. **Immediate** — fire the KBL_PIPELINE_DASHBOARD_MVP dispatch to B1 (drafted but not sent; pending your refresh).
2. **Parallel** — verify shadow-mode activation flags (Render env + Mac Mini `~/.kbl.env` + Step 7 `BAKER_VAULT_DISABLE_PUSH` respect).
3. **After dashboard merges** — flip shadow mode on, watch first signals flow.
4. **After stable burn-in (days)** — flip production push on, polish PR lands.
5. **Then** — KBL-C §4-10 authoring → B1 implements KBL-C handlers (ayoniso dispatcher, WhatsApp reply handler, vault-edit watcher, dashboard feeder).

---

## Trust levels

- **B1:** VERY high. Shipped 5 pipeline PRs this session (#13, #14, #15, #16 initial + #16 S1 fix) + cleanly flagged architectural ambiguities when task briefs had gaps (the Inv 9 cross-link flow ambiguity was B1's catch — he stopped before implementing and asked AI Head for design). Multiple REDIRECT → delta → APPROVE cycles handled smoothly. Memory-hygiene terminal-quitting discipline practiced.
- **B2:** VERY high. ~8 reviews this session, each with rigorous call-count + call-order + CHANDA invariant audits. Caught the pipeline-wide CHECK constraint issue (S1 on PR #10) that would have crashed production. Caught the Inv 4 race in PR #16 that would have silently let Step 7 overwrite Director's Gold. Exemplary citation discipline on §5 Q1/Q2. Memory-hygiene terminal-quitting discipline practiced.
- **B3:** High. Worked-example expansion + Step 6 spec authoring + Mac Mini infra. Pre-flight check on Mac Mini surfaced KBL-A legacy that would have caused coexistence issues — B3 STOPPED before writing and flagged 4 escalation questions with recommendations. That caution is exactly right.
- **AI Dennis:** Untested. Just deployed. First invocation pending.

---

## You are refreshed. Start by:

1. Read this handover end-to-end (~10 min).
2. `cd /tmp/bm-draft && rm -rf /tmp/bm-draft && git clone git@github.com:vallen300-bit/baker-master.git /tmp/bm-draft && cd /tmp/bm-draft` (fresh clone; `/tmp/` may have been wiped).
3. Read `CHANDA.md` at repo root.
4. `git log --oneline -15` — scan for commits since this handover's push (e.g., if B1 ran the drafted dashboard dispatch already, you'll see #17 activity).
5. `ls briefs/_reports/ | sort | tail -15` — any reports filed?
6. Read `briefs/_tasks/CODE_1_PENDING.md` — the drafted but unfired dashboard dispatch is here.
7. `gh pr list --repo vallen300-bit/baker-master --state open` — any in-flight PRs?
8. `curl -sS https://baker-master.onrender.com/health` — Render healthy?
9. `ollama list` (need `ollama serve` first) — confirm only `gemma4` remains (mistral/qwen/minilm deleted this session).
10. `ssh macmini 'launchctl list | grep brisen'` — confirm `com.brisen.baker.poller` + `com.brisen.baker.heartbeat` active; old KBL-A agents renamed to `.retired-*`.

**First status ping to Director (template):**

```
AI Head refreshed. State as of git <SHA>:
- Merges since handover: <list or "none">
- B1: <current task or idle>
- B2: <current task or idle>
- B3: <current task or idle>
- AI Dennis: deployed, Cowork invocation <pending/live>
- Open PRs: <list>
- Mac Mini: <heartbeat status, launchctl grep result>
- Render: <health status>
- Pipeline: <shadow-off / shadow-on / production>
- KBL-C brief: §1-3 ratified; §4-10 <pending authoring>
Standing by.
```

**Then:**
- If Director hasn't fired the dashboard B1 dispatch yet (most likely case): write the paste block for him and flag the shadow-mode activation verification as in-flight AI Head work.
- If Director is ready to flip shadow mode: answer his observability question (dashboard status) + write the env-var flip instructions.
- If KBL-C §4-10 is queued: continue authoring in parallel with whatever else is in flight.

---

## Session-specific lessons (don't repeat)

1. **Don't hardcode scripts in dispatches when env patterns already exist.** My original Mac Mini prep brief had B3 paste `DATABASE_URL` literally into plists. B3's pre-flight correctly flagged that Mac Mini already had `~/.kbl.env` with 600 perms holding the secret; plists should source that via wrapper, not duplicate. **Always recommend pre-flight before writing concrete execution scripts in infra briefs.**
2. **Chicken-and-egg on observability vs go-live.** Director asked "what do I see in shadow mode?" and the honest answer was "nothing in the dashboard." Should have caught this BEFORE offering shadow mode as an option. Lesson: before recommending a go-live path, verify the observability surface is ready OR flag the gap up front.
3. **Steel-man your own recommendations when Director asks.** Option B → Option C cross-link flow flip this session was the right call — I had optimized for "keep the 60-min ETA" which is scope-protection, not architecture. Director's "argue against your choice" prompt is a recurring check. Don't anchor.
4. **Terminal tabs ≠ free.** Director's Mac hit 95 GB from three concurrent long-running Claude Code CLI agents. Memory-hygiene rule ("quit tab after PR cycle") is now durable. Future Phase 2 work: honor it by briefing this on every dispatch.
5. **Bare shell vs Claude CLI vs Claude App confusion is a real UX cost.** Fenced blocks only work inside a running Claude session. Director pasted blocks into bare shells multiple times this session. Keep the "paste INTO Claude, not into the shell" note where relevant.

---

*Prepared 2026-04-19 (late afternoon) by outgoing AI Head. Phase 1 Cortex T3 code shipped; shadow-mode go-live pending dashboard MVP + env-var flip. All agents idle, all PRs merged, clean handoff state.*
