# Handover — Code Brisen #1 (terminal instance)

**Date:** 2026-04-17
**From:** B1 (current instance, being refreshed)
**To:** Fresh B1 instance
**Director:** Dimitry Vallen
**Orchestrator:** AI Head (Claude Opus 4.7, Mac app)

---

## Who you are

You are **Code Brisen #1** — the deepest-context Code agent on Baker/KBL. Terminal instance, CLI harness, 1M context. You've had two major roles so far: (1) R1/R2/R3 architecture reviewer on the decisions doc + KBL-A brief, (2) implementer of KBL-A PR #1.

Your identity config:
```
git config user.name "Code Brisen 1"
git config user.email "dvallen@brisengroup.com"
```

---

## CRITICAL: current state

**PR #1 is APPROVED by B2 (commit `3bbc337`). Awaiting Director merge.**
- Branch: `kbl-a-impl`
- HEAD: `f34fdef` (Phase 8 + 10 revision fix commits)
- Report you filed: `briefs/_reports/B1_kbl_a_pr1_revisions_20260417.md` at `3fa0a94`

Your current task file (`briefs/_tasks/CODE_1_PENDING.md` — commit `699c05f`) was the revision task. **Revisions are done.** No new task has been assigned yet.

**You are in standby.**

---

## What to expect next

After Director merges PR #1 and runs post-merge install:
- If install succeeds cleanly → KBL-B brief comes next. AI Head writes, you either review or implement (likely implement, given your KBL-A depth).
- If install hits bumps → Director/AI Head may ask you to patch via new fix commits on a new branch.

**If no new `CODE_1_PENDING.md` task appears:** just stand by. AI Head will write you one when ready.

---

## Project state (60-second read)

- **KBL** = Baker's compiled-wiki knowledge architecture replacing RAG. 3-tier (Render Tier-1, Mac Mini Tier-2, Director Tier-3).
- **KBL-A** (your PR #1) = Infrastructure foundation only. No pipeline logic. `pipeline_tick.py` is a STUB. 5 PG tables + schema migrations + install script + Mac Mini runtime glue.
- **KBL-B** (next) = 8-step pipeline (Layer 0 → Triage → Resolve → Extract → Classify → Opus Step 5 → Sonnet Step 6 → Commit). Replaces your stub.
- **KBL-C** = WhatsApp handlers + ayoniso + dashboard. Third brief.

---

## What you shipped in PR #1

11 Python modules in `kbl/` + 6 shell scripts in `scripts/` + 4 LaunchAgent plists + newsyslog config + env.mac-mini.yml.example + 329-line diff to `memory/store_back.py` for 7 `_ensure_*` methods.

**8 phase commits + 10 fix commits** (for B2's review feedback). Full list in your revision report.

All 25 R1/R2 lesson checkpoints verified by B2. 3 documented deviations all ACCEPTED.

---

## Critical invariants (don't regress these if you patch later)

1. **Gold drain transaction order:** claim → filesystem → commit+push → THEN mark PG done. Push failure rolls back filesystem via `git checkout`.
2. **`git add <specific paths>`, never `-A`** (per R1.S3, B2 verified)
3. **`kbl_log.level` CHECK forbids INFO.** INFO goes to local file only. Any `emit_log("INFO", ...)` is a bug.
4. **`kbl/db.py` bypasses `SentinelStoreBack`** — direct psycopg2 contextmanager. Don't reintroduce the class.
5. **LaunchAgent secrets via `~/.kbl.env`** (NOT plist, NOT `~/.zshrc`). All 4 wrapper scripts source it. Per B2.B1 fix.
6. **Heartbeat sole owner is `kbl/heartbeat.py`** — `pipeline_tick.py` doesn't write `mac_mini_heartbeat`. Per R1.S7.

---

## Workflow patterns

### Mailbox pattern
- Tasks to you: `briefs/_tasks/CODE_1_PENDING.md` — always git pull before reading
- Your reports: `briefs/_reports/B1_<topic>_<YYYYMMDD>.md` per `briefs/_reports/README.md`
- Chat one-liner format: `<action> at <path>, commit <SHA>. TL;DR: <verdict/result>.`

### Git flow
- Fresh `/tmp` clone on refresh if needed (your old working dir may be stale)
- NEVER force-push the `kbl-a-impl` branch
- Commit-per-fix with semantic prefix: `feat(kbl-a):`, `fix(kbl-a):`, `chore(kbl-a):`

### Review discipline
- If you're asked to review: follow R1-R5 pattern, file report with BLOCKERS/SHOULD-FIX/NICE/MISSING
- Time budget stated in task file — respect it
- If confused, ask AI Head before implementing

---

## Key files from your work

| File | What |
|---|---|
| `briefs/KBL-A_INFRASTRUCTURE_CODE_BRIEF.md` | Ratified brief (source of truth) |
| `briefs/_reports/B1_kbl_a_implementation_20260417.md` | Your original Phase 1-8 report |
| `briefs/_reports/B1_kbl_a_pr1_revisions_20260417.md` | Your revision report (10 fixes) |
| `briefs/_reports/B2_pr1_review_20260417.md` | B2's first PR review |
| `briefs/_reports/B2_pr1_reverify_20260417.md` | B2's approve verdict |

---

## Relationship with other agents

- **B2:** independent reviewer. Reviewed your PR #1. Found 1 blocker + 5 should-fix + 6 nice. All fixed; B2 approved.
- **B3:** labeling companion, working on D1 eval. Independent thread — don't block their work.
- **AI Head:** your task dispatcher + brief author. They write `CODE_1_PENDING.md` and review your reports.

---

## If Director asks you to help with post-merge install

Post-merge steps require macmini SSH + sudo. You can help:
1. SSH in, run `scripts/install_kbl_mac_mini.sh`
2. Help Director populate `~/.kbl.env` (read keys from Director's macmini `~/.zshrc` via one-liner)
3. Commit `config/env.mac-mini.yml` to baker-vault repo (separate clone)
4. Watch first tick via `tail -f /var/log/kbl/pipeline.log`
5. Verify heartbeat via `psql $DATABASE_URL -c "SELECT * FROM kbl_runtime_state"`

---

## Gotchas + lessons relevant to you

- **Lesson #34** — structural verification ≠ integration verification. Your implementation caught this correctly (deferred deploy-only tests). Keep this pattern.
- **Non-interactive SSH PATH** — `/opt/homebrew/bin` missing. Use `PATH=/opt/homebrew/bin:$PATH` prefix OR HTTP API for Ollama (your code uses HTTP API, correct).
- **`01_build` clone is abandoned** — 790+ commits behind. Don't touch it. Fresh clone or use existing `/tmp/b1-workspace` (if present).
- **Stash + rebase + pop** when main moves during your work. I've done this pattern ~10 times successfully in this session.

---

## You are refreshed. Start by:

1. Read this handover
2. `git pull` in your working dir
3. Check `briefs/_tasks/CODE_1_PENDING.md` for new task (may still be the stale revision task if no new work dispatched yet)
4. If new task: execute
5. If no new task: stand by, announce readiness to Director via chat: "B1 refreshed, standing by"

---

*Prepared 2026-04-17 by outgoing B1. PR #1 is your crowning work — approved and ready to merge. Next likely work: KBL-B implementation after AI Head ships the brief.*
