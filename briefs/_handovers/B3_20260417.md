# Handover — Code Brisen #3 (app instance)

**Date:** 2026-04-17
**From:** B3 (current instance, being refreshed)
**To:** Fresh B3 instance
**Director:** Dimitry Vallen
**Orchestrator:** AI Head (Claude Opus 4.7, Mac app)

---

## Who you are

You are **Code Brisen #3** — fresh session spawned today for D1 pre-shadow eval labeling companion work. App-instance Claude Code. Unlike B1 (deep KBL-A context) and B2 (schema + review), you came in clean for one specific job: shepherd Director through labeling 50 signals, then run Gemma/Qwen eval.

Your identity config:
```
git config user.name "Code Brisen 3"
git config user.email "dvallen@brisengroup.com"
```

---

## CRITICAL: what just happened + next action

**D1 eval executed — both models FAILED thresholds:**
- Gemma: 70% vedana / 100% JSON / 30% matter (need ≥90%/100%/≥80%)
- Qwen: 66% / 100% / 36% (same thresholds)

**Root cause is prompt engineering, NOT model capability:**
1. The prompt only lists 6 of 19 ground-truth matter slugs
2. The vedana semantic rule isn't in the prompt ("opportunity = NEW strategic gains only; defensive wins stay in threat arc")
3. 7 near-duplicate Baker analyses + 1 garbled transcript drag averages

**Director approved Option C: retry eval with improved prompt.** AI Head is dispatching your retry task. When you refresh:

1. Check `briefs/_tasks/CODE_3_PENDING.md` — AI Head will have overwritten with retry task
2. If not yet written, you'll see stale labeling-companion task content. Wait for AI Head to update.

### Retry task scope (what you'll be asked to do)

1. **Patch the prompt** in `scripts/run_kbl_eval.py` (or wherever the prompt template lives):
   - Add all **19 matter slugs** to the enum (list below)
   - Add Director's **vedana rule**: "opportunity = NEW strategic gains ONLY; defensive wins stay in threat arc. routine = noise (receipts, newsletters, FYI)."
2. **Re-run** `.venv/bin/python3 scripts/run_kbl_eval.py outputs/kbl_eval_set_20260417_labeled.jsonl --compare-qwen`
   - **DO NOT re-label.** Director's labels are ground truth. Prompt is what changes.
3. **Compare results** vs thresholds
4. **File report** at `briefs/_reports/B3_d1_eval_retry_20260417.md`

---

## The 19 matter slugs (complete list)

You'll need these for the prompt patch. 8 NEW slugs were added during Director's labeling session (not in AI Head's original enum of 7):

**Original 7 (from `DECISIONS_PRE_KBL_A_V2.md` task-file enum):**
- `hagenauer-rg7`
- `cupial`
- `mo-vie`
- `ao`
- `morv`
- `lilienmatt`
- `wertheimer`

**NEW 8 (discovered during labeling):**
- `aukera`
- `kitzbuhel-six-senses`
- `kitz-kempinski`
- `steininger`
- `balducci`
- `constantinos`
- `franck-muller`
- `brisen-lp` (wertheimer was separated from brisen-lp during labeling — confirm count with labels file)

**Plus null for "no matter."**

Verify the full list by grepping your `outputs/kbl_eval_set_20260417_labeled.jsonl` for `primary_matter_expected` distinct values. Trust the labels file over this handover — it's ground truth.

---

## The vedana rule (add verbatim to prompt)

```
vedana classification rules:
- opportunity: NEW strategic gains ONLY — a new deal, investor interest,
  unrequested approach, favorable market shift, novel capability revealed.
  Defensive wins inside an ongoing threat arc (e.g., court ruling in our
  favor on a dispute) stay in threat, not opportunity.
- threat: risks, problems, disputes, deadlines, unpaid invoices, regulatory
  issues, counterparty demands, anything requiring defensive action.
- routine: noise — receipts, automated notifications, newsletters, FYI
  emails, admin correspondence with no action required.
```

---

## Files you've created

| File | What |
|---|---|
| `outputs/kbl_eval_set_20260417.jsonl` | 50 unlabeled signals (seed) |
| `outputs/kbl_eval_set_20260417_labeling_template.jsonl` | 50 with empty label fields |
| `outputs/kbl_eval_set_20260417_labeled.jsonl` | **50 Director-labeled signals — ground truth** |
| `outputs/kbl_eval_results_20260417.json` | First eval results (Gemma 70% / Qwen 66%) |
| `briefs/_reports/B3_d1_eval_results_20260417.md` | Your first eval report |
| `scripts/apply_label.py`, `scripts/present_signal.py` | Helper scripts for companion UX |

---

## Environment setup

You're in `/tmp/bm-b3` (disposable fresh clone). Critical env:

- **Python:** `.venv/bin/python3` (3.12, venv at `/tmp/bm-b3/.venv`)
- **`config/.env`** copied from `~/Desktop/baker-code/config/.env` (gitignored, dies with clone)
- **`DATABASE_URL`** loaded via python-dotenv from `config/.env` (unset shadow from prior SSH fetch)
- **macmini SSH** works via Tailscale (`ssh macmini`)
- **Ollama** on macmini: `gemma4:latest` (9.6 GB) + `qwen2.5:14b` (9.0 GB) — use HTTP API, NOT CLI (PATH issue)

---

## Bug history (your catches)

- **`build_eval_seed.py` v1:** `email_messages.id` doesn't exist — PK is `message_id`. You patched at commits `1f3d467` + `76f3412`.
- **`.env` loading hack:** copied from active clone. Productionization pending (deferred).
- **Non-interactive SSH PATH:** `/opt/homebrew/bin` missing. Your eval uses HTTP API so unaffected, but documented for future work.

These generated **Lesson #34 in `tasks/lessons.md`** — "structural verification ≠ integration verification."

---

## Project state (60-second read)

- **KBL** = Baker's compiled-wiki knowledge architecture. 3-tier.
- **KBL-A** (infrastructure) PR #1 is APPROVED, awaiting Director merge. Independent of your thread.
- **D1 eval** (your thread) — failed with naive prompt, retry pending.
- **KBL-B** (pipeline) — not yet briefed. Your D1 result feeds into it.

---

## Workflow patterns

### Mailbox pattern
- Task: `briefs/_tasks/CODE_3_PENDING.md`
- Report: `briefs/_reports/B3_<topic>_<YYYYMMDD>.md` per `briefs/_reports/README.md`
- Chat one-liner: `<action> at <path>, commit <SHA>. TL;DR: <result>.`

### Git flow
- Fresh `/tmp/bm-b3` clone is your working dir (DO NOT touch `01_build` in Dropbox — abandoned, 790+ commits behind)
- `git pull --ff-only origin main` before any work
- Git identity: `Code Brisen 3` / `dvallen@brisengroup.com`

### Python
- Always `.venv/bin/python3`, never bare `python3` (system 3.9 is incompatible)
- Dotenv loads `config/.env` automatically via `memory/store_back.py` imports

---

## Spawned AI Head task (slug registry)

During labeling you surfaced "slugs are a living body" architectural concern. You queued a spawned task for AI Head to design a slug registry for KBL-B. That's not your job — AI Head owns that decision. You just surfaced it.

---

## Relationship with other agents

- **B1:** KBL-A implementer, currently standby. Independent of your thread.
- **B2:** PR reviewer + schema author. Wrote the eval scripts you ran. Caught `build_eval_seed.py` structural-verification gap → Lesson #34.
- **AI Head:** your dispatcher. Writes your tasks, reads your reports.
- **Director:** does the labeling (the ~55-min task you companioned).

---

## Trust level

AI Head trusts you for:
- D1 eval retry (your current work)
- Future KBL-C labeling workflows (if needed)
- Data-shape archaeology (you're good at spotting real-world data gotchas)

AI Head will NOT expect you to:
- Review KBL-A/B implementation code (B2 owns that)
- Write architecture briefs (AI Head owns that)

---

## You are refreshed. Start by:

1. Read this handover
2. `git pull` in `/tmp/bm-b3`
3. Check `briefs/_tasks/CODE_3_PENDING.md` for new retry task
4. If retry task present: execute
5. If not yet: announce readiness to Director: "B3 refreshed, standing by for D1 retry task"

---

*Prepared 2026-04-17 by outgoing B3. Labeling session complete (50/50), eval run complete, root cause identified (prompt, not model). Retry is next — likely 30 min work including report. Good session.*
