# B2 PR #16 S1 delta — APPROVE

**Reviewer:** Code Brisen #2
**Date:** 2026-04-19 (late afternoon)
**PR:** https://github.com/vallen300-bit/baker-master/pull/16
**Branch head:** `2e845d5` (delta from `79ad641`)
**Task:** `briefs/_tasks/CODE_2_PENDING.md` @ `63b9a62` — Task P
**Initial review:** `briefs/_reports/B2_pr16_review_20260419.md` @ `1e4552b`
**Verdict:** **APPROVE**

## What changed

B1 landed the S1 fix exactly as recommended. Two files, 91 insertions,
1 deletion — pure fix + new race test, zero scope creep.

### `kbl/steps/step7_commit.py` — 1-line reorder

Line 579 (pre-flock call) **deleted**; the guard now sits inside the
`with acquire_vault_lock(...)` block, directly after
`_git_pull_rebase(cfg)`, with a 4-line comment explaining the race.
Final order is exactly what the initial review prescribed:

```
acquire flock
  → git pull --rebase
  → _inv4_guard_target_path(main_abs)   # ← moved here
  → _atomic_write (main + stubs)
  → git add / commit / push
release flock
```

`_assert_path_inside_vault` stayed outside the lock — correct, since
path containment is lexical and doesn't depend on FS state.

### `tests/test_step7_commit.py` — new `test_commit_inv4_collision_after_rebase_refuses`

85-line test, matches the shape I suggested in the initial report:

- 2-clone fixture: Clone A = Mac Mini (`vault_dir`), Clone B = Director's
  dev Mac (`tmp_path / "clone_b"`), both pointing at the same bare
  remote.
- Clone B writes a Gold file with `author: director`, commits,
  pushes — Clone A still at seed locally.
- `assert not (vault_dir / target_rel).exists()` explicitly proves the
  file is **not** on Clone A before `commit()` runs. This kills the
  "guard saw it pre-pull" hypothesis.
- `commit()` called. `pytest.raises(CommitError, match="Director-authored")`.
- Post-assertions:
  - File content still shows `author: director` + `"Director body content."`
    (not overwritten).
  - `voice: silver` absent (pipeline's Silver never landed).
  - `commit_failed` state flip recorded on the MagicMock.
  - `git log` on Clone A shows `["director: race file", "seed"]` — no
    Step 7 commit on top.

This test would have failed on `79ad641` (Inv 4 guard ran pre-pull →
file absent locally → guard passed → write overwrote Director → log
would have shown Step 7's commit on top). On `2e845d5` it passes. Tight
forcing function for the reorder — a future refactor that accidentally
hoists the guard back out of the lock will break this test.

## Verification checklist

| Item | Status |
|------|--------|
| New order inside lock (`_git_pull_rebase` → `_inv4_guard_target_path` → writes) | ✓ |
| No `_inv4_guard_target_path` call remaining outside the lock | ✓ (grep: 2 call sites in source — one in `commit()` inside lock, one in test) |
| New race test `test_commit_inv4_collision_after_rebase_refuses` passes | ✓ |
| Existing `test_commit_inv4_collision_refuses` (local-seeded) still passes | ✓ (guard now runs after pull but file is still present locally from the test's own pre-seed, so it fires there too) |
| No scope creep — only the reorder + new test | ✓ (git show: 2 files, +91/-1, no S2 or N work snuck in) |
| All 5 Inv 4 tests green | ✓ (`pytest -k inv4` → 5 passed) |
| Full step7 + step6 + pipeline_tick suite green | ✓ (78/78 passed in 3.77s) |
| Commit message explains the race + fix + deferrals | ✓ |

## Test results

```
$ pytest tests/test_step7_commit.py tests/test_pipeline_tick.py tests/test_step6_finalize.py -q
78 passed in 3.77s

$ pytest tests/test_step7_commit.py -q -k inv4
5 passed, 22 deselected in 0.69s
```

## Dispatch

**APPROVE. Ready to auto-merge.** S2.1 (rebase-abort before hard-reset),
S2.2 (idempotent no-op commit), and N1-N6 are correctly deferred to a
post-Phase-1 polish PR per the Task O deferral plan.

Post-merge: **7 of 7 pipeline steps on main. Cortex T3 Phase 1 SHIPPED.**
