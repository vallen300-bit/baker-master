# PR #13 S1 Delta — APPROVE (B2 — Task J)

**From:** Code Brisen #2
**To:** AI Head
**Re:** [`briefs/_tasks/CODE_2_PENDING.md`](../_tasks/CODE_2_PENDING.md) Task J — PR #13 S1 delta
**PR:** https://github.com/vallen300-bit/baker-master/pull/13
**Head reviewed:** `315695f` (one commit advance from `4d38a44`; prior REDIRECT report: `dedab68` → `briefs/_reports/B2_pr13_review_20260419.md`)
**Diff:** `tests/test_step4_classify.py` only, +36 lines, zero production-code change
**Date:** 2026-04-19
**Time:** ~5 min

---

## Verdict

**APPROVE.** The S1 gap identified in my initial review is closed. The new test `test_classify_cross_link_only_guard_raises_and_marks_failed` (inserted immediately after `test_classify_below_threshold_raises_and_marks_failed` — logical twin placement) genuinely exercises the runtime guard path, not a trivially-passing assertion that bypasses it.

---

## Verification against brief §Scope

| Check | Verdict | Evidence |
|---|---|---|
| Exercises the runtime guard (not trivially-passing) | ✓ | `@patch("kbl.steps.step4_classify._evaluate_rules", return_value=(ClassifyDecision.CROSS_LINK_ONLY, False))` forces the guard to fire — the only code path from `_evaluate_rules` returning `CROSS_LINK_ONLY` is the `if decision is ClassifyDecision.CROSS_LINK_ONLY:` block in `classify()`. No other way to reach this branch in production. |
| Exception type assertion present | ✓ | `with pytest.raises(ClassifyError, match="CROSS_LINK_ONLY"):` — both type AND message substring asserted |
| State-flip assertion (`classify_failed` BEFORE the raise) | ✓ | `failed_rows = [c for c in conn._calls if ... c[1] == ("classify_failed", 77)]; assert failed_rows` — specifically targets the params tuple `("classify_failed", 77)`, not just any status update |
| "No decision write on guard" negative assertion | ✓ | `assert not decision_rows` — no `step_5_decision` UPDATE fires |
| Naming + location consistency | ✓ | Naming mirrors `test_classify_below_threshold_raises_and_marks_failed`; placement immediately following that test function (line 442 vs prior 417) |
| N1/N2/N3 nice-to-haves deferred (no scope creep) | ✓ | `git diff 4d38a44..315695f` touches only `tests/test_step4_classify.py`. Zero changes to: `_get_scope_env_override` docstring (N1), `_parse_hot_md_active` tests (N2 Watch-list), `classify()` docstring re: Step 6 cross_link_hint consumer (N3). Clean deferral. |

---

## Subtle point worth noting (not a finding)

The new test includes `with _patch_scope("movie")` for defensive hygiene — technically unnecessary since `_evaluate_rules` itself is mocked, but harmless and makes the test robust against future refactors that might probe `allowed` before calling `_evaluate_rules`. Good belt-and-suspenders.

Also subtle: `_load_allowed_scope()` is still called even though its result is unused (because `_evaluate_rules` is mocked before it would consume the result). This is actually a *feature* — it means Inv 3's "hot.md read per invocation" contract remains honored even on the unreachable-guard path, matching the precedent from the below-threshold guard test.

---

## Merge readiness

- 41 → 41 tests green (was 40, +1 new). Brief says "41/41 tests green (was 40)" — my count confirms.
- No production code change → no integration risk with other Phase 1 PRs.
- No conflicts with current `main` (only mailbox-file diffs since `d31349d`).

Ready to auto-merge.

---

*Reviewed 2026-04-19 by Code Brisen #2. Files @ `315695f`. Scope: S1 test delta only; earlier REDIRECT audit coverage (`dedab68`) otherwise unchanged. ~5 min.*
