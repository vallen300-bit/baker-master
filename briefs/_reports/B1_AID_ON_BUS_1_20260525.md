---
report_id: B1_AID_ON_BUS_1_20260525
brief_id: AID_ON_BUS_1
brief_path: ~/bm-aihead1/briefs/BRIEF_AID_ON_BUS_1.md
brief_anchor: 96e4e5d
target_repos:
  - brisen-lab (vallen300-bit/brisen-lab)
  - baker-vault (vallen300-bit/baker-vault)
b1_branch: b1/aid-on-bus-1
brisen_lab_pr: https://github.com/vallen300-bit/brisen-lab/pull/41
baker_vault_pr: https://github.com/vallen300-bit/baker-vault/pull/115
brisen_lab_head_sha: 01d266f
baker_vault_head_sha: a723866
status: SHIPPED (awaiting deputy gates + lead merge)
reply_target: lead
dispatched_by: lead (bus #1131)
shipped_at: 2026-05-25T20:30Z
---

# B1 — AID_ON_BUS_1 ship report

## Summary

Wired `aid` slug into brisen-lab (rows 10, 11a-d, 13) + baker-vault skill text. Two PRs open. 29/29 tests pass (incl. 2 new AID regression tests).

## PRs

| Repo | PR | HEAD SHA | Branch |
|---|---|---|---|
| brisen-lab | #41 | `01d266f` | `b1/aid-on-bus-1` |
| baker-vault | #115 | `a723866` | `b1/aid-on-bus-1` |

## Files touched (brisen-lab)

| File | Change | Row |
|---|---|---|
| `static/index.html` | +1 card article in `.row-shared`; cache-bust `app.js?v=18 → v=19` | 10 |
| `static/app.js` | TERMINALS + TERMINAL_LABELS + CARD_TYPE entries | 10 |
| `bus.py` | KNOWN_CARD_SLUGS (~941) + `_build_terminals_response` for-loop (~1051) | 11a + 11b |
| `app.py` | TERMINALS list (~40) | 11c |
| `tests/test_a3_a8_a9_bus.py` | 2 new regression tests | 11d |
| `tools/wake-handler/wake-handler.applescript` | fnMap pair `{"aid", "aid"}` | 13 |

## Files touched (baker-vault)

| File | Change |
|---|---|
| `_ops/skills/agent-bus-posting-contract/SKILL.md` | recipient line + §AID subsection |

## Quality Checkpoints (literal output)

### QC1 — py_compile

```
$ python3 -c "import py_compile; [py_compile.compile(f, doraise=True) for f in ['app.py','bus.py','tests/test_a3_a8_a9_bus.py']]" && echo "py_compile OK"
py_compile OK
```

### QC2-5 — Grep verification counts

```
$ grep -cE '"aid"|aid:' static/app.js     # 3 (TERMINALS array + LABELS key + CARD_TYPE key — same shape as researcher)
3
$ grep -c '"aid"' bus.py                  # ≥2 (KNOWN_CARD_SLUGS + for-loop)
2
$ grep -c '"aid"' app.py                  # ≥1 (TERMINALS list)
1
$ grep -c '"aid"' tools/wake-handler/wake-handler.applescript  # ≥1 (fnMap pair)
1
$ grep -c 'data-alias="aid"' static/index.html  # 1 (card article)
1
```

**NB:** brief specified `grep -c '"aid"' static/app.js >= 2` but existing dict-literal style uses unquoted identifier keys (`researcher: "Researcher"`, not `"researcher": "Researcher"`). Researcher itself yields 1 quoted-string match. I followed the existing convention. Aggregate `'"aid"|aid:'` returns 3, matching researcher.

### QC6 — Cache-bust

`static/index.html` line 127: `app.js?v=18 → app.js?v=19`. `styles.css` unchanged (no CSS edits).

### QC7 — No collateral edits

```
$ git diff --stat origin/main..HEAD  # brisen-lab
 app.py                                      |  2 +-
 bus.py                                      |  4 +--
 static/app.js                               |  4 ++-
 static/index.html                           |  3 +-
 tests/test_a3_a8_a9_bus.py                  | 50 +++++++++++++++++++++++++++++
 tools/wake-handler/wake-handler.applescript |  1 +
 6 files changed, 59 insertions(+), 5 deletions(-)
```

Exact match to brief's "Files touched by b1" list — no collateral edits.

## Pytest output (literal)

```
$ pytest tests/test_a3_a8_a9_bus.py -v
...
tests/test_a3_a8_a9_bus.py::test_bus_badge_change_emitted_for_researcher PASSED [ 44%]
tests/test_a3_a8_a9_bus.py::test_v2_terminals_response_includes_researcher PASSED [ 48%]
tests/test_a3_a8_a9_bus.py::test_bus_badge_change_emitted_for_aid PASSED [ 51%]
tests/test_a3_a8_a9_bus.py::test_v2_terminals_response_includes_aid PASSED [ 55%]
...
================== 29 passed, 3 warnings in 212.13s (0:03:32) ==================
```

All 29 tests pass. 2 new AID tests included.

## Brief discrepancies (fail-loud)

1. **Row 10 card slot:** brief specified `.row-desks` + `card-desk` class. Actual code uses `.row-shared` + plain `card` class (where researcher lives). Followed existing researcher pattern.
2. **`grep -c '"aid"' static/app.js >= 2`:** brief's check assumed quoted-string keys in JS dict literals. The existing convention uses unquoted identifier keys when the slug is a valid JS identifier (researcher, aid). Aggregate-pattern match (`'"aid"|aid:'`) returns 3, matching researcher exactly.

Neither affects correctness; flagged for SOP refresh on the next install brief.

## NOT done by b1 (Out-of-band Tier-A/B AH1 actions, per brief)

- AC1-4 — picker dir + zsh function + Terminal.app profile + `~/bm-aid/CLAUDE.md` (Tier-A)
- AC5 — Render env JSON merge `BRISEN_LAB_TERMINAL_KEYS["aid"]` (Tier-B)
- AC11 — `forge_snapshot_push.sh` TERMINALS edit (Tier-A)
- AC12 post-merge — wake-handler rebuild via `build.sh` (Tier-A)
- AC14 — browser verification on brisen-lab cockpit (Tier-A)
- AC15 — end-to-end smoke + bus posts (Tier-A)

## Bus thread

- Dispatched: bus #1131 (lead → b1, topic `dispatch/aid-on-bus-1`)
- Ship: this report — bus reply to lead with topic `ship/aid-on-bus-1`
- Gates: deputy Gate-2 + Gate-4 on PR open per `ai-head-autonomy-charter §3`
- Merge: lead (AH1) after gates clear

## Mailbox state (not flipped by b1)

`briefs/_tasks/CODE_1_PENDING.md` still holds the merged `WAKE_VIA_SSE_PIVOT_1_V02` brief (status PENDING). Lead's dispatch #1131 referenced the mailbox path but didn't refresh the file. Leaving the flip to lead/AH1 post-merge of this AID PR — out of b1 scope to write a new PENDING brief for the same role.
