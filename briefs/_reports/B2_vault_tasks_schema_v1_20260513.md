---
report_type: ship
from: b2
to: ai-head-1
brief: briefs/BRIEF_VAULT_TASKS_SCHEMA_V1.md
dispatch_mailbox: briefs/_tasks/CODE_2_PENDING.md
trigger_class: TIER_B_VAULT_SCHEMA_INFRA
completed_at: 2026-05-13
repo: baker-vault (vault-only — direct push, no PR)
commit: 7cf5718
push_target: origin/main (baker-vault)
hard_ship_gate: PASS — `python3 scripts/validate_vault_tasks.py` exit 0 against MOHG-debrief seed
files_changed: 5 (394 insertions)
---

# Ship report — VAULT_TASKS_SCHEMA_V1

## Outcome
Three-tier scheduled-tasks frame's vault layer landed.

## Files created (baker-vault, 1 atomic commit)
- `_ops/processes/vault-tasks-schema-v1.md` — canonical schema + closure protocol
- `_ops/processes/schedule-registry.yml` — empty (`jobs: []`) placeholder
- `_ops/agents/movie-desk/tasks/active/2026-05-13-mohg-debrief.md` — MOHG migration seed
- `_ops/agents/movie-desk/tasks/README.md` — desk pointer
- `scripts/validate_vault_tasks.py` — lint-mode validator (~230 LOC)

## Hard ship gate — literal validator output

```
$ cd ~/baker-vault && python3 scripts/validate_vault_tasks.py
OK    _ops/agents/movie-desk/tasks/active/2026-05-13-mohg-debrief.md

validate_vault_tasks: 1 file(s) checked, 0 violation(s)
exit=0
```

## Negative tests (per brief Test plan)

Both fired correctly + clean state restored.

```
--- Negative test 1: bad matter_slug ---
FAIL  _ops/agents/movie-desk/tasks/active/_scratch_neg_test.md
      - unknown matter_slug 'not-a-real-slug' — not in slugs.yml (canonical or alias)
exit=1

--- Negative test 2: bad status ---
FAIL  _ops/agents/movie-desk/tasks/active/_scratch_neg_test.md
      - invalid status 'invalid' — must be one of ['blocked', 'done', 'dropped', 'in_flight', 'queued']
exit=1

--- Re-verify clean state ---
OK    _ops/agents/movie-desk/tasks/active/2026-05-13-mohg-debrief.md
exit=0
```

## Notes / design calls
- `matter_slug` validator accepts canonical slug OR alias (case-insensitive, whitespace-collapsed). Mirrors the `kbl/slug_registry.py` loader semantics described in slugs.yml header. The MOHG seed uses `mo-vie` which is an alias of canonical `mo-vie-am` — both resolve.
- `draft`-status slugs are NOT accepted by the validator (per slugs.yml header: "draft — NOT offered, NOT accepted").
- `owner_agent` valid set: union of (a) 13 canonical bus slugs + (b) directory names under `_ops/agents/`. Permissive lint, not block-mode.
- `closed/YYYY-MM/` directory NOT created (per brief — created on first close).
- `upcoming-deadlines.md` NOT created (per brief — generated by Brief 2 scanner).
- Single atomic commit `7cf5718` covers only the 5 brief-scoped files. Other agents' dirty/untracked changes in `~/baker-vault` left untouched (Lesson #7 isolation).

## Coordination
- Independent of Brief 2 (b3 / APSCHEDULER_VAULT_SCANNER_V1) + Brief 3 (b4 / HARD_DEADLINE_AUDIT_V1).
- Brief 2's scanner can now run successfully against `_ops/agents/movie-desk/tasks/active/`.

## Bus posts
- ACK on dispatch msg #188 — HTTP 200.
- Ship post to `lead` topic `ship/VAULT_TASKS_SCHEMA_V1` — pending (final step).
