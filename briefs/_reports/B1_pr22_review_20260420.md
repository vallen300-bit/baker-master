# B1 Review — PR #22 STORE_BACK_DEAD_CODE_AND_DB_ENV_FALLBACK

**PR:** https://github.com/vallen300-bit/baker-master/pull/22
**Head SHA:** `84f15db0f1fb22ed5a71ea9a386c8b5e283009a2`
**Branch:** `store-back-dead-code-and-db-env-fallback` → `main`
**Author:** B2
**Reviewer:** B1 (reviewer-separation rotation: B2's code → B1)
**Date:** 2026-04-20

---

## Verdict: **APPROVE**

Scope matches brief, all acceptance criteria pass, no regressions vs. main baseline. Ready for Tier A auto-merge.

---

## Part A — dead code removal (`memory/store_back.py`)

| Acceptance check | Result | Evidence |
|---|---|---|
| `grep -rn "_ensure_kbl_cost_ledger\|_ensure_kbl_log" .` returns zero `.py` matches | ✅ PASS | empty grep output at `84f15db` |
| No other `_ensure_*` method modified | ✅ PASS | 20 sibling `_ensure_*` defs still present at `memory/store_back.py:238..1738` |
| Forward-reference comment at ex-line 6379 cleaned | ✅ PASS | `_ensure_signal_queue_additions` docstring trimmed (diff L6374-L6378); removed "MUST run before _ensure_kbl_cost_ledger / _ensure_kbl_log" clause |
| Callers at ex-lines 193-194 removed | ✅ PASS | both `self._ensure_kbl_cost_ledger()` and `self._ensure_kbl_log()` deleted from `init_schema` (diff L184-L194); replacement comment correctly points to `migrations/20260419_add_kbl_cost_ledger_and_kbl_log.sql` per lesson #37 |

Delta: `-98 / +4` lines — clean, orthogonal, no collateral edits.

## Part B — `kbl/db.py` env fallback

| Acceptance check | Result | Evidence |
|---|---|---|
| `_build_dsn()` prefers `DATABASE_URL` when set | ✅ PASS | `kbl/db.py:20-22` early return `if url: return url` |
| Missing required `POSTGRES_*` raises `RuntimeError` with clear missing-key list | ✅ PASS | `kbl/db.py:24-29` builds `missing = [k for k in required if not os.environ.get(k)]` → `RuntimeError(f"neither DATABASE_URL nor POSTGRES_* fallback available; missing: {missing}")`. Message explicitly names `DATABASE_URL` and the missing split vars. Not a bare `KeyError`. |
| `quote_plus` on **both** user and password | ✅ PASS | `kbl/db.py:31-32` — `user = quote_plus(...)`, `pw = quote_plus(...)`. Neon usernames can contain `@` (tenant-qualified), so quoting both is correct defensively, not over-engineered. |
| `POSTGRES_PORT` defaults to `5432` | ✅ PASS | `kbl/db.py:34` `os.environ.get("POSTGRES_PORT", "5432")` |
| `get_conn()` now routes through `_build_dsn()` | ✅ PASS | `kbl/db.py:52` replaces `os.environ["DATABASE_URL"]` with `_build_dsn()` |

**B2 note on the 4th default-port test:** NOT over-fitting. The default `"5432"` is an implicit promise that future refactors could silently drop. Keep it — cost is 6 lines, catches one class of regression outright.

## Test verification

All 4 new tests pass cleanly on py3.12 in `~/bm-b2-venv`:

```
tests/test_kbl_db.py::test_database_url_wins_when_both_are_set PASSED
tests/test_kbl_db.py::test_split_form_builds_expected_url PASSED
tests/test_kbl_db.py::test_missing_split_var_raises_clear_error PASSED
tests/test_kbl_db.py::test_default_port_when_not_set PASSED
4 passed in 0.02s
```

`test_split_form_builds_expected_url` uses password `"p@ss:w#rd"` — exercises the three URL-reserved chars that would break `psycopg2.connect` if passed raw. Strong test.

`test_missing_split_var_raises_clear_error` asserts `DATABASE_URL`, `POSTGRES_PASSWORD`, `POSTGRES_DB` all appear in the error message — catches the "drop-and-forget" refactor.

## Cross-cutting

| Check | Result | Evidence |
|---|---|---|
| No schema changes | ✅ PASS | no `migrations/*.sql` files in PR (`gh pr view 22 --json files` shows `kbl/db.py`, `memory/store_back.py`, `tests/test_kbl_db.py` only) |
| No Render env changes required | ✅ PASS | `DATABASE_URL` remains canonical on Render; fallback path only fires where `DATABASE_URL` is absent (Mac Mini scheduler with `POSTGRES_*` from `~/.kbl.env`) |
| Syntax clean | ✅ PASS | `py_compile` green on `memory/store_back.py` + `kbl/db.py` |
| Regression baseline | ✅ PASS | main@`34a9648`: 16 failed, 592 passed. pr-22@`84f15db`: 16 failed, 596 passed (+4 new tests). Same 16 pre-existing failures (scan endpoint 401/503, clickup integration voyage errors, prompt-format test) — all unrelated to Part A/B scope. Spot-checked `test_scan_returns_sse_stream` (503 startup init, not store_back) and `test_add_tag_wrong_space_raises` (clickup tag logic) — confirmed unrelated. |
| py3.9 PEP-604 landmine in `tools/ingest/extractors.py` | ✅ CONFIRMED pre-existing | Reproduces on `main` without PR #22 applied; unrelated to this PR's scope. |

## Non-blocking observations

1. **No action needed.** The `RuntimeError` message format `f"... missing: {missing}"` renders as `missing: ['POSTGRES_PASSWORD', 'POSTGRES_DB']` with Python list-repr. Readable enough for a startup-abort log line; future-polish could `", ".join(missing)` for prose-style output, but it's not a review blocker.

2. **Lesson #36 citation is correct.** The `_build_dsn` docstring cites "lesson #36 (PR #19 hotfix)". Confirmed consistent with `tasks/lessons.md` at `34a9648`.

---

## Final call

**APPROVE.** AI Head may auto-merge per Tier A protocol. Post-merge verification: `scripts/run_kbl_eval.py` on Mac Mini should continue to resolve DSN via `POSTGRES_*`; Render web service continues via `DATABASE_URL`. Both paths now share the same code path and URL-encoding contract.

— B1, 2026-04-20
