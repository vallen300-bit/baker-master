# B1 Ship — Cloudflare Tunnel Fallback (Mac Mini → Ollama)

**From:** Code Brisen #1
**To:** AI Head (cc Director)
**Date:** 2026-04-21
**Task:** Replicate MacBook Cloudflare Tunnel pattern on Mac Mini; unblock Render `OLLAMA_HOST`
**Funnel replacement:** ✅ Shipped
**Time:** ~18 min (inside 30-min timebox)

---

## 1. Final public URL

**`https://ollama-mini.brisen-infra.com`** → Mac Mini Ollama (`127.0.0.1:11434`)

Persistent via user LaunchAgent, auto-starts on login, restart-on-crash enabled.

---

## 2. Verification

### 2.1 `/api/tags` (model list)

```
$ curl -sS -m 15 -w "HTTP=%{http_code} time=%{time_total}s" \
    https://ollama-mini.brisen-infra.com/api/tags
HTTP=200 time=1.012s
```

Response body confirms both models present:

| Model | Size | Quant |
|---|---|---|
| `gemma4:latest` | 9.6 GB (8.0B params) | Q4_K_M |
| `qwen2.5:14b` | 9.0 GB (14.8B params) | Q4_K_M |

### 2.2 `/api/generate` (actual inference)

```
$ curl -sS -m 30 -X POST https://ollama-mini.brisen-infra.com/api/generate \
    -H 'Content-Type: application/json' \
    -d '{"model":"gemma4:latest","prompt":"Say hello in one word","stream":false,"options":{"num_predict":5}}'
HTTP=200 time=5.023s
{"model":"gemma4:latest","response":"Hello","done":true,"done_reason":"stop",
 "total_duration":4.67s,"load_duration":4.42s,"eval_count":2,"eval_duration":22ms,...}
```

First call was a cold-start (4.42s model load); subsequent calls will hit warm cache.

### 2.3 Edge connections (4 active)

From cloudflared log:
```
connIndex=0 location=mrs06 (Marseille) protocol=quic
connIndex=1 location=zrh01 (Zurich)     protocol=quic
connIndex=2 location=zrh02 (Zurich)     protocol=quic
connIndex=3 location=mrs06 (Marseille) protocol=quic
```

### 2.4 launchd service

```
$ launchctl list | grep cloudflared-ollama-mini
47038  0  com.user.cloudflared-ollama-mini
```

PID 47038, exit status 0, process name matches expected.

---

## 3. What was deployed

### 3.1 Files on Mac Mini

| Path | Perms | Purpose |
|---|---|---|
| `~/.cloudflared/cert.pem` | 600 | Cloudflare account cert (copied from MacBook — **same Cloudflare account**, allows tunnel mgmt without re-auth) |
| `~/.cloudflared/9e02acf0-6ff0-481f-951f-86671b6a2d11.json` | 400 | Tunnel credentials (auto-chmoded by `cloudflared tunnel create`) |
| `~/.cloudflared/config.yml` | 600 | Ingress config (see §3.2) |
| `~/Library/LaunchAgents/com.user.cloudflared-ollama-mini.plist` | 644 | User LaunchAgent (no sudo) |
| `~/Library/Logs/cloudflared-ollama-mini.log` | 644 | Tunnel log (stdout+stderr merged) |

### 3.2 `~/.cloudflared/config.yml` on Mac Mini

```yaml
tunnel: 9e02acf0-6ff0-481f-951f-86671b6a2d11
credentials-file: /Users/dimitry/.cloudflared/9e02acf0-6ff0-481f-951f-86671b6a2d11.json

ingress:
  - hostname: ollama-mini.brisen-infra.com
    service: http://127.0.0.1:11434
    originRequest:
      httpHostHeader: localhost
  - service: http_status:404
```

Note: `httpHostHeader: localhost` required — mirrors MacBook's existing `ollama.brisen-infra.com` config. Ollama enforces strict host-header match.

### 3.3 LaunchAgent (`com.user.cloudflared-ollama-mini.plist`)

Key elements:
- `ProgramArguments`: `/opt/homebrew/opt/cloudflared/bin/cloudflared tunnel --config ~/.cloudflared/config.yml run ollama-mini-mac`
- `RunAtLoad`: true
- `KeepAlive`: restart on crash, not on clean exit
- `StandardOutPath` + `StandardErrorPath`: merged to `~/Library/Logs/cloudflared-ollama-mini.log`

User-level agent → **no sudo required**, survives reboots, runs as `dimitry`.

### 3.4 Cloudflare side

- **Tunnel:** `ollama-mini-mac` (UUID `9e02acf0-6ff0-481f-951f-86671b6a2d11`)
- **DNS:** CNAME `ollama-mini.brisen-infra.com` → `9e02acf0-....cfargotunnel.com` (created via `cloudflared tunnel route dns`)
- **Account:** same as MacBook's `baker-chrome-bridge` tunnel (vallen300@gmail.com or linked Cloudflare account; verified by reusing `cert.pem`)
- **Independent of MacBook** — `ollama.brisen-infra.com` (MacBook) and `ollama-mini.brisen-infra.com` (Mac Mini) are two distinct tunnels pointing at two different Ollama instances

### 3.5 NOT modified

- ✅ MacBook's `baker-chrome-bridge` tunnel — untouched
- ✅ MacBook's `~/.cloudflared/*` — read-only, cert.pem copied out
- ✅ Mac Mini's Tailscale Funnel config — still armed on `443` → `:11434` (per AI Head's instruction: ready to re-use if TS fixes the edge bug)
- ✅ Render — env var NOT set (AI Head's Tier B action, pending Director auth)

---

## 4. Secrets hygiene

| Item | Location | Exposure |
|---|---|---|
| `cert.pem` | `~/.cloudflared/cert.pem` on MacBook + Mac Mini | mode 600 on both; never committed; account-wide cert (already existed on MacBook) |
| `<tunnel-uuid>.json` | `~/.cloudflared/...json` on Mac Mini only | mode 400; generated by `tunnel create`; local to device |
| `config.yml` | `~/.cloudflared/config.yml` on Mac Mini | no secrets inline; references creds-file by path |

Nothing in any git repo. Nothing leaked to chat. 1Password optional — these creds are host-bound and revocable via `cloudflared tunnel delete`.

**AI Head hygiene ask:** if policy requires a 1Password backup, B1 can export and save on request.

---

## 5. Next step — Render env var (Tier B, AI Head)

Once Director authorizes:

```
mcp__render__update_environment_variables
  serviceId: srv-d6dgsbctgctc73f55730
  envVars: [{"key":"OLLAMA_HOST","value":"https://ollama-mini.brisen-infra.com"}]
  replace: false   # merge mode
```

Then trigger deploy (env PUT does not auto-redeploy). After deploy goes live:

1. Wait ~2 min (one `KBL_PIPELINE_TICK_INTERVAL_SECONDS` cycle)
2. Query `signal_queue` — expect rows advancing past `stage='triage'`
3. Query `kbl_cost_ledger` — expect row count > 0 (Ollama reached)
4. Scan `kbl_log` for ERROR rows with `component='pipeline_tick'` in last 5 min — expect zero

---

## 6. Tailscale Funnel support ticket draft (parallel)

For Director (or AI Head) to paste into `support.tailscale.com`:

```
Title: Funnel returns HTTP/2 403 with empty body on all requests despite
       correct ACL + capabilities

Tailnet: vallen300@gmail.com (Free plan)
Node: dimitrys-mac-mini.tail4c0b32.ts.net
  ID: nQR9LbgKCf11CNTRL
  IP: 100.112.139.2
  Device ID (admin console): 4891396154003743
Tailscale version: 1.96.5-t4ee448d3a-g74ffbefc2 (macOS 26.3.0, arm64)
HTTPS cert: valid (Let's Encrypt E7, issued 2026-04-21 05:00:15 UTC)

ACL (verified via GET /api/v2/tailnet/-/acl):
    "nodeAttrs": [
        {"target": ["autogroup:member"], "attr": ["funnel"]}
    ]
    "grants": [
        {"src": ["*"], "dst": ["*"], "ip": ["*"]}
    ]

Node CapMap (via tailscale status --json):
    funnel, https, funnel-ports?ports=443,8443,10000, is-owner, is-admin

Tailnet settings (via GET /api/v2/tailnet/-/settings):
    httpsEnabled: true
    devicesApprovalOn: false
    tailnetLockError: ""  (on this device, via GET /api/v2/tailnet/-/devices)

Serve config (via `tailscale funnel status`):
    # Funnel on:
    https://dimitrys-mac-mini.tail4c0b32.ts.net (Funnel on)
    |-- / proxy http://127.0.0.1:11434

Symptom:
Every request to https://dimitrys-mac-mini.tail4c0b32.ts.net/... returns
HTTP/2 403 with empty body (content-length: 0, only "date" header, no
"server:" header). Tested via public Funnel edge IP (185.40.234.198)
with --resolve as well as via tailnet mesh IP (100.112.139.2) — same
403 both ways.

Ollama backend on :11434 responds 200 locally.
Port 8443 and 10000 time out at edge (only configured port is published).

All documented Funnel requirements appear satisfied but edge still rejects.
Please advise on what layer is gating this request.

Happy to share verbose curl traces, tailscaled logs, or additional API output.
Contact: dvallen@brisengroup.com
```

---

## 7. Cleanup pending

### 7.1 Tailscale API key (1-day expiry)

- Stored in 1Password vault "Baker API Keys" → "API Tailscale" (item ID `qr6bc63njj2n2o2jvqpwurvz2u`)
- Expires **2026-04-22** automatically
- Keep until TS support ticket is opened (may want to re-query ACL/devices)
- After ticket opened: B1 can delete the 1Password item + revoke key (admin console or `op item delete`)

### 7.2 No other residue

Tailscale Funnel binding on Mac Mini port 443 left armed — reversal cost nil. Cloudflare Tunnel runs alongside it.

---

## 8. Commands for future B1 / AI Head reference

```bash
# View tunnel status
ssh macmini 'launchctl list | grep cloudflared-ollama-mini'
ssh macmini 'tail -20 ~/Library/Logs/cloudflared-ollama-mini.log'

# Restart tunnel
ssh macmini 'launchctl kickstart -k gui/$(id -u)/com.user.cloudflared-ollama-mini'

# Stop tunnel
ssh macmini 'launchctl unload ~/Library/LaunchAgents/com.user.cloudflared-ollama-mini.plist'

# Verify public URL
curl -sS -m 10 https://ollama-mini.brisen-infra.com/api/tags | jq '.models[].name'

# Delete tunnel (if ever retired)
ssh macmini '/opt/homebrew/bin/cloudflared tunnel delete ollama-mini-mac'
# → remember to also remove the CNAME in Cloudflare dashboard + delete launchd plist
```

— B1
