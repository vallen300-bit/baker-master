---
review_for: PR #78 — CORTEX_TRIGGER_ENDPOINT_1
reviewer: b1
reviewed_at: 2026-04-29T00:30:00Z
trigger_class: HIGH
review_kind: structural-second-pair (RA-24)
brief: briefs/BRIEF_CORTEX_TRIGGER_ENDPOINT_1.md
ship_report: briefs/_reports/B2_cortex_trigger_endpoint_20260428.md
target_branch: cortex-trigger-endpoint-1
target_pr: https://github.com/vallen300-bit/baker-master/pull/78
verdict: PASS
---

# B1 — PR #78 Structural Review (CORTEX_TRIGGER_ENDPOINT_1)

## Verdict: ✅ PASS — all 7 review sections clear. APPROVE merge.

A is running `/security-review` in parallel; this report is structural only. Both must clear before Tier-A merge.

---

## §0 — Pre-flight

- Fetched PR head: `git fetch origin pull/78/head:pr-78-review` ✅
- Files touched (4 total, exactly per brief):
  - `outputs/dashboard.py` (+66/-0)
  - `tests/test_cortex_trigger_endpoint.py` (+140 NEW)
  - `briefs/_reports/B2_cortex_trigger_endpoint_20260428.md` (+141 NEW, bookkeeping)
  - `briefs/_tasks/CODE_2_PENDING.md` (+4/-4, B2 mailbox claim hygiene)
- Re-ran B2's literal test command on the PR head:

```
$ pytest tests/test_cortex_trigger_endpoint.py -v
collected 4 items
tests/test_cortex_trigger_endpoint.py::test_trigger_cortex_cycle_happy_path PASSED [ 25%]
tests/test_cortex_trigger_endpoint.py::test_trigger_cortex_cycle_unauthorized PASSED [ 50%]
tests/test_cortex_trigger_endpoint.py::test_trigger_cortex_cycle_validation_short_question PASSED [ 75%]
tests/test_cortex_trigger_endpoint.py::test_trigger_cortex_cycle_timeout_translates_to_504 PASSED [100%]
======================== 4 passed, 5 warnings in 1.61s =========================
```

- Phase 5 regression (45 tests = 4 trigger + 20 phase5_act + 21 phase5_idempotency): `45 passed in 1.58s` — clean.
- `py_compile` on `outputs/dashboard.py` clean (pre-existing `\[` regex SyntaxWarning at line 2530 is unrelated).

---

## §A — Auth correctness — ✅ PASS

| Item | Evidence | Verdict |
|---|---|---|
| `dependencies=[Depends(verify_api_key)]` matches existing pattern | `outputs/dashboard.py:4105` — same form as 60+ existing routes | ✅ |
| No second auth mechanism, no `Header(...)` on body, no token-in-body | `CortexTriggerRequest` (lines 343-352) has no token field; endpoint signature has only `req: CortexTriggerRequest`, no extra Header(...) | ✅ |
| 401 test covers wrong key AND verifies `maybe_run_cycle` NOT awaited | `test_trigger_cortex_cycle_unauthorized` lines 88-90: `assert resp.status_code == 401` + `m.assert_not_awaited()` | ✅ |

`verify_api_key` (line 96) raises 401 on mismatch (line 105-109) and 503 if `BAKER_API_KEY` env unset (line 100-103) — both paths return responses BEFORE the handler body is entered, so any handler-side logic is preempted.

---

## §B — Pydantic / input validation — ✅ PASS

```python
# outputs/dashboard.py:343-352
class CortexTriggerRequest(BaseModel):
    matter_slug: str = Field(..., min_length=1, max_length=64, ...)
    director_question: str = Field(..., min_length=10, max_length=4000, ...)
    triggered_by: str = Field(default="director_manual", min_length=1, max_length=64, ...)
```

| Item | Evidence | Verdict |
|---|---|---|
| All 3 fields have explicit min/max length bounds | Lines 347, 349, 351 | ✅ |
| director_question min_length=10 prevents trivial smoke abuse | `min_length=10` line 349 | ✅ |
| 422 test verifies handler not invoked | `test_trigger_cortex_cycle_validation_short_question` line 115: `m.assert_not_awaited()` | ✅ |
| No regex-based validation that could ReDoS | No `pattern=...` regex on any field; only length bounds | ✅ |

---

## §C — `maybe_run_cycle` integration — ✅ PASS (no signature drift)

Verified `maybe_run_cycle` source-of-truth signature at `orchestrator/cortex_runner.py:62-68`:

```python
async def maybe_run_cycle(
    *,
    matter_slug: str,
    triggered_by: str,
    trigger_signal_id: Optional[int] = None,
    director_question: Optional[str] = None,
) -> CortexCycle:
```

Endpoint call at `outputs/dashboard.py:4122-4126`:

```python
cycle = await maybe_run_cycle(
    matter_slug=req.matter_slug,
    triggered_by=req.triggered_by,
    director_question=req.director_question,
)
```

| Item | Evidence | Verdict |
|---|---|---|
| Kwargs-only, matches kw-only `*` signature | All 3 args passed by keyword; `trigger_signal_id` defaults to None (correct for Director-fire path) | ✅ |
| `await` used (async function, no blocking call) | `await maybe_run_cycle(...)` line 4122; no `asyncio.run()`, no `asyncio.run_coroutine_threadsafe()`, no `loop.run_until_complete()` | ✅ |
| `asyncio.TimeoutError` → 504 (test confirms) | Lines 4127-4135; `test_trigger_cortex_cycle_timeout_translates_to_504` confirms | ✅ |
| `except HTTPException: raise` prevents wrap-into-500 bug | Lines 4136-4138; placed BETWEEN TimeoutError and generic Exception so inner HTTPException propagates with its real status | ✅ |
| Generic `Exception` → 500 with `str(e)[:200]` truncation | Line 4144: `detail=f"Cycle invocation failed: {str(e)[:200]}"` | ✅ |
| Response dict reads only documented CortexCycle fields | Lines 4146-4155: 8 explicit field reads (cycle_id, matter_slug, triggered_by, status, current_phase, cost_tokens, cost_dollars, aborted_reason). No `.dict()`, no `.__dict__`, no `vars(cycle)`. `getattr(cycle, "aborted_reason", None)` defensive against missing attr. | ✅ |

**Except-clause ordering audit:** `TimeoutError` → `HTTPException` → `Exception`. Order is correct — if `HTTPException` came after the bare `Exception`, the latter would catch first since `HTTPException` is-a `Exception`. ✅

---

## §D — Logging discipline — ✅ PASS

Grep over `outputs/dashboard.py` for `director_question` and `aborted_reason`:

```
349:    director_question: str = Field(..., min_length=10, max_length=4000,    ← Pydantic field def
4118:    Sensitive payload (director_question, aborted_reason) is NOT info-logged —    ← docstring
4125:            director_question=req.director_question,                       ← kwarg pass to maybe_run_cycle
4154:        "aborted_reason": getattr(cycle, "aborted_reason", None),          ← response body field
```

| Item | Evidence | Verdict |
|---|---|---|
| `req.director_question` NEVER appears in any log call | Only 4 occurrences total, none in `logger.*()` calls | ✅ |
| `cycle.aborted_reason` NEVER appears in any log call | Only in response dict (line 4154); never in `logger.*()` | ✅ |
| Only `req.matter_slug` + `req.triggered_by` appear in error-level logs | `logger.error(... matter=%s, triggered_by=%s ...)` at lines 4128-4131 (timeout) and 4140-4143 (generic) — those two fields only | ✅ |

---

## §E — Test integrity (Lesson #48 — no "by inspection") — ✅ PASS

| Item | Evidence | Verdict |
|---|---|---|
| All 4 tests run as `pytest tests/test_cortex_trigger_endpoint.py -v` and PASS | Re-run on PR head: `4 passed, 5 warnings in 1.61s` (pasted in §0) | ✅ |
| All 4 tests use real `TestClient(app)` against actual FastAPI app | `from outputs.dashboard import app` (line 16) + `client = TestClient(app)` in each test | ✅ |
| `_FakeCycle` mirrors only the 8 fields the endpoint reads | Lines 19-30: cycle_id, matter_slug, triggered_by, status, current_phase, cost_tokens, cost_dollars, aborted_reason — exact superset of the 8 fields the endpoint pulls (no extras, no missing) | ✅ |
| No test silently skips on missing env / dependency | Each test uses `monkeypatch.setenv("BAKER_API_KEY", ...)` + rebinds `dash._BAKER_API_KEY` directly. Zero skip markers. | ✅ |

---

## §F — Scope discipline — ✅ PASS

| Item | Evidence | Verdict |
|---|---|---|
| Only 2 source files touched (mailbox + ship report = bookkeeping) | `gh pr diff 78 --name-only` returns: dashboard.py, test_cortex_trigger_endpoint.py, B2 ship report, CODE_2_PENDING.md (mailbox flip) | ✅ |
| `orchestrator/cortex_runner.py` untouched | Not in diff name list | ✅ |
| `triggers/cortex_pipeline.py` untouched | Not in diff name list | ✅ |
| No other dashboard endpoints modified | Three exact insertions in dashboard.py: import @ line 48, model @ lines 343-352, endpoint @ lines 4105-4155. No edits to existing endpoints. | ✅ |

---

## §G — Render deploy survival — ✅ PASS

| Item | Evidence | Verdict |
|---|---|---|
| No new env var required | Endpoint uses existing `_BAKER_API_KEY` (already on Render) | ✅ |
| No DB migration | Endpoint does no DDL/SQL of its own; `maybe_run_cycle` writes use existing `cortex_cycles` schema | ✅ |
| No new package dependency | `asyncio`, `fastapi`, `pydantic` all pre-existing | ✅ |

---

## §H — Summary table

| § | Section | Verdict |
|---|---|:---:|
| A | Auth correctness | ✅ PASS |
| B | Pydantic / input validation | ✅ PASS |
| C | `maybe_run_cycle` integration (no signature drift) | ✅ PASS |
| D | Logging discipline (no sensitive payload logged) | ✅ PASS |
| E | Test integrity (4/4 PASS literal, real TestClient) | ✅ PASS |
| F | Scope discipline (only brief-listed files touched) | ✅ PASS |
| G | Render deploy survival (no env / migration / dep) | ✅ PASS |

**Overall: 7 / 7 PASS. No blockers. No observations beyond what B2's ship report already captured.**

---

## §I — Comment-fallback APPROVE message text (for `gh pr comment 78`)

> ## ✅ B1 STRUCTURAL REVIEW — APPROVE
>
> Per `briefs/_tasks/CODE_1_PENDING.md` dispatch from AI Head A (HIGH trigger class, RA-24 second-pair-of-eyes review).
>
> **Verdict: PASS — all 7 review sections clear.**
>
> | § | Section | Verdict |
> |---|---|:---:|
> | A | Auth correctness (`Depends(verify_api_key)`, no second mechanism, 401 test asserts `m.assert_not_awaited()`) | ✅ |
> | B | Pydantic validation (3 fields × explicit min/max length, no ReDoS) | ✅ |
> | C | `maybe_run_cycle` integration (kwargs-only matches `*` kw-only signature, `await` used, TimeoutError→504, `except HTTPException: raise` ordering correct, no `.dict()`/`.__dict__` leak) | ✅ |
> | D | Logging discipline (`director_question` + `aborted_reason` NEVER appear in `logger.*()` calls — grep-confirmed) | ✅ |
> | E | Test integrity (4/4 PASS literal: 1.61s; real `TestClient(app)`; `_FakeCycle` exact-superset of read fields; no silent skips) | ✅ |
> | F | Scope discipline (only the 2 brief-listed files + bookkeeping; cortex_runner.py + cortex_pipeline.py untouched per `gh pr diff --name-only`) | ✅ |
> | G | Render deploy survival (no new env, no migration, no new dep) | ✅ |
>
> **Phase 5 regression:** 45/45 PASS in 1.58s (4 trigger + 20 phase5_act + 21 phase5_idempotency).
>
> **Lesson #48 compliance:** tests re-executed on PR head — literal stdout pasted in `briefs/_reports/B1_pr78_review_20260429.md` §0.
>
> **Formal GitHub APPROVE blocked by self-PR rule (B1/B2/A all under same GitHub identity):** this comment is the gate.
>
> Awaiting AI Head A's `/security-review` clearance before Tier-A squash-merge.
>
> Full review: `briefs/_reports/B1_pr78_review_20260429.md`

---

## §J — Co-authored-by

```
Co-authored-by: Code Brisen #1 <b1@brisengroup.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
