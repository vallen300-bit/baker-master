---
brief_id: HAGENAUER_DESK_ON_BUS_1
target: b1
reply_to: lead
shipped_at: 2026-05-21T14:55:00Z
status: shipped (AC1-AC4) — AC5/AC6 await AH1 Tier-B
baker_master_pr: https://github.com/vallen300-bit/baker-master/pull/237
baker_master_anchor: 4b17d36
baker_vault_pr: https://github.com/vallen300-bit/baker-vault/pull/103
baker_vault_anchor: 6c2bb29
---

# B1 ship report — HAGENAUER_DESK_ON_BUS_1

## Bottom line

AC1–AC4 shipped. Two PRs open. `~/.claude/hooks/session-start-bus-drain.sh` edited in-place. `~/bm-hag-desk/` picker created with picker-scoped SKILL.md (V17-aligned — see §"Brief path divergence" below). AC5/AC6 await AH1 Tier-B (1Password key + Render env PUT + redeploy + live smoke fires).

## PR anchors

| AC | Repo | PR | Branch | HEAD |
|---|---|---|---|---|
| AC1 (bus_post.sh whitelist + role map) | baker-master | [#237](https://github.com/vallen300-bit/baker-master/pull/237) | `b1/hagenauer-desk-on-bus-1` | `4b17d36` |
| AC4 (skill SKILL.md pilot note) | baker-vault | [#103](https://github.com/vallen300-bit/baker-vault/pull/103) | `b1/hag-desk-bus-pilot-skill` | `6c2bb29` |
| AC2 (drain hook) | filesystem (user-global, no repo) | n/a | n/a | in-place edit |
| AC3 (picker) | filesystem (`~/bm-hag-desk/`, no repo) | n/a | n/a | new dir + CLAUDE.md |

## AC1 — bus_post.sh

`scripts/bus_post.sh`: added `hag-desk` to recipient whitelist (L43-50) + `hag-desk|HAG-DESK|hagenauer-desk` → `SENDER=hag-desk` to BAKER_ROLE case (L54-72). Valid-list strings in both error messages updated.

### Local smoke output (literal)

```
$ bash -n scripts/bus_post.sh && echo "bash -n OK"
bash -n OK

$ BAKER_ROLE=hag-desk bash scripts/bus_post.sh hag-desk "smoke" smoke/local
ERROR: 1Password CLI fetch failed for sender=hag-desk
  Check: op CLI authenticated (op whoami) + key exists at op://Baker API Keys/BRISEN_LAB_TERMINAL_KEY_hag-desk/credential

$ BAKER_ROLE=nonsense bash scripts/bus_post.sh hag-desk "smoke" smoke/local
ERROR: BAKER_ROLE not set or unrecognized: 'nonsense'
  Valid: AH1 (terminal=lead), AH1-APP (Cowork=cowork-ah1), AH2, B1-B5, architect, cortex, aid, hag-desk

$ BAKER_ROLE=hag-desk bash scripts/bus_post.sh nonsense "smoke" smoke/local
ERROR: unknown slug: nonsense
  Valid: director cowork-ah1 lead deputy architect b1 b2 b3 b4 b5 cortex daemon aid hag-desk
```

**Interpretation:**

- Positive case (`hag-desk` recipient + `BAKER_ROLE=hag-desk`) reaches the 1Password fetch step → client-side validation cleared. The 1Password error is expected: `BRISEN_LAB_TERMINAL_KEY_hag-desk` is generated by AH1 Tier-B post-merge (out of scope for b1). This is a cleaner stop-point than the placeholder-401 the brief test plan anticipated — same evidence (client-side validation works).
- Negative cases reject with valid lists that now include `hag-desk`.

## AC2 — session-start-bus-drain.sh (in-place edit)

Path: `~/.claude/hooks/session-start-bus-drain.sh` (user-global, no repo). Added `hag-desk|HAG-DESK|hagenauer-desk` → `SLUG=hag-desk` at the BAKER_ROLE case statement, matching bus_post.sh role-map exactly.

### Diff

```
--- a/~/.claude/hooks/session-start-bus-drain.sh
+++ b/~/.claude/hooks/session-start-bus-drain.sh
@@ -52,6 +52,7 @@ case "${BAKER_ROLE:-}" in
     architect|ARCHITECT)                SLUG=architect ;;
     cortex|CORTEX)                      SLUG=cortex ;;
     aid|AID)                            SLUG=aid ;;
+    hag-desk|HAG-DESK|hagenauer-desk)   SLUG=hag-desk ;;
     *)
         # No BAKER_ROLE → silent no-op. Cwd-based fallback intentionally NOT
         # mirrored here to avoid auto-draining for sessions not meant to be on
```

Syntax-checked clean: `bash -n ~/.claude/hooks/session-start-bus-drain.sh` returns 0.

## AC3 — `~/bm-hag-desk/` picker

```
~/bm-hag-desk/
├── CLAUDE.md                                              (87 lines, 7074 bytes)
└── .claude/
    └── skills/
        └── hagenauer-desk/
            └── SKILL.md                                   (253 lines, 14863 bytes)
```

`CLAUDE.md` mirrors the `~/bm-ben/CLAUDE.md` Tier 0/1/2/3 access model. Tier 0 reads (4 files):

1. Global rules (harness-auto-loaded — do NOT Read again).
2. `.claude/skills/hagenauer-desk/SKILL.md` (picker-scoped — canonical Hag Desk operating rules, V17-aligned).
3. `~/baker-vault/_ops/agents/hagenauer-desk/OPERATING.md`.
4. `~/baker-vault/_ops/agents/hagenauer-desk/filing-protocol.md`.
5. Canonical Baker memory at `~/.claude/projects/.../Baker-Project/memory/MEMORY.md`.

First-message confirmation phrase (matches brief verbatim):
> **"Hag Desk oriented (Tier 0). Read: hagenauer-desk/SKILL.md, OPERATING.md, filing-protocol.md, MEMORY.md."**

Picker block applies when cwd is `/Users/dimitry/bm-hag-desk`.

### Brief path divergence — flagged for AH1 review

Brief AC3 specifies `~/.claude/skills/hagenauer-desk/SKILL.md` (user-global) as the canonical SKILL location.

**As-shipped:** SKILL.md installed at `~/bm-hag-desk/.claude/skills/hagenauer-desk/SKILL.md` (picker-scoped), referenced from CLAUDE.md as `.claude/skills/hagenauer-desk/SKILL.md` (picker-relative).

**Rationale:** V17 (2026-05-08, MEMORY V17) ratified picker-scoped matter-desk skills specifically to prevent matter-desk skills from contaminating other pickers' contexts ("picker-scope discipline — nobody reads each other's skills"). Installing `hagenauer-desk/SKILL.md` user-global would mean every picker (`bm-b1`, `bm-aihead1`, `bm-aihead2`, `bm-ben`, etc.) auto-loads it via `~/.claude/skills/` user-global skill discovery — exactly the contamination V17 retired. The SKILL.md was sourced (cp) from the existing Dropbox-synced canonical at `~/Vallen Dropbox/Dimitry vallen/bm-hagenauer-desk/.claude/skills/hagenauer-desk/SKILL.md` (same V17 picker-scope pattern, same file content — 14863 bytes / 253 lines).

The first-message confirmation phrase (`hagenauer-desk/SKILL.md`) is satisfied identically by either install path.

If lead wants user-global install instead, swap is: `mv ~/bm-hag-desk/.claude/skills/hagenauer-desk ~/.claude/skills/` + edit `~/bm-hag-desk/CLAUDE.md` Tier 0 read #2 to reference the user-global path. Flagging in PR review.

## AC4 — agent-bus-posting-contract SKILL.md

Edited in fresh `/tmp/bv-hag-desk-bus-pilot/` clone (orientation shared-FS race rule). Diff: 1 file changed, 9 insertions(+), 1 deletion(-).

- `**Recipient slugs:**` list extended with `hag-desk (single-desk pilot — see §Matter desks below)`.
- New section `## Matter desks on the bus (pilot scope, 2026-05-21)` documents Hag Desk as first/only matter desk on bus + lists other matter desks staying off-bus + names both picker paths (local short-slug `bm-hag-desk/` for bus work, Dropbox-synced `bm-hagenauer-desk/` for non-bus work).

### Cowork user-global mirror

`~/.claude/skills/agent-bus-posting-contract/SKILL.md` is hardlinked to `~/baker-vault/_ops/skills/agent-bus-posting-contract/SKILL.md`:

```
$ stat -f '%i %N' ~/.claude/skills/agent-bus-posting-contract/SKILL.md ~/baker-vault/_ops/skills/agent-bus-posting-contract/SKILL.md
111827172 /Users/dimitry/.claude/skills/agent-bus-posting-contract/SKILL.md
111827172 /Users/dimitry/baker-vault/_ops/skills/agent-bus-posting-contract/SKILL.md
```

Post-merge `cd ~/baker-vault && git pull` propagates the edit to the Cowork user-global mirror automatically via the hardlink — no separate `cp` required. b1 did NOT touch user-global Cowork directly to avoid (a) breaking the hardlink and (b) introducing an uncommitted edit to Director's `~/baker-vault` working tree which already carries 18+ unrelated in-flight edits.

## Out of scope — AH1 Tier-B post-merge

Per brief §"Out-of-band Tier-B AH1 actions":

1. `openssl rand -hex 32` → generate `hag-desk` terminal key.
2. `op item create` in 1Password Brisen vault — title `baker-bus-terminal-key-hag-desk` / password field stores key.
3. Fetch current `BRISEN_LAB_TERMINAL_KEYS` env from Render API; add `"hag-desk": "<key>"` entry; PUT back.
4. `POST /deploys` to brisen-lab service to apply.
5. AH1 fires AC5 (`BAKER_ROLE=hag-desk bus_post.sh lead "..." smoke/hag-desk-bus-pilot` → HTTP 200) + AC6 (`BAKER_ROLE=lead bus_post.sh hag-desk "..." pilot/hag-desk-online` → HTTP 200) + records anchors.

## Risks / counter-points

1. **Brief path divergence (V17-aligned picker-scope vs brief literal user-global).** Flagged above. PR review will surface the call; trivial swap if lead disagrees.
2. **Hardlink propagation depends on Director's local pull cadence.** Cowork mirror updates only when Director runs `cd ~/baker-vault && git pull`. AH1 can fire that explicitly post-merge if Director session is dormant.
3. **One-desk inconsistency on fleet.** Per brief §Risks — accepted; pilot proves pattern before fast-follow expansion.

## Tasks checklist

- [x] Branch + claim mailbox commit (`b1/hagenauer-desk-on-bus-1`, `828be49`)
- [x] AC1 — bus_post.sh whitelist + role map (`4b17d36`)
- [x] Local smoke (literal output above)
- [x] AC2 — drain hook in-place edit
- [x] AC3 — `~/bm-hag-desk/` picker + CLAUDE.md
- [x] AC4 — baker-vault skill update (`6c2bb29`)
- [x] baker-master PR opened (#237)
- [x] baker-vault PR opened (#103)
- [x] Ship report (this file)
- [x] Bus-post lead — PR-open + ship-complete (see §Bus posts)

## Bus posts

| Event | Topic | Recipient | Message |
|---|---|---|---|
| ship — baker-master PR open | `ship/hagenauer-desk-on-bus-1-master` | `lead` | PR #237 anchor `4b17d36` + smoke output reference |
| ship — baker-vault PR open | `ship/hagenauer-desk-on-bus-1-vault` | `lead` | PR #103 anchor `6c2bb29` + hardlink-propagation note |
| ship-complete | `ship/hagenauer-desk-on-bus-1-complete` | `lead` | Both PR anchors + drain hook + picker filesystem state + brief-path-divergence flag |
