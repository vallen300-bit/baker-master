# B1 — PR #83 STRUCTURAL REVIEW (CORTEX_PHASE5_STATUS_RECONCILE_1)

**Reviewer:** Code Brisen #1 (B1)
**Builder:** B3 (≠ B1 ✓)
**Authorized by:** AI Head A — Director path-B ratification post cycle-7dc3201b reject test
**PR:** https://github.com/vallen300-bit/baker-master/pull/83
**Branch:** `cortex-phase5-status-reconcile-1`
**PR head:** `adc1577` (`adc157708f04b3e102916dca4ee074de50627691`)
**Merge-base:** `0c69c9b` (brief+dispatch parent commit)
**Brief:** `briefs/BRIEF_CORTEX_PHASE5_STATUS_RECONCILE_1.md`
**Trigger class:** HIGH (DB migration + cross-capability state writes — RA-24)
**Date:** 2026-04-29
**Verdict:** ✅ **OVERALL: PASS** — 10/10 sections clear; 4 non-blocking observations (§K).

---

## §0 — Ship-gate execution (literal stdout per Lesson #48)

### 0.1 — Phase 5 + idempotency suites (44 tests on PR head)

```
$ ~/bm-b2/.venv312/bin/pytest tests/test_cortex_phase5_act.py tests/test_cortex_phase5_idempotency.py -v
============================= test session starts ==============================
platform darwin -- Python 3.12.12, pytest-9.0.3, pluggy-1.6.0 -- /Users/dimitry/bm-b2/.venv312/bin/python3.12
cachedir: .pytest_cache
rootdir: /Users/dimitry/bm-b1
plugins: langsmith-0.7.33, anyio-4.13.0
collecting ... collected 44 items

tests/test_cortex_phase5_act.py::test_archive_cycle_updates_and_inserts PASSED [  2%]
tests/test_cortex_phase5_act.py::test_feedback_ledger_uses_canonical_columns PASSED [  4%]
tests/test_cortex_phase5_act.py::test_feedback_ledger_payload_includes_cycle_id PASSED [  6%]
tests/test_cortex_phase5_act.py::test_cortex_reject_archives_and_writes_feedback PASSED [  9%]
tests/test_cortex_phase5_act.py::test_cortex_reject_default_reason_when_missing PASSED [ 11%]
tests/test_cortex_phase5_act.py::test_cortex_edit_persists_edited_text PASSED [ 13%]
tests/test_cortex_phase5_act.py::test_cortex_edit_no_edits_returns_warning PASSED [ 15%]
tests/test_cortex_phase5_act.py::test_cortex_approve_returns_freshness_warning_when_not_fresh PASSED [ 18%]
tests/test_cortex_phase5_act.py::test_cortex_approve_dry_run_skips_execute PASSED [ 20%]
tests/test_cortex_phase5_act.py::test_cortex_approve_writes_gold_then_propagates_then_archives PASSED [ 22%]
tests/test_cortex_phase5_act.py::test_cortex_approve_no_cycle_returns_error PASSED [ 25%]
tests/test_cortex_phase5_act.py::test_is_fresh_fails_open_on_db_error PASSED [ 27%]
tests/test_cortex_phase5_act.py::test_is_fresh_returns_false_when_recent_email_matches PASSED [ 29%]
tests/test_cortex_phase5_act.py::test_write_gold_proposals_calls_gold_proposer_propose PASSED [ 31%]
tests/test_cortex_phase5_act.py::test_write_gold_proposals_continues_on_individual_failure PASSED [ 34%]
tests/test_cortex_phase5_act.py::test_write_gold_proposals_empty_returns_zero PASSED [ 36%]
tests/test_cortex_phase5_act.py::test_propagate_logs_only_when_mac_mini_host_unset PASSED [ 38%]
tests/test_cortex_phase5_act.py::test_propagate_skips_when_no_staged_files PASSED [ 40%]
tests/test_cortex_phase5_act.py::test_cortex_refresh_returns_new_proposal_id PASSED [ 43%]
tests/test_cortex_phase5_act.py::test_cortex_refresh_no_cycle PASSED     [ 45%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_first_fire_returns_none PASSED [ 47%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_second_fire_returns_already_actioned PASSED [ 50%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_missing_cycle_returns_not_found_marker PASSED [ 52%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_no_db_returns_error PASSED [ 54%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_accepts_proposed PASSED [ 56%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_accepts_tier_b_pending PASSED [ 59%]
tests/test_cortex_phase5_idempotency.py::test_cas_lock_cycle_rejects_random_state PASSED [ 61%]
tests/test_cortex_phase5_idempotency.py::test_cortex_approve_first_fire_proceeds_normally PASSED [ 63%]
tests/test_cortex_phase5_idempotency.py::test_cortex_approve_second_fire_returns_already_actioned PASSED [ 65%]
tests/test_cortex_phase5_idempotency.py::test_cortex_approve_third_fire_still_idempotent PASSED [ 68%]
tests/test_cortex_phase5_idempotency.py::test_cortex_edit_first_fire_persists_then_releases PASSED [ 70%]
tests/test_cortex_phase5_idempotency.py::test_cortex_edit_second_fire_returns_already_actioned_no_insert PASSED [ 72%]
tests/test_cortex_phase5_idempotency.py::test_cortex_edit_third_fire_still_idempotent PASSED [ 75%]
tests/test_cortex_phase5_idempotency.py::test_cortex_refresh_first_fire_proceeds_then_releases PASSED [ 77%]
tests/test_cortex_phase5_idempotency.py::test_cortex_refresh_second_fire_returns_already_actioned PASSED [ 79%]
tests/test_cortex_phase5_idempotency.py::test_cortex_refresh_third_fire_still_idempotent PASSED [ 81%]
tests/test_cortex_phase5_idempotency.py::test_cortex_reject_first_fire_archives_with_from_status PASSED [ 84%]
tests/test_cortex_phase5_idempotency.py::test_cortex_reject_second_fire_returns_already_actioned PASSED [ 86%]
tests/test_cortex_phase5_idempotency.py::test_cortex_reject_third_fire_still_idempotent PASSED [ 88%]
tests/test_cortex_phase5_idempotency.py::test_archive_cycle_with_from_status_succeeds_on_match PASSED [ 90%]
tests/test_cortex_phase5_idempotency.py::test_archive_cycle_with_from_status_returns_warning_on_mismatch PASSED [ 93%]
tests/test_cortex_phase5_idempotency.py::test_archive_cycle_without_from_status_legacy_unconditional PASSED [ 95%]
tests/test_cortex_phase5_idempotency.py::test_cortex_approve_all_gold_fails_returns_approved_with_errors PASSED [ 97%]
tests/test_cortex_phase5_idempotency.py::test_cortex_approve_some_gold_fails_returns_approved_with_partial_errors PASSED [100%]

============================== 44 passed in 0.06s ==============================
```

**44 / 44 PASS in 0.06s** — includes the 3 NEW reconcile tests at indices 56% / 59% / 61%.

### 0.2 — Cross-cap regression (34 tests on PR head)

```
$ ~/bm-b2/.venv312/bin/pytest tests/test_cortex_runner_phase126.py tests/test_cortex_pre_review_gate.py tests/test_cortex_slack_interactivity.py
======================== 34 passed, 6 warnings in 1.97s ========================
```

**34 / 34 PASS in 1.97s** — runner_phase126 + pre_review_gate + slack_interactivity (the three nearby capabilities most exposed to a CAS-shape change).

### 0.3 — Syntax check both touched .py

```
$ python3 -c "import py_compile; py_compile.compile('orchestrator/cortex_phase5_act.py', doraise=True); py_compile.compile('memory/store_back.py', doraise=True); print('OK both')"
OK both
```

### 0.4 — PR file scope (8 files, gh API)

```
$ gh pr view 83 --json files --jq '.files[].path' | sort
briefs/_reports/B3_cortex_phase5_status_reconcile_20260429.md
briefs/_tasks/CODE_3_PENDING.md
memory/MEMORY.md
memory/feedback_render_envvar_paginated_put.md
memory/store_back.py
migrations/20260429_cortex_cycles_add_transient_statuses.sql
orchestrator/cortex_phase5_act.py
tests/test_cortex_phase5_idempotency.py
```

```
$ MB=$(git merge-base origin/main HEAD); git diff --name-only "$MB"..HEAD | sort
briefs/_reports/B3_cortex_phase5_status_reconcile_20260429.md
briefs/_tasks/CODE_3_PENDING.md
memory/MEMORY.md
memory/feedback_render_envvar_paginated_put.md
memory/store_back.py
migrations/20260429_cortex_cycles_add_transient_statuses.sql
orchestrator/cortex_phase5_act.py
tests/test_cortex_phase5_idempotency.py
```

Both methods agree: **exactly 8 files**, matching brief §I expectation.

```
$ git diff "$MB"..HEAD -- triggers/ orchestrator/cortex_runner.py orchestrator/cortex_phase4_proposal.py | wc -l
       0
```

Phase 1-4 paths + all triggers UNTOUCHED.

---

## §A — `_cas_lock_cycle` signature evolution — ✅ PASS

**Evidence:** `orchestrator/cortex_phase5_act.py:49-128`

- Type hint exact match: `from_statuses: tuple[str, ...] | list[str] | str` (line 52). ✓
- Coercion correct (lines 77-80):
  ```python
  if isinstance(from_statuses, str):
      from_statuses_list = [from_statuses]
  else:
      from_statuses_list = list(from_statuses)
  ```
  Single-string falls through to 1-element list; tuple/list copied to list. List-coercion eats both `tuple` and `list` inputs cleanly.
- SQL is param-bound, not interpolated:
  ```sql
  WHERE cycle_id=%s AND status = ANY(%s)
  ```
  (line 95) — psycopg2 maps Python `list` to PG array. No SQL injection surface introduced.
- Docstring updated (lines 56-67) explicitly documents BOTH `proposed` and `tier_b_pending` as valid pre-button states, naming Phase-4 → tier_b_pending and legacy direct-test → proposed.
- Return semantics preserved: `None` on transition success (line 119), warning dict on no-match (lines 111-116), re-raise on `Exception` (line 126). Fail-CLOSED behavior on DB failure preserved (line 73-75 docstring + line 120-126 try/except).

## §B — 4 handler call sites updated correctly — ✅ PASS

**Evidence:** `orchestrator/cortex_phase5_act.py`

| Handler | Line | `from_statuses=` | `to_status=` |
|---|---|---|---|
| `cortex_approve` | 187-191 | `("proposed", "tier_b_pending")` | `"approving"` |
| `cortex_edit` | 277-281 | `("proposed", "tier_b_pending")` | `"editing"` |
| `cortex_refresh` | 328-332 | `("proposed", "tier_b_pending")` | `"refreshing"` |
| `cortex_reject` | 393-397 | `("proposed", "tier_b_pending")` | `"rejecting"` |

All 4 dispatches use the identical 2-tuple of pre-button states. No site accepts a status outside the canonical 2.

## §C — Existing direct callers updated — ✅ PASS

**Evidence:** `grep -n "_cas_lock_cycle" orchestrator/cortex_phase5_act.py`

```
49:def _cas_lock_cycle(
125:        logger.error("_cas_lock_cycle failed cycle=%s: %s", cycle_id, e)
187:    guard = _cas_lock_cycle(  # cortex_approve
277:    guard = _cas_lock_cycle(  # cortex_edit
328:    guard = _cas_lock_cycle(  # cortex_refresh
393:    guard = _cas_lock_cycle(  # cortex_reject
```

6 occurrences = 1 def + 1 logger format-string + 4 handler dispatches. **Zero leftover direct callers using the old `from_status=` kwarg.**

In `tests/test_cortex_phase5_idempotency.py`, all 7 direct-call test sites use the new kwarg shape:

```
$ grep -B1 -A3 "_cas_lock_cycle(" tests/test_cortex_phase5_idempotency.py | grep "from_status"
        "cyc-1", from_statuses="proposed", to_status="approving",
        "cyc-1", from_statuses="proposed", to_status="approving",
        "cyc-missing", from_statuses="proposed", to_status="approving",
        "cyc-1", from_statuses="proposed", to_status="approving",
        from_statuses=("proposed", "tier_b_pending"),
        from_statuses=("proposed", "tier_b_pending"),
        from_statuses=("proposed", "tier_b_pending"),
```

4 string-form (legacy backward-compat path, exercises str→list coercion) + 3 tuple-form (new behavior). All `from_statuses=`. ✓

`_cas_release_to_proposed` (line 131) and `_archive_cycle` (line 616) are different functions whose `from_status=` kwargs are intentionally preserved — those carry a single `*ing` transient lock value (e.g. `from_status="approving"`), not a multi-state set.

## §D — Migration SQL correctness — ✅ PASS

**File:** `migrations/20260429_cortex_cycles_add_transient_statuses.sql` (69 lines)

- Atomicity: `BEGIN;` (line 23) … `COMMIT;` (line 51). ✓
- Idempotency: `DROP CONSTRAINT IF EXISTS` (line 25). Safe to re-run. ✓
- Constraint name preserved: `cortex_cycles_status_check` (lines 25 + 27 — both DROP and ADD use the same name; DOES NOT orphan the live constraint added by Director's 09:47Z manual ALTER). ✓
- Enumerates exactly 15 statuses (lines 28-49):
  - **Originals (10):** `in_flight`, `awaiting_reason`, `proposed`, `tier_b_pending`, `approved`, `rejected`, `modified`, `failed`, `superseded`, `abandoned`.
  - **Prior fold (1):** `archive_failed` (added 2026-04-29 by `20260429_cortex_cycles_add_archive_failed_status.sql`).
  - **NEW (4):** `approving`, `rejecting`, `editing`, `refreshing` — the *ing transient locks set by `_cas_lock_cycle`.
- Header comments (lines 1-21) document provenance: PR #75 introduced the *ing writes, 09:47Z manual ALTER, drift-defense companion in `store_back.py`. Operator-runnable invocation snippet on line 21.
- `migrate:down` block included as a commented disaster-recovery template (lines 54-69) with explicit drain step. (See §K obs #1.)

## §E — `store_back.py` drift-defense match — ✅ PASS

**Evidence:** `memory/store_back.py:587` (only diff in this file):

```diff
-    CHECK (status IN ('in_flight','awaiting_reason','proposed','tier_b_pending','approved','rejected','modified','failed','superseded','abandoned','archive_failed')),
+    CHECK (status IN ('in_flight','awaiting_reason','proposed','tier_b_pending','approved','rejected','modified','failed','superseded','abandoned','archive_failed','approving','rejecting','editing','refreshing')),
```

Sorted-set comparison vs migration enumeration:

| store_back.py:587 | migration line 28-49 |
|---|---|
| `in_flight` | `in_flight` |
| `awaiting_reason` | `awaiting_reason` |
| `proposed` | `proposed` |
| `tier_b_pending` | `tier_b_pending` |
| `approved` | `approved` |
| `rejected` | `rejected` |
| `modified` | `modified` |
| `failed` | `failed` |
| `superseded` | `superseded` |
| `abandoned` | `abandoned` |
| `archive_failed` | `archive_failed` |
| `approving` | `approving` |
| `rejecting` | `rejecting` |
| `editing` | `editing` |
| `refreshing` | `refreshing` |

**15 / 15 identical.** Drift-defense holds. Exactly mirrors the PR #82 `archive_failed` fold-in pattern.

No other status-enum literal lists in the repo were searched and found out-of-sync (the `cortex_cycles.status` value space is centralized in these two locations).

## §F — `feedback_render_envvar_paginated_put.md` shape — ✅ PASS

**File:** `memory/feedback_render_envvar_paginated_put.md` (31 lines)

- Frontmatter (lines 1-5): `name`, `description` (one-line), `type: feedback`. ✓
- Body opens with **Rule:** (line 7) — the operative claim: never raw PUT without paginating GET; default page is 20.
- **Why:** (line 11) cites the 2026-04-29 09:14Z incident concretely:
  - actor: AI Head A
  - mechanism: raw PUT after `?limit=` un-set GET returned 20 of ~100 vars
  - blast radius: 80 vars wiped
  - recovery cost: ~45 min, 32 regenerated, some still on code-default fallbacks
- **How to apply:** (line 22) lists 3 remediations (`?limit=100`, per-key endpoint, MCP merge mode) + defense-in-depth (log+abort on shrinking) + cross-ref to `.claude/rules/python-backend.md` + hard-stop hook suggestion.
- **Secret-name discipline holds:** the file references `SLACK_SIGNING_SECRET`, `BAKER_API_KEY`, `POSTGRES_PASSWORD`, `QDRANT_API_KEY`, `ANTHROPIC_API_KEY`, `NEON_*`, `BAKER_*` — **all by NAME, never by VALUE**. Brief STOP criterion on this point: clear.

## §G — `MEMORY.md` index entry — ✅ PASS

**File:** `memory/MEMORY.md` (177 bytes, 1 line; new file).

```
- [Render env-var paginated PUT regression](feedback_render_envvar_paginated_put.md) — never raw PUT without ?limit=100; default page is 20, silently wipes vars beyond page 1
```

- Format matches: `- [Title](file.md) — one-line hook` ✓
- ~165 chars, well under the 200-char ceiling ✓
- File was new (didn't exist on PR base) — B3 created it as the project-scoped memory index. Acceptable: this mirrors the auto-memory `MEMORY.md` convention applied to the project's own `memory/` directory. No conflict with my `~/.claude/projects/-Users-dimitry-bm-b1/memory/MEMORY.md` (separate auto-memory namespace).

## §H — Test integrity — ✅ PASS

**Evidence:** §0.1 + §0.2 stdout literal.

- 3 NEW reconcile tests present and PASS:
  - `test_cas_lock_cycle_accepts_proposed` (test file line 188): asserts `params[2] == ["proposed", "tier_b_pending"]` — **proves the correct list was passed to ANY(%s)**.
  - `test_cas_lock_cycle_accepts_tier_b_pending` (line 208): asserts `"tier_b_pending" in params[2]` AND `committed is True` — **proves the new state is server-side accepted**.
  - `test_cas_lock_cycle_rejects_random_state` (line 229) [optional in brief, present]: asserts `result["warning"] == "already_actioned"` for `failed` state — **proves multi-state acceptance does NOT include unrelated terminals**.
- Existing 41 tests updated to new kwarg shape; 41 + 3 = 44 / 44 PASS in 0.06s on PR head.
- Cross-cap regression: 34 / 34 PASS in 1.97s.
- **Zero `pytest.skip`, `xfail`, or "by inspection" claims.** All assertions are runtime-evaluated. New tests carry load-bearing parameter-introspection asserts (not just resp.status_code == X). Lesson #50 satisfied.

## §I — Scope discipline — ✅ PASS

8 files modified (matches brief). See §0.4 stdout literal — both the gh API and `git diff merge-base..HEAD` agree.

```
briefs/_reports/B3_cortex_phase5_status_reconcile_20260429.md   (NEW, ship report)
briefs/_tasks/CODE_3_PENDING.md                                  (mailbox flip)
memory/MEMORY.md                                                 (NEW, index)
memory/feedback_render_envvar_paginated_put.md                   (NEW, feedback)
memory/store_back.py                                             (1 +1 / -1 line CHECK)
migrations/20260429_cortex_cycles_add_transient_statuses.sql     (NEW, 69 lines)
orchestrator/cortex_phase5_act.py                                (+21 / -10 lines)
tests/test_cortex_phase5_idempotency.py                          (+73 / -4 lines)
```

`triggers/slack_interactivity.py`, `triggers/cortex_stuck_cycle_sentinel.py`, `orchestrator/cortex_runner.py`, `orchestrator/cortex_phase4_proposal.py`, all of Phase 1-4 — **0 lines diff vs merge-base**. ✓

## §J — Render deploy survival — ✅ PASS

- Migration is **additive** (DROP + ADD same constraint name) — no data migration, no row deletion. Existing rows in any of the 11 prior valid statuses continue to validate.
- Constraint name `cortex_cycles_status_check` preserved → does NOT orphan the live constraint added at 09:47Z manually. Migration is essentially a no-op on the current prod, but **mandatory for fresh DB / replica spin-up** to keep parity.
- No new third-party dependency added.
- No new environment variable added by this PR (the feedback memory documents env-var ops but doesn't introduce one).
- Idempotent (`DROP CONSTRAINT IF EXISTS`) — safe to re-run if the bootstrapping app-boot migration runner replays.

---

## STOP-criteria checklist

| Criterion | Status |
|---|---|
| `_cas_lock_cycle` accepts a state outside `proposed`/`tier_b_pending` | ❌ NOT TRIGGERED — `test_cas_lock_cycle_rejects_random_state` proves `"failed"` is rejected |
| Migration changes constraint NAME (orphans live constraint) | ❌ NOT TRIGGERED — both DROP and ADD use `cortex_cycles_status_check` |
| store_back bootstrap CHECK enum diverges from migration CHECK enum | ❌ NOT TRIGGERED — 15 / 15 identical (§E) |
| Tests fail or any "by inspection" / `pytest.skip` claims | ❌ NOT TRIGGERED — 78 / 78 PASS literally; no skip |
| Files outside the 8-file scope modified | ❌ NOT TRIGGERED — exactly 8 files |
| Pre-existing handler logic semantically changed beyond kwarg + SQL evolution | ❌ NOT TRIGGERED — handler bodies unchanged; only call signature evolved |
| Feedback memory file has values that look like real secrets | ❌ NOT TRIGGERED — only NAMES referenced in backticks; no values |

**No STOP criterion triggered. PASS.**

---

## §K — Non-blocking observations (note-only)

1. **Migration `down` block is commented-out, not active** (`migrations/20260429_cortex_cycles_add_transient_statuses.sql:54-69`). The `-- == migrate:down ==` marker on line 54 IS a parser hook for tools like `dbmate` / `golang-migrate` — but the actual SQL body of the down step is comment-prefixed, so a tooling-driven rollback would silently no-op. The `up` block on line 1 has the same parser-hook style and IS active. This is presumably deliberate ("disaster recovery only — operator must hand-uncomment after draining transient rows") and the header comment on line 55-59 documents the drain prerequisite. Worth flagging in case the project later adopts an active migration tool. **Not a blocker.**

2. **str→list coercion path is exercised via legacy tests** — the 3 NEW reconcile tests use the tuple form (`from_statuses=("proposed", "tier_b_pending")`), but the 4 pre-existing direct-call tests were updated to use the string form (`from_statuses="proposed"`), which exercises the `isinstance(from_statuses, str)` branch (lines 77-78). Net coverage: both branches of the coercion exercised. Just an observation — no gap.

3. **Stale doc-only reference in `triggers/slack_interactivity.py:37`** says "Phase 5 handlers themselves use `_cas_lock_cycle` (CORTEX_PHASE5_IDEMPOTENCY_1)". Still accurate at the function-name level, but doesn't mention the new multi-state behavior. The triggers file is correctly UNTOUCHED per scope discipline; a future doc-sweep could fold this in alongside `CORTEX_PHASE5_STATUS_RECONCILE_1`. **Not a blocker.**

4. **Project-scope `memory/MEMORY.md` is a NEW file** (didn't exist on PR base). The auto-memory MEMORY.md at `~/.claude/projects/-Users-dimitry-bm-b1/memory/MEMORY.md` already follows this convention; B3 has now mirrored it for the project's own `memory/` directory. Two separate index files now exist (one auto-memory, one project-scoped). Both legitimate. No conflict, but worth noting for future readers expecting a single MEMORY.md.

---

## Lesson #48 compliance

All ship-gate suites re-executed on PR head `adc1577`. Literal stdout pasted in §0.1 / §0.2 / §0.3. No "by-inspection" claims; no `pytest.skip`. Total runtime: **0.06s + 1.97s = 2.03s**.

## Self-PR rule (precedent #67/#69/#70/#71/#72/#73/#74/#78/#80/#81)

Builder is B3 (≠ B1 ✓), but all bots share the same Claude identity at the GitHub-API layer. Per established precedent, formal GitHub `APPROVE` review is blocked; **comment-fallback approval is the gate**.

---

## OVERALL: ✅ PASS

10 / 10 sections clear. 0 STOP criteria triggered. 4 non-blocking observations (§K) — all note-only.

**Awaiting:** AI Head A's `/security-review` clearance before merge (HIGH trigger class default).

## Co-Authored-By

```
Co-authored-by: Code Brisen #1 <b1@brisengroup.com>
Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```
