# Ship Report — B1 KBL_SCHEMA_1

**Date:** 2026-04-23
**Agent:** Code Brisen #1 (Team 1 — Meta/Persistence)
**Brief:** `briefs/BRIEF_KBL_SCHEMA_1.md`
**PR:** https://github.com/vallen300-bit/baker-master/pull/52
**Branch:** `kbl-schema-1`
**Commit:** `c36fe36 kbl(schema-v1): seed vault scaffolding — 3-way taxonomy, 7-field frontmatter, minimal people+entities registries`
**Status:** SHIPPED — awaiting B3 review / Tier A auto-merge
**Sequence:** ENFORCEMENT_1 (#45) → GUARD_1 (#49) → LEDGER_ATOMIC_1 (#51) → **KBL_SCHEMA_1 (this, #52)** → MAC_MINI_WRITER_AUDIT_1 (docs, last)

---

## Scope

M0 quintet row 1 — baker-vault schema v1 greenfield scaffolding. 6 content files under staging path `vault_scaffolding/v1/` in baker-master. Zero baker-vault writes — Mac Mini is sole vault writer per CHANDA #9; AI Head post-merge SSH-mirrors.

Unblocks KBL_INGEST_ENDPOINT (M0 row 3) and future `kbl/people_registry.py` + `kbl/entity_registry.py` loader briefs.

## `git diff --stat`

```
 vault_scaffolding/v1/entities.yml               |  35 ++++++
 vault_scaffolding/v1/people.yml                 |  33 +++++
 vault_scaffolding/v1/schema/VAULT.md            | 158 ++++++++++++++++++++++++
 vault_scaffolding/v1/schema/templates/entity.md |  40 ++++++
 vault_scaffolding/v1/schema/templates/matter.md |  47 +++++++
 vault_scaffolding/v1/schema/templates/person.md |  39 ++++++
 6 files changed, 352 insertions(+)
```

## Per-file line counts

| File | Lines |
|---|---|
| `vault_scaffolding/v1/schema/VAULT.md` | 158 |
| `vault_scaffolding/v1/schema/templates/matter.md` | 47 |
| `vault_scaffolding/v1/schema/templates/entity.md` | 40 |
| `vault_scaffolding/v1/schema/templates/person.md` | 39 |
| `vault_scaffolding/v1/entities.yml` | 35 |
| `vault_scaffolding/v1/people.yml` | 33 |
| **Total** | **352** |

Brief estimated ~345 lines. Actual 352. Within tolerance (+2%).

## Main baseline pytest (pre-branching)

```
$ /Users/dimitry/bm-b2/.venv312/bin/pytest tests/ 2>&1 | tail -3
ERROR tests/test_mcp_vault_tools.py::test_mcp_dispatch_baker_vault_list_returns_json
ERROR tests/test_mcp_vault_tools.py::test_read_rejects_symlink_escape_outside_ops
====== 19 failed, 830 passed, 21 skipped, 9 warnings, 19 errors in 11.26s ======
```

Recorded on main `7074242` (post-PR #51 merge + dispatch commit) before creating `kbl-schema-1`.

## 10 Quality Checkpoints — literal outputs

### 1. YAML frontmatter parses — VAULT.md

```
$ python3 -c "import yaml; raw = open('vault_scaffolding/v1/schema/VAULT.md').read(); fm = raw.split('---')[1]; d = yaml.safe_load(fm); assert d['type'] == 'schema' and d['version'] == 1"
(clean — zero output, zero error)
```
**PASS.**

### 2. YAML frontmatter parses — 3 templates

```
$ for f in vault_scaffolding/v1/schema/templates/*.md; do python3 -c "import yaml; raw = open('$f').read(); fm = raw.split('---')[1]; yaml.safe_load(fm)" || { echo "FAIL: $f"; exit 1; }; done && echo "All 3 templates parse OK."
All 3 templates parse OK.
```
**PASS.**

### 3. Template frontmatter has all 7 required keys

```
$ for f in vault_scaffolding/v1/schema/templates/*.md; do n=$(grep -E '^(type|slug|name|updated|author|tags|related):' "$f" | wc -l | tr -d ' '); [ "$n" = "7" ] || { echo "FAIL: $f has $n keys"; exit 1; }; done && echo "All 3 templates have 7 frontmatter keys."
All 3 templates have 7 frontmatter keys.
```
**PASS.**

### 4. Registry files load + slug uniqueness

```
$ python3 -c "
import yaml
for fn, key in [('vault_scaffolding/v1/people.yml', 'people'), ('vault_scaffolding/v1/entities.yml', 'entities')]:
    d = yaml.safe_load(open(fn))
    assert d['version'] == 1, fn
    slugs = [e['slug'] for e in d[key]]
    assert len(slugs) == len(set(slugs)), f'{fn}: duplicate slugs'
    for e in d[key]:
        assert e['slug'] not in e.get('aliases', []), f'{fn}: slug in own aliases ({e[\"slug\"]})'
        assert e['status'] in ('active', 'retired', 'draft'), f'{fn}: bad status'
print('Registry files valid.')
"
Registry files valid.
```
**PASS.** Both registries load; 2 unique people slugs (`dimitry-vallen`, `andrey-oskolkov`), 3 unique entity slugs (`brisen-capital-sa`, `brisen-development-gmbh`, `aelio-holding-ltd`); no slug appears in its own aliases; all statuses `active`.

### 5. VAULT.md structure counts

```
$ grep -c "^## §" vault_scaffolding/v1/schema/VAULT.md
9
```
**PASS.** §1 through §9 all present.

### 6. Directory structure — exactly 6 files

```
$ find vault_scaffolding -type f | sort
vault_scaffolding/v1/entities.yml
vault_scaffolding/v1/people.yml
vault_scaffolding/v1/schema/VAULT.md
vault_scaffolding/v1/schema/templates/entity.md
vault_scaffolding/v1/schema/templates/matter.md
vault_scaffolding/v1/schema/templates/person.md
```
**PASS.** Exactly 6 files, matching brief expected list.

### 7. No leaked absolute paths

```
$ grep -rn "/Users/dimitry" vault_scaffolding/ || echo "OK: no absolute paths leaked."
OK: no absolute paths leaked.
```
**PASS.**

### 8. Markdown syntax smoke (optional)

`markdownlint` not installed in environment. Skipped per brief ("optional — if available"). Content visually verified via manual re-read of diff; fenced YAML block inside §2 preserved with triple backticks.

### 9. Singleton hook still green

```
$ bash scripts/check_singletons.sh
OK: No singleton violations found.
```
**PASS.**

### 10. Full-suite regression

**Main (pre-branching, baseline):**
```
19 failed, 830 passed, 21 skipped, 9 warnings, 19 errors in 11.26s
```

**Branch (post-change):**
```
$ /Users/dimitry/bm-b2/.venv312/bin/pytest tests/ 2>&1 | tail -3
ERROR tests/test_mcp_vault_tools.py::test_mcp_dispatch_baker_vault_list_returns_json
ERROR tests/test_mcp_vault_tools.py::test_read_rejects_symlink_escape_outside_ops
====== 19 failed, 830 passed, 21 skipped, 8 warnings, 19 errors in 11.53s ======
```

**Delta:** `passed` 0 (830 → 830), `failed` 0, `errors` 0. Warnings −1 (cosmetic, DeprecationWarning count variance — unrelated to this diff). **PASS — zero regressions, content-only diff.**

## No baker-vault writes (CHANDA #9 respect)

```
$ git diff --name-only main...HEAD | grep -E "^(baker-vault|~?/baker-vault)" || echo "OK: no baker-vault paths in diff."
OK: no baker-vault paths in diff.
```
**PASS.** All 6 paths under `vault_scaffolding/v1/` in baker-master.

## Files

- **A** `vault_scaffolding/v1/schema/VAULT.md` (NEW, 158 lines) — §1–§9 schema rules
- **A** `vault_scaffolding/v1/schema/templates/matter.md` (NEW, 47 lines) — matter template
- **A** `vault_scaffolding/v1/schema/templates/person.md` (NEW, 39 lines) — person template
- **A** `vault_scaffolding/v1/schema/templates/entity.md` (NEW, 40 lines) — entity template
- **A** `vault_scaffolding/v1/people.yml` (NEW, 33 lines) — 2-seed people registry
- **A** `vault_scaffolding/v1/entities.yml` (NEW, 35 lines) — 3-seed entities registry

Total: **352 insertions, 0 deletions** across 6 new files.

## Out of scope (confirmed)

- ✅ No `baker-vault/*` writes — Mac Mini sole writer (CHANDA #9). AI Head SSH-mirrors post-merge.
- ✅ No `slugs.yml` touch — v9 authoritative for matters, unchanged.
- ✅ No `kbl/people_registry.py` / `kbl/entity_registry.py` loaders — follow-on briefs.
- ✅ No existing `wiki/` retrofit — Director-gated (`KBL_SLUGS_RETAXONOMY_1`).
- ✅ No bulk people import from CLAUDE.md — 2-seed deliberate, per-PR growth.
- ✅ No bulk entities beyond 3-seed — same reason.
- ✅ No CHANDA_enforcement.md amendment — VAULT.md is vault content, not baker-master invariant.
- ✅ No touch to `triggers/embedded_scheduler.py`, `memory/store_back.py`, `models/cortex.py`, `CHANDA.md`.

## Timebox

Target: 2–2.5h. Actual: **~25 min** (brief read + 6 writes + 10 checkpoints + PR + report). Well under — content-heavy, low-logic brief delivered verbatim from spec.

## Post-merge AI Head actions (per brief §Post-merge — NOT B-code scope)

1. **SSH Mac Mini + mirror to baker-vault** (sole writer per CHANDA #9):
   ```
   ssh macmini
   cd ~/baker-vault
   git pull -q
   BM=<path-to-baker-master-clone>/vault_scaffolding/v1
   cp $BM/schema/VAULT.md schema/VAULT.md
   mkdir -p schema/templates
   cp $BM/schema/templates/*.md schema/templates/
   cp $BM/people.yml people.yml
   cp $BM/entities.yml entities.yml
   git add schema/VAULT.md schema/templates/ people.yml entities.yml
   git commit -m 'schema(v1): seed 3-way taxonomy, templates, people.yml, entities.yml

   Director-signed: "default recom is fine (2026-04-23)"'
   git push origin main
   ```
   CHANDA #4 hook (AUTHOR_DIRECTOR_GUARD_1) will allow the commit because VAULT.md carries `author: director` frontmatter AND the `Director-signed:` quoted marker is present.
2. **Verify vault state:**
   ```
   ssh macmini "cd ~/baker-vault && ls schema/templates/ && head -10 people.yml && head -10 entities.yml"
   ```
3. **Log AI Head action** to `_ops/agents/ai-head/actions_log.md` (file paths + commit SHA in baker-vault).

## Rollback

`git revert <merge-sha>` — single-PR, clean. No runtime side-effects (nothing loads `vault_scaffolding/*` yet; loaders are follow-on briefs). Baker-vault mirror is separate commit in separate repo; its rollback is independent.

---

**Dispatch ack:** received 2026-04-23, Team 1 fifth brief this session. Ready for B3 review.
