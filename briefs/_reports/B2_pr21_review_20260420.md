---
title: B2 PR #21 DASHBOARD_COST_ALIAS_RENAME — APPROVE
voice: report
author: code-brisen-2
created: 2026-04-20
---

# PR #21 Review — DASHBOARD_COST_ALIAS_RENAME (B2)

**From:** Code Brisen #2
**To:** AI Head
**Re:** [`briefs/_tasks/CODE_2_PENDING.md`](../_tasks/CODE_2_PENDING.md) Task 1
**PR:** https://github.com/vallen300-bit/baker-master/pull/21
**Branch:** `dashboard-cost-alias-rename`
**Head commit:** `6198feb`
**Base:** `main` at `e63a836`
**Date:** 2026-04-20
**Time:** ~10 min

---

## Verdict

**APPROVE.** Scope respected exactly — pure SQL alias + downstream-consumer rename in a single endpoint. No schema change, no behaviour change, no drift into adjacent cost widgets or endpoints. CI drift guards survive the rename. CHANDA Q1/Q2 both pass (no learning-loop touch; serves the wish by closing a future-misread invitation).

---

## Scope verification (verdict focus, 1-for-1 with brief)

| Check | Result |
|---|---|
| `grep -n 'total_usd' outputs/dashboard.py` zero matches post-change | ✅ 0 matches |
| JS consumer (`outputs/static/app.js`) updated — no `data.total_usd` feeding cost widget | ✅ Line 10081 `r.total_eur` (only site) |
| Test fixture refs (3) + assertion (1) updated | ✅ Lines 161, 182, 213 (`rollup_cols`) + line 178 (assertion on `body["rollup"][0]["total_eur"]`) |
| `pytest tests/test_dashboard_kbl_endpoints.py` green per B1 | ⚠️ Local re-run blocked by known py3.9 `tools/ingest/extractors.py:275` PEP-604 landmine (reproduces on main; not this PR). Code-read + B1's CI-equivalent 9/9 report trusted per standing instruction #8 |
| Frontend `€` currency label intact | ✅ `kblFmtMoney` at `app.js:9961` + prefix at line 9964 still emit `€`; table header `'Total'` neutral |
| Schema untouched — `cost_usd` column name unchanged | ✅ `cost_usd` still appears 10× in `outputs/dashboard.py` as raw column reference (lines 7843, 9055, 9065, 9066, 9173, 9184, 9185, 10926, 10938) and 1× in `migrations/20260419_add_kbl_cost_ledger_and_kbl_log.sql` |
| Other endpoints unaffected | ✅ Repo-wide grep for `total_usd` across `*.py`, `*.js`, `*.html` returns zero (filtered `briefs/`, `.git/`) |

---

## Core diff inspection

**`outputs/dashboard.py` (2 line-edits, both inside `/api/kbl/cost-rollup`):**

- Line 10926: `AS total_usd` → `AS total_eur` (SELECT list alias).
- Line 10932: `ORDER BY total_usd DESC` → `ORDER BY total_eur DESC` (same query, alias-consistent).

Surrounding intent-comment at line 10913 (`cost_usd ledger column stores EUR values per the same module's contract`) makes the rename load-bearing for future readers — it finally matches the comment's claim.

Second query at line 10938 (`SELECT COALESCE(SUM(cost_usd), 0) AS day_total`) not touched — correct. That alias is neutral (`day_total`), column reference is raw `cost_usd`. Out of scope.

**`outputs/static/app.js:10081`:** `kblFmtMoney(r.total_eur)` — the only `_loadKBLCost` table-row field drawing from the rollup. Matches SQL alias → `_kbl_rows_to_dicts` dict key → JSON → JS. Chain consistent across all four layers.

**`tests/test_dashboard_kbl_endpoints.py`:**

- `test_kbl_cost_rollup_happy_path` (line 161 fixture cols, line 178 assertion).
- `test_kbl_cost_rollup_empty_state` (line 182 fixture cols, asserts empty list — alias name doesn't flow through).
- `test_kbl_cost_rollup_sql_uses_canonical_ts_column` (line 213 fixture cols).

Drift guard `test_kbl_cost_rollup_sql_uses_canonical_ts_column` still valid post-rename — it asserts `"WHERE ts" in q` + `"created_at" not in q` (which are untouched in this PR). The alias rename does not affect the drift guard's substring-match logic; it still guards the right thing.

Fixture-column flow reviewed: `_FakeCursor.description` (line 48) is rebuilt from the fixture's `cols` list on each `execute()`. `_kbl_rows_to_dicts` (`outputs/dashboard.py:10869`) zips off `cur.description[*].name`. Fixture name must match SQL alias for the response dict key to round-trip. Post-rename: fixture says `total_eur`, SQL says `total_eur` — consistent.

---

## Landmine checks (every review)

| Pattern | Result |
|---|---|
| Column-name drift (#34, #42) | N/A — rename is ALIAS only; column `cost_usd` unchanged in SQL. Tests still include the `ts`-canonical drift guard untouched. |
| Unbounded queries | N/A — no query shape change. |
| Missing `conn.rollback()` in except | N/A — `try/except` block at 10919-10947 left intact; HTTPException path unchanged. |
| Fixture-only tests missing real schema | Noted — tests here are structurally fixture-based. The `ts`-canonical drift guard (#42 countermeasure) is already in place and survives the rename. Not a regression. |
| LLM call signature + response access three-way match | N/A — no LLM path. |
| Wrong env var name assumption | N/A. |

---

## CHANDA pre-push test

- **Q1 (Loop Test):** No touch to Leg 1 (Gold reading), Leg 2 (ledger atomicity), or Leg 3 (Step 1 reads hot.md + ledger). Pure presentation-layer rename. **PASS.**
- **Q2 (Wish Test):** Serves the wish. The brief's motivation — "so whoever reads the SQL next doesn't misinterpret it" — is a durability/clarity improvement that reduces future-drift risk. Also modest convenience. Both sides. **PASS.**

Structural invariants §3.4-10 untouched (no director-authored files, no wiki frontmatter change, no pipeline step skip, no prompt self-modification).

---

## Nits

None.

---

## Dispatch-back

B2 verdict: **APPROVE** PR #21 at head `6198feb`. Report at `briefs/_reports/B2_pr21_review_20260420.md`. AI Head may auto-merge per brief authorization.

*— Code Brisen #2*
