# B5 — BRISEN_LAB_1 build report (2026-05-01)

**Status:** BUILT_LOCALLY_AWAITING_DIRECTOR_PROVISIONING.
**Brief:** `briefs/BRIEF_BRISEN_LAB_1.md` (1384 lines, Director-ratified 2026-04-30).
**Mailbox:** `briefs/_tasks/CODE_5_PENDING.md` (dispatched by AI Head A 2026-05-01).
**Worktree:** `~/bm-b5` (reactivated for this brief).

## Summary

All four parts built and locally verified. Open PRs **deferred until Director's GitHub repo + Render service + env-var provisioning lands** (per brief §Prerequisites + mailbox L20-21). Static QC items pass; live deployment QC items paused pending provisioning.

## Director-side prerequisites status (2026-05-01T11:35Z)

| Requirement | Status | Verification |
|---|---|---|
| GitHub repo `vallen300-bit/brisen-lab` | NOT YET CREATED | `gh repo view vallen300-bit/brisen-lab` → "Could not resolve to a Repository" |
| Render Web Service `brisen-lab` (Starter $7/mo) | NOT YET CREATED | dependent on GitHub repo |
| Render env vars `DATABASE_URL` + `FORGE_KEY` | NOT YET SET | dependent on Render service |

When Director signals these are provisioned, B5 resumes with: push staging git history to `vallen300-bit/brisen-lab`, run live deploy QCs (#1-4, #7-13, #16-20), open PRs, complete CODE_5_PENDING.

## Files built / edited

### Part 1 — Brisen Lab repo (staged at `~/brisen-lab-staging/`, ready to push)

Local git: 10-file root commit `851843a`. Will be pushed to `vallen300-bit/brisen-lab` once repo exists.

| File | Lines | Purpose |
|---|---|---|
| `app.py` | 261 | FastAPI service. Routes: `GET /healthz`, `GET /`, `GET /api/state`, `GET /sse/stream`, `POST /api/register`, `POST /api/event`, `POST /api/snapshot` |
| `db.py` | 105 | psycopg2 `ThreadedConnectionPool(min=1, max=10)`, 11-pattern `scrub_secrets()`, `bootstrap()` for 3 `forge_*` tables |
| `requirements.txt` | 4 | fastapi==0.115.0, uvicorn==0.32.0, psycopg2-binary==2.9.9, python-dotenv==1.0.1 |
| `start.sh` | 2 | `exec uvicorn app:app --host 0.0.0.0 --port "${PORT:-8080}"` |
| `render.yaml` | ~20 | Render service spec (Starter, autodeploy from main) |
| `static/index.html` | 38 | Single-page UI; cache-bust `?v=1` on JS + CSS |
| `static/app.js` | 196 | Vanilla JS, hub-and-spoke render, SSE client. **Zero `innerHTML`/`outerHTML`/`insertAdjacentHTML`/document-write** (QC #15 verified) |
| `static/styles.css` | 165 | Dark theme, status-dot color coding (working/idle/waiting), modal dialog |
| `README.md` | 86 | Architecture overview, endpoint table, env var reference |
| `.gitignore` | 6 | `__pycache__/`, `*.pyc`, `.venv/`, `.env*`, `.DS_Store` |

Sync test (DATABASE_URL stub): all 6 expected app routes register via FastAPI route table.

### Part 2 — MacBook daemon (`~/forge-agent/`, local-only)

| File | Purpose |
|---|---|
| `agent.py` | 305 lines. JSONL tailer + worktree poller. Two-layer secret scrubber (matches db.py patterns). Unmapped-event buffer (cap 50, TTL 30s) flushes on first registration. Sessions cache mtime-keyed. JSONL discovery loop @ 2s; snapshot loop @ 30s. |
| `requirements.txt` | `httpx==0.27.2` |
| `sessions.json` | `{}` (initial empty mapping) |
| `session-start-hook.sh` | chmod +x; reads stdin JSON; parses `session_id` + `cwd`; atomic write to sessions.json via tempfile + `os.replace`; POSTs `/api/register` with `--max-time 5` and `\|\| true`. **Always exits 0** (smoke-tested with both `FORGE_TERMINAL=b1` set and unset; both exited 0). |

**launchd plist:** `~/Library/LaunchAgents/com.brisen.lab-agent.plist` written. `plutil` validates OK. **Not yet loaded** — Director must:
1. `python3 -m venv ~/forge-agent/.venv && ~/forge-agent/.venv/bin/pip install -r ~/forge-agent/requirements.txt`
2. Replace `__SET_BY_DIRECTOR_BEFORE_LOAD__` placeholder in plist's `FORGE_KEY` value (same as Render env var).
3. `launchctl load ~/Library/LaunchAgents/com.brisen.lab-agent.plist`

Smoke-tests passed: all 11 secret patterns scrub; buffer queue/drain logic; `_classify` for user/assistant/tool_result message types; project-dir encoding (`/Users/dimitry/bm-b1` → `-Users-dimitry-bm-b1`).

### Part 4 — Frontend

Built as part of Part 1's staging directory (`~/brisen-lab-staging/static/`). QC #15 grep verified zero unsafe-HTML APIs. JS syntax-check via `node --check` passes.

### Part 3 — SessionStart hook + zshrc + 5 settings.json

**zshrc (~/.zshrc):**
- Backed up: `~/.zshrc.bak.20260501` (1.85K).
- 2 exports added: `FORGE_KEY="__SET_BY_DIRECTOR_BEFORE_USE__"`, `LAB_URL="https://brisen-lab.onrender.com"`.
- 6 function injections (b1, b2, b3, b4, aihead1, aihead2): `FORGE_TERMINAL=<alias>` env var prefix on `claude` invocation.
- **Surgical inject (NOT replacement)** — preserved each function's existing `cd` + tab-title + AI-Head `--name`/`--append-system-prompt` flags. Brief's L859-865 proposed simpler replacements that would have wiped the persona system from `aihead1`/`aihead2`. Diverged from brief literal text in favor of preserving Director's existing operating-model flags. **Flagging this for AI Head A review.** Full diff captured in zshrc git via backup.
- b5 deliberately omitted (not a watched terminal in v1 per brief locked decision Q9). aihead_biz + baker also untouched.
- `zsh -n ~/.zshrc` syntax check: OK.
- `grep -c "^function aihead1" ~/.zshrc`: 1 (uniqueness preserved).

**5 settings.json files** (none currently tracked in git; per-clone local files):

| Path | SessionStart | PostToolUse | PreToolUse |
|---|---|---|---|
| `~/Desktop/baker-code/.claude/settings.json` | NEW | preserved | preserved |
| `~/bm-b1/.claude/settings.json` | NEW (file created) | — | — |
| `~/bm-b2/.claude/settings.json` | NEW (file created) | — | — |
| `~/bm-b3/.claude/settings.json` | NEW (file created) | — | — |
| `~/bm-b4/.claude/settings.json` | NEW (file created) | — | — |

All 5 parse as valid JSON; all 5 reference `/Users/dimitry/forge-agent/session-start-hook.sh` with `timeout: 10`. (`~/bm-b5/.claude/settings.json` deliberately NOT modified — b5 is the build-pool spare, not in the watched-6 set.)

## QC results

### Static QCs (verifiable without deployment)

| # | Item | Status |
|---|---|---|
| 5 | All 6 shell functions in `~/.zshrc` carry `FORGE_TERMINAL=<alias>` prefix | ✅ PASS (b1-b4 + lead + deputy injected; b5 + baker + aihead_biz untouched per design) |
| 6 | All 5 `.claude/settings.json` files contain SessionStart hook entry; existing PostToolUse/PreToolUse hooks still present in Desktop | ✅ PASS |
| 14 | No literal FORGE_KEY value or DATABASE_URL DSN in any committed file | ✅ PASS (regex-scanned brisen-lab staging git tree; only the placeholder strings present) |
| 15 | Frontend XSS check: `grep -nE "innerHTML\|outerHTML\|insertAdjacentHTML\|document\.write" static/app.js` returns zero matches | ✅ PASS |

### Live QCs (paused — require deployed Render service + loaded launchd agent)

| # | Item | Status |
|---|---|---|
| 1 | `https://brisen-lab.onrender.com/healthz` → `{"ok": true}` | ⏸ pending Render deploy |
| 2 | `\dt forge_*` shows three tables | ⏸ pending Render deploy (bootstrap runs on first startup) |
| 3 | `launchctl list \| grep brisen.lab-agent` shows running | ⏸ pending Director-side `launchctl load` |
| 4 | `~/forge-agent/agent.log` ≥ 12 snapshot POSTs after 2 min | ⏸ pending agent load |
| 7 | Launching `aihead1` registers row in `forge_sessions` ≤ 5s | ⏸ pending full stack |
| 8 | Browser dashboard renders 6 cards in hub-and-spoke | ⏸ pending Render deploy |
| 9 | SSE indicator shows "live" ≤ 3s of page load | ⏸ pending Render deploy |
| 10 | Sending a prompt flips card to `status-working` ≤ 2s | ⏸ pending full stack |
| 11 | Click card → detail modal opens | ⏸ pending Render deploy |
| 12 | Tab restart hydrates from `/api/state` | ⏸ pending Render deploy |
| 13 | Render manual redeploy → reconnect, daemon keeps POSTing | ⏸ pending Render deploy |
| 16 | Connection pool stress (10 evt/sec, 5-conn DB cap) | ⏸ pending Render deploy |
| 17 | Test prompt with `sk-test1234567890123456789012345` → `[REDACTED]` in DB | ⏸ pending full stack |
| 18 | `EXPLAIN DELETE FROM forge_events WHERE occurred_at < NOW() - INTERVAL '14 days'` runs | ⏸ pending Render deploy |
| 19 | Unmapped-event buffer: prompt at session start preserved | ⏸ pending full stack |
| 20 | SSE idle ≥ 2 min stays alive (no reconnect toggle) | ⏸ pending Render deploy |

**Static QCs: 4/4 pass. Live QCs: 0/16 run (paused).**

## Divergences from brief literal text

1. **`~/.zshrc` aihead1/aihead2 functions** — brief L859-864 proposed simple replacement (`function aihead1() { printf "\033]0;Lead\007"; FORGE_TERMINAL=lead claude "$@"; }`). Existing functions in `~/.zshrc` carry the AI-Head persona system (`cd ~/Desktop/baker-code && claude --name "AI Head A" --append-system-prompt "..."`) which a literal replacement would have wiped, breaking the bank-model session-init protocol described in `_ops/processes/ai-head-autonomy-charter.md`. Did surgical `FORGE_TERMINAL=` env-var prefix inject instead. Diff visible via `diff ~/.zshrc.bak.20260501 ~/.zshrc`. Flagged here for AI Head A review.

2. **PR opening deferred** — mailbox L48-49 says "for the baker-master `.claude/settings.json` edits, open ONE PR". The 5 settings.json files have absolute Director-machine-specific paths and are intentionally per-clone (not currently tracked in git anywhere). Committing them to git as-is would create cross-clone hook references (e.g., b1 invoking `Desktop/baker-code/.claude/hooks/syntax-check.sh`). Per brief L47 ("After Part 4 verifies, open PR(s)"), I'm holding the PR(s) until full deploy verification can run. Specifically requesting AI Head A guidance on whether the baker-master PR should:
   - Track `.claude/settings.json` files in git (would normalize 5 per-clone files into 1 tracked source — but absolute-path issue), OR
   - Add a doc note in `_ops/processes/brisen-lab-session-start-hook.md` documenting the local edits (audit trail without polluting clone-local config), OR
   - Track only the brisen-lab repo PR and skip a baker-master PR (since the settings.json edits are per-clone, not tracked).

## Next actions (when Director signals provisioning ready)

1. Director creates `vallen300-bit/brisen-lab` (empty repo, init with one commit OR empty).
2. Director creates Render Web Service `brisen-lab`, Starter plan, autodeploy from `main`, connected to new GitHub repo.
3. Director sets Render env vars: `DATABASE_URL` (Neon DSN, same as baker-master) + `FORGE_KEY` (`openssl rand -hex 32` output).
4. Director sets same `FORGE_KEY` value into `~/.zshrc` (replace `__SET_BY_DIRECTOR_BEFORE_USE__`) AND `~/Library/LaunchAgents/com.brisen.lab-agent.plist` (replace `__SET_BY_DIRECTOR_BEFORE_LOAD__`).
5. B5 resumes: `cd ~/brisen-lab-staging && git remote add origin git@github.com:vallen300-bit/brisen-lab.git && git push -u origin main`. Render auto-deploys.
6. Director runs `python3 -m venv ~/forge-agent/.venv && ~/forge-agent/.venv/bin/pip install -r ~/forge-agent/requirements.txt && launchctl load ~/Library/LaunchAgents/com.brisen.lab-agent.plist`.
7. B5 runs live QCs #1-4, #7-13, #16-20.
8. B5 opens PR(s) per AI Head A guidance on the baker-master PR question above.
9. B5 overwrites `briefs/_tasks/CODE_5_PENDING.md` with COMPLETE + summary.

## Lessons noted (for `tasks/lessons.md` after deploy)

Will append after live QC complete:
- launchd plist `EnvironmentVariables` quirks on macOS 14/15 (specifically: `KeepAlive` semantics if Python crashes during venv-activation).
- Whether the unmapped-event buffer's 30s TTL is too short under cold-start `python3` invocations (relevant only if the SessionStart hook's python3+curl chain ever exceeds 30s).
- Any divergence between Claude Code's `session_id` field name in JSONL versus `sessionId` (currently `agent.py` checks both).
- `~/.zshrc` editing pattern when existing functions carry significant body (surgical inject > replacement).
