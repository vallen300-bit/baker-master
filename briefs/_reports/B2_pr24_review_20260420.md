---
title: B2 PR #24 FEEDLY_WHOOP_KILL — APPROVE
voice: report
author: code-brisen-2
created: 2026-04-20
---

# PR #24 Review — FEEDLY_WHOOP_KILL (B2)

**From:** Code Brisen #2
**To:** AI Head
**Re:** [`briefs/_tasks/CODE_2_PENDING.md`](../_tasks/CODE_2_PENDING.md) (commit `6823521`)
**PR:** https://github.com/vallen300-bit/baker-master/pull/24
**Branch:** `feedly-whoop-kill`
**Head commit:** `9600168`
**Base:** `main` at `4b61453`
**Date:** 2026-04-20
**Time:** ~15 min

---

## Verdict

**APPROVE.** Scope respected — seven files touched, every code-layer reference to Whoop and Feedly removed cleanly, schema preserved, test count unchanged (588 both sides). Zero Whoop/Feedly mentions remain in any `.py` file or in the active agent-definition layer. Residual mentions live only in historical/design-doc files, captured as informational nits with follow-up scope. CHANDA Q1/Q2 pass.

---

## Scope verification (verdict focus, 1-for-1 with brief)

| Check | Result |
|---|---|
| Whoop residuals outside archive/frozen carve-out | See N1-N3 below — zero `.py` refs, zero in live agent definitions. Root-level design-doc briefs still mention, flagged informational |
| Feedly residuals outside archive/frozen carve-out | See N1 — zero `.py` refs; same root-level BRIEF_*.md cluster |
| `whoop_records` table NOT dropped | ✅ No migration files touched (`git diff main..HEAD -- migrations/` empty). DDL previously in `store_back._ensure_whoop_tables` is gone but the live table in Neon persists. |
| `rss_feeds` / `rss_articles` tables untouched | ✅ No migration diff; `triggers/rss_trigger.py`, `triggers/rss_client.py` logic preserved — only the OPML parser docstring generalized |
| No regressions — pytest count matches main baseline | ✅ `grep -rE "^(async )?def test_" tests/` returns 588 on both main and branch. Zero tests reference `whoop` or `feedly` (grep clean). Collection still hits the known py3.9 `tools/ingest/extractors.py:275` PEP-604 landmine unchanged by this PR; trust B1's 16/596 match report |
| No schema changes. No migration added | ✅ Confirmed |
| Other `_ensure_*` methods intact (Qdrant collection ensures, signal_queue ensures, etc.) | ✅ `diff` of `_ensure_*` method names on `main` vs branch shows **exactly one** removal: `_ensure_whoop_tables`. Every other method name preserved |
| Do NOT execute the 3 Tier B post-merge items flagged in PR description | ✅ Acknowledged — not touched. Noted for AI Head handoff below |

---

## Per-file diff inspection

**`memory/store_back.py` (−134):** Two complete method bodies removed at the right boundaries.

- `_ensure_whoop_tables` block (ex-line ~1427): DDL for `whoop_records` + commit/rollback scaffolding. Clean removal. Zero callers in any `.py` file (grep clean).
- `upsert_whoop_record` block (ex-line ~3272): full INSERT...ON CONFLICT...DO UPDATE body with content-hash dedup. Clean removal. Zero callers in any `.py` file (grep clean).
- Surrounding class `SentinelStoreBack` parses cleanly (`python3 -c "import ast; ast.parse(...)"` green).
- File still contains 112 `_ensure_*` references (was 113 on main — exact delta of one, matching `_ensure_whoop_tables` removal).

**`orchestrator/agent.py` (−3 lines, 3 inline edits):** All inside `TOOL_DEFINITIONS` for `browse_website`.

- Line 628: removed "WHOOP" from Chrome-session example (now `"WhatsApp, Gmail, Dropbox, etc."`).
- Line 642: URL example flipped from `shop.whoop.com/us/en/products/` to neutral `example.com/products/`.
- Line 692: action description example flipped from `"Click Add to Cart for WHOOP 4.0 Band ($49)"` to `"Click Add to Cart for Item Name ($49)"`.

No logic change. Tool definition schema intact.

**`orchestrator/complexity_router.py` (−1 regex token):** `_DEEP_FORCE` compiled regex shopping-keywords alternation dropped `|whoop` between `amazon` and `rode`. Regex compiles (file parses). Semantically: shopping-intent triage will no longer upgrade "whoop" queries to deep-path. Correct — the sensor is gone.

**`triggers/rss_client.py` (−1 word):** Docstring at `_parse_opml_content` — "Feedly exports" → "OPML exports often". Implementation unchanged. Good — nested `<outline>` OPML format is a standard, not Feedly-specific.

**`.claude/agents/ai-head.md` (−1 phantom line):** Deleted `whoop_trigger.py — Health data (24h)` from the sentinels list + flipped header count `10 sentinels → 9 sentinels`. Confirmed no `triggers/whoop_trigger.py` file exists in the repo (`ls triggers/whoop*` → no matches); the line was purely phantom documentation.

**`.claude/agents/baker-it.md` (−1 token in MCP-integrations list):** "Gmail, Slack, ClickUp, Todoist, **Whoop**, etc." → "Gmail, Slack, ClickUp, Todoist, etc."

**`.claude/agent-memory/ai-head/MEMORY.md` (−1 +1 line):** Sentinel-health status string updated from `"10 sentinels tracked, Todoist/Whoop were down (credential issues)"` to `"9 sentinels tracked (Whoop retired 2026-04-20, Feedly retired 2026-04-20)"`. The only "whoop" mention remaining in this file is the retirement annotation itself — intentional.

---

## Landmine checks (every review)

| Pattern | Result |
|---|---|
| Column-name drift (#34, #42) | N/A — no SQL column reads/writes added. Only deletions of existing code. |
| Unbounded queries | N/A — deletions only. |
| Missing `conn.rollback()` in except | N/A — deleted methods contained proper rollback already; remaining methods untouched. |
| Fixture-only tests missing real schema | N/A — no new tests. |
| LLM call signature + response access match | N/A — no LLM code path. |
| Wrong env var name assumption | N/A — no env var usage added. |
| py3.9 PEP-604 landmine | Clean — no new `X \| None` syntax introduced. All four touched `.py` files parse on py3.9. |
| Dangling callers of deleted methods | ✅ `grep -rn "upsert_whoop_record\|_ensure_whoop_tables" . --include="*.py"` returns zero matches. |
| Dangling imports (e.g., `import json` left over if only whoop used it) | Spot-checked `store_back.py` — `json` still used throughout unrelated methods. No import-list cleanup required. |
| Regex syntax after token drop | `_DEEP_FORCE` regex re-parses (file imports cleanly). |

---

## CHANDA pre-push test

- **Q1 (Loop Test):** Pure deletion of sensor infrastructure (Whoop DDL + upsert + doc/regex mentions) and docstring generalization (Feedly → OPML). No touch to Leg 1 (Gold reading), Leg 2 (Director-action ledger atomicity), Leg 3 (Step 1 `hot.md` + ledger reads). `_init_schema` call graph untouched; only the retired method was removed from the chain. **PASS.**
- **Q2 (Wish Test):** Director-authorized retirement (2026-04-20 per brief). Removes silent cruft that invites future drift — a Chief-of-Staff reading `.claude/agents/ai-head.md` today would see `whoop_trigger.py` and search for a file that doesn't exist, wasting cycles. Removing dead documentation is the clarity side of the wish. Not convenience; serves the wish. **PASS.**

Structural invariants §3.4-10 untouched (confirmed via `git diff --stat`: no wiki files, no `author: director` files modified, no prompt self-modification, no Mac Mini agent writer path change).

---

## Nits

**N1 (informational, follow-up scope):** Root-level design-doc briefs still mention Whoop / Feedly. These are not live code but they're also not in the brief's explicit archive carve-out (`tasks/lessons.md`, `briefs/_handovers/`, `briefs/_reports/`):

| File | Type of reference |
|---|---|
| `BRIEF_PHASE_4_SCOPE.md` | Lists "10 data sources" incl. WHOOP (line 12); Feedly Pro+ upgrade item already marked DECIDED=drop (lines 49, 51, 95) |
| `BRIEF_COCKPIT_V3.md:216` | Cockpit intelligence-feed panel references "RSS feed items (Feedly)" |
| `BRIEF_SENTINEL_HEALTH_1.md` | Lists `triggers/whoop_trigger.py` as a sensor (lines 68, 179, 209) |
| `BRIEF_BROWSER_AGENT_1.md` | Uses Whoop as the narrative example for browser-agent capability ("Order me a Whoop band") |
| `BRIEF_CITATION_CONFIDENCE_1.md:52` | Citation example: `"Recovery was 72% [Source: Whoop, 12 Mar 2026]"` |

These are design-doc cruft; their presence does not break running code. Recommend a follow-up pass (out of scope for this PR) to either mark each brief `status: superseded` in frontmatter or update the examples. If AI Head agrees, I can draft a mini-brief.

**N2 (informational):** `strategy/BAKER_BACKLOG_v1.md` still has active-looking Whoop items (A5 "Health-aware scheduling", G1 "Whoop OAuth fix"). The v2 successor (`strategy/BAKER_BACKLOG_v2.md:30`) already announces `A5 (health-aware scheduling — Whoop killed), G1 (Whoop OAuth — killed)`, so the signal is captured in the active copy. Recommend marking v1 superseded if not already.

**N3 (informational):** `tasks/lessons.md:128` mentions "Whoop dead (22 days)" in historical narrative. Explicitly in brief carve-out. No action needed — documenting for completeness.

None of N1-N3 block merge.

---

## Post-merge (AI Head Tier B) — acknowledged, not executed

Per brief's explicit instruction, I did NOT execute these. Flagging for AI Head:

1. Render env-var scan — strip any `WHOOP_*` / `FEEDLY_*` keys from the production service (the code no longer reads them, but leaking secrets in env is a hygiene issue).
2. `trigger_watermarks` row delete SQL — one-time DELETE for `trigger_name IN ('whoop', 'feedly')` (or whatever the convention is; `baker_watermarks` table inspection needed to confirm column + names).
3. `CLAUDE.md` Dropbox edit — root `CLAUDE.md` lists `Whoop` / `Feedly` in the Tools & Integrations table. Director must edit directly (author: director carve-out, CHANDA Inv 4).

---

## Dispatch-back

B2 verdict: **APPROVE** PR #24 at head `9600168`. Report at `briefs/_reports/B2_pr24_review_20260420.md`. AI Head may auto-merge per Tier A protocol.

Three informational nits (N1-N3) flag design-doc cruft for follow-up; none block. Three Tier B post-merge items acknowledged and left for AI Head.

*— Code Brisen #2*
